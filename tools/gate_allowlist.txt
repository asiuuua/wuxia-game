# gate_allowlist.txt — gate_check.sh 的"预期 ERROR"白名单
# 每行一个固定子串；run_all 输出的 ERROR: 行若包含其中任意一条，视为负向测试的
# 预期错误，不判负。若新增了合法的预期错误，把其特征串加进本文件即可。
# 注意：真实 bug 产生的 ERROR 不应匹配这些串，否则会被放过——保持串具体。
String formatting error
配置不存在
NPC 不存在
物品不存在
Parse JSON failed
存档解析失败
# 以下两条为 Godot headless 退出期引擎关闭噪声（创建纹理/字体后退出必现，非真实 bug）
leaked at exit
resources still in use at exit
# 以下为背包窗口 InventoryScreen 在 run_all 测试 teardown 期的良性警告（树忙时同步 remove_child）；
# 仅警告、不崩、套件仍全过，非真实 bug。UI 窗口经评估：原写法 queue_free 模式安全，不加延迟删除
# 以免双重释放崩溃，故列入白名单（对应派单 748ef82c1731，背包窗口可后续做正规 call_deferred 重构）。
busy adding/removing children
