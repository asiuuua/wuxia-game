# 变更通告 · v2 HUD 四面板拆分落地

- **窗口**：UI 窗口（收口）
- **日期**：2026-08-28
- **关联方案**：`docs/HUD常驻系统落地方案v2_2026-08-29.md`
- **提交**：`[master 662c32f] [UI 窗口] v2 HUD 四面板拆分落地`（13 文件，+660/-296）

## 一、改动文件清单

### 新增（UI 主权）
- `scenes/ui/overlays/hud/status_card_panel.gd`（左上角状态卡：头像/姓名/等级/银两 + 气血/内力/六维条）
- `scenes/ui/overlays/hud/top_right_menu_panel.gd`（姻缘按钮+红点 + 菜单按钮）
- `scenes/ui/overlays/hud/quest_track_panel.gd`（任务追踪列表 + 目标进度）
- `scenes/ui/overlays/hud/skill_bar_panel.gd`（快捷栏 6 槽，display-only 灰发）
- `tests/unit/test_hud_panels.gd`（四面板自验：实例化/类型/不崩/6 槽）

### 修改（UI 主权）
- `scenes/ui/overlays/hud/Hud.gd`：重构为纯四面板容器（`_ready` 仅 `.new()` 四个面板并 `add_child`；旧 `_build_*`/`_refresh`/`_exit_tree` 全部下沉到各面板）
- `autoload/EventBus.gd`：**共享地基纯追加** 3 信号（见下表）
- `services/quest/quest_service.gd`：**任务窗主权，用户『你直接搞』授权跨窗** 补 3 处 `notify_quest_track_changed.emit()`

### 生成（工具重跑）
- `docs/契约总表.md`：`gen_contract.gd` 重跑（信号 92 · 公开方法 296）

## 二、共享地基增量表（信号/枚举末尾追加，绝不动已有项）

| 文件 | 类型 | 增量 | 备注 |
| --- | --- | --- | --- |
| `autoload/EventBus.gd` | 信号（纯追加） | `notify_quest_track_changed()` | 任务窗在 accept/turn_in/reset emit |
| `autoload/EventBus.gd` | 信号（纯追加） | `notify_skill_bar_changed()` | 武学窗在 装备/卸下 emit |
| `autoload/EventBus.gd` | 信号（纯追加） | `notify_skill_cd_update(skill_id: String, remain_time: float)` | 武学窗冷却推进 emit（秒） |

> 三条信号均加 `@warning_ignore("unused_signal")`，仅声明、不在 UI 窗 emit（谁产生谁 emit）。

## 三、跨窗改动说明（需打招呼）
- **任务窗 `quest_service.gd`**：在 `accept()`（tracked_ids.append 后）、`turn_in()`（tracked_ids.erase 后）、`reset()`（tracked_ids.clear 后）各追加 `EventBus.notify_quest_track_changed.emit()`。属非阻塞的简单追加，经用户『你直接搞』授权由 UI 窗一并落地；任务窗后续若有本窗 WIP 请自行 rebase。
- **武学窗 `ability_service.gd`**：未改动。冷却与 `notify_skill_*` emit 已派单（见第四节），待武学窗主权内实现。

## 四、遗留 / 派单
- **派单 `9ffe590492e9`（UI窗口 → 武学窗口，open）**：实现快捷栏技能冷却并补齐信号 emit。
  - `equip_combat_skill` / `unequip_combat_skill` 处 emit `notify_skill_bar_changed()`；
  - 冷却计时期间持续 emit `notify_skill_cd_update(skill_id, remain_time)`（秒），归零 emit `remain_time=0` 清除读秒；
  - 技能栏 `skill_bar_panel.gd` 已订阅两信号并展示，**无需 UI 窗再改**。
- **技能栏本期为 display-only 灰发**：无冷却数据时仅展示已装备武学图标+名称，读秒层默认隐藏。

## 五、自验结果（双闸门）
- 门禁① `--headless --quit`：**零** SCRIPT/Parse/Compile ERROR。
- 门禁② `run_all.tscn`：**18 套件全绿，0 ✗**（新增 `test_hud_panels` 3/3 通过）。
- 修复记录：`quest_track_panel.gd` 初版 `var title := cfg.get(...)` 触发项目 "inferred from Variant" 警告（视为错误）→ 改为显式 `var title: String = ...` 后转绿。
