# 变更通告 · 战斗模块核检修复（2026-08-29）

> 本通告对应 `docs/战斗模块核检报告_2026-08-29.md` 中"战斗窗口主权、可立即修复"项。
> **治理铁律**：战斗窗口不自 `git commit`，只产代码 + 本通告 + 精确文件清单，交 UI 模块统一收口（禁 `-A`/`.`）。

## 一、本次修复项（战斗窗口主权，已动手）

| # | 严重度 | 核检报告条目 | 修复内容 | 文件 |
|---|--------|--------------|----------|------|
| ⑤ | 🟠中 | 自动战斗无回合上限（死循环卡死） | `_on_auto_pressed` 的 `while not is_over()` 增加 `MAX_AUTO_ROUNDS=200` 上限，超限强制停手并 `finalize`，消除双方互不可杀时的死循环 | `scenes/gameplay/battle/BattleScene.gd` |
| ④ | 🟠中 | ATB 未真实插队（顺序条只是装饰） | 新增 `get_round_sequence()`（SEQ：玩家先；ATB：按 `effective_charge_rate` 降序）与 `enemy_act(enemy_id)`（单敌行动）；`enemy_phase()` 改为按序列驱动；演出层手动/自动路径按序列插队（快敌先于玩家行动）。ATB 模式从此真实生效 | `services/combat/combat_core.gd`、`services/combat/combat_service.gd`、`scenes/gameplay/battle/BattleScene.gd` |
| ② | 🟠中 | 旧伤害路径地雷（绕过 M3-2） | 删除全工程零调用方的 `CombatCharacter.basic_attack`；`take_damage` 加警示注释（仍被 `ability_service.gd:60` 引用，**不可删**，归 ability 窗口） | `data/runtime/combat_character.gd` |
| 逃跑 | 🟡低 | 逃跑判定用全局 `randf()`（不可复现） | `try_escape` 改为走内核 `SeededRNG`（`_core.rng.randf()`），`_core` 为空时回退 `randf()` | `services/combat/combat_service.gd` |
| 顺序条 | 🟡低 | 顺序条含死亡单位 | `action_order()` 仅纳入存活单位 | `services/combat/combat_core.gd` |
| HUD 装配 | 🟠中 | 敌人 HUD 在 `_ready()` 前调用 `setup()`，致 `_name` 为 `Nil`，入宗试炼等真实战斗**运行时崩溃** | `BattleScene._build_units()` 改为先 `add_child(hud)` 再 `hud.setup(...)`；`UnitHud._tween_bar()` 加 `is_inside_tree()` 防御，未入树直接落位 | `scenes/gameplay/battle/BattleScene.gd`、`scenes/gameplay/battle/unit_hud.gd` |

## 二、本次新增测试（战斗窗口）

- `tests/unit/test_battle_turn_order.gd`（3 项）：
  - `test_atb_round_sequence_puts_fastest_first` — ATB 序列首位为最快敌 `bandit_002`(speed11) > 玩家(10) > `bandit_001`(8)，断言插队生效。
  - `test_enemy_act_emits_events` — 单敌行动接口 `enemy_act_events` 产出非空事件流且含其 `TURN_START`。
  - `test_action_order_sorted_desc_and_alive_only` — 顺序条按速度降序且仅存活单位。

## 三、双闸门结果

- ① `Godot --headless --quit`：**零 SCRIPT/PARSE/COMPILE 错误**。
- ② `run_all.tscn`：**套件 通过 13 · 失败 0**（新增 `test_battle_turn_order` 3/0，战斗套件 `test_combat_smoke` 23/0、`test_battle_roster` 3/0、`test_battle_status_content` 2/0 全绿）。

## 四、未动手项（非战斗窗口主权，已派单/归对应窗口）

| 条目 | 归属 | 状态 |
|------|------|------|
| ① `TownScene.gd:52` 玩家掉血即崩（`player_hp_changed` 2参→1参） | gameplay/town | 已派单 `fed3f00da584`（handoff） |
| ③ 玩家 `speed` 硬编码 10（`PlayerState` 无 `speed` 字段） | 共享数据 / PlayerState 主权 | 待 PlayerState 窗口加字段后在本窗口接 `ps.speed` |
| ② 另一半：`ability_service.use_combat_skill`（`ability_service.gd:60`）走旧 `take_damage` 路径 | ability 窗口 | 建议该窗口移除对 `CombatCharacter` 战斗内伤害依赖或加注释，避免被误用在战斗内 |
| ③ 种子未存档（战斗一般不存档，低优先） | 存档窗口 | 暂缓 |

## 五、精确文件清单（交 UI 模块，禁 `-A`/`.`）

```text
services/combat/combat_core.gd
services/combat/combat_service.gd
data/runtime/combat_character.gd
scenes/gameplay/battle/BattleScene.gd
scenes/gameplay/battle/unit_hud.gd
tests/unit/test_battle_turn_order.gd
tests/unit/test_battle_turn_order.gd.uid
docs/变更通告_2026-08-29_战斗模块核检修复.md
```

提交信息建议：
```
[战斗窗口] 核检修复：自动战斗回合上限(防死循环) + ATB真实插队(get_round_sequence/enemy_act) + 删basic_attack旧伤害路径 + 逃跑改种子随机 + 修复UnitHud装配时序崩溃(入宗试炼等真实战斗闪退)；新增test_battle_turn_order(3/0)；影响：ATB模式生效、消除M3-2绕过地雷与死循环；不影响：共享地基与背包/UI窗口
```
