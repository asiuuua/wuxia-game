# 变更通告 · 武侠主题物品扩张（+28 件）

- 日期：2026-08-29
- 窗口：背包模块（兼 PM / 架构师）
- 提交权：本对话（git 已固化）
- 主题：Phase 3 内容扩张 · 武侠风味物品批量填充

---

## 一、范围与边界

- **仅改背包主权内** `data/configs/items/` 四文件（weapons / pills / equipment / materials），纯 JSON 数据，零代码改动、零越权。
- `ConfigManager` 硬编码加载此四文件，新增物品自动进入背包 / 装备 / 炼药 / 锻造数据池，无需改加载逻辑。
- **未碰**：`scenes/battles.json` / `npcs/enemies.json` / `abilities/skills.json`（战斗 / 能力窗口 WIP，08-29 07:58 仍在改）；`services/bond/*`（结缘窗口 WIP）。

## 二、新增内容（武侠主题，共 28 件）

**兵器 / 暗器**（`weapons.json` +10）
- 名器：青锋剑、雁翎刀、梨花枪、打狗棒（丐帮风）、子母双环、流星锤、峨眉刺
- 暗器：袖里箭、飞蝗石、含沙射影（毒砂）

**护甲 / 饰品**（`equipment.json` +6）
- 夜行衣、软猬甲、金钟罩衣、蓑衣斗笠
- 武林令牌（门派信物）、长命锁

**疗伤圣药**（`pills.json` +5）
- 九花玉露丸、黑玉断续膏、续命金丹、醉仙灵芙、五宝花蜜酒

**锻造 / 炼药名材**（`materials.json` +7）
- 玄铁、寒铁、天蚕丝、百年人参、雪山雪莲、蛊毒草、硝石

## 三、数据规范

- 字段格式、`ItemFlag` 掩码完全沿用既有：weapon / armor / accessory = 67，pill = 51，material = 19。
- 仅用既有三档 `rarity`（common / uncommon / rare），不引入新枚举值，难度系统团灭丢物逻辑无需改动。
- 总数：武器 19 / 丹药 12 / 装备 15 / 材料 18 = **64 件**（原 36，净增 28）。

## 四、验证（双闸门）

- JSON 合法：四文件 `json.load` 通过，全部 `id` 唯一、无重复。
- 单测：`run_all.tscn` 中 `test_inventory_service.gd` **28 项全部通过（0 失败）**；背包加载 / 堆叠 / 装备 / 消耗逻辑未受新增数据影响。

## 五、已知阻塞（非本次引入，已通知归属窗口）

- `services/bond/bond_service.gd` 第 20 行 `func _init() -> void:` 为空函数体（仅注释、缺 `pass`），解析失败 → `class_name BondService` 未注册 → `GameManager` 引用处告警、`test_bond_service.gd` 无法 load（run_all 收尾 fail 1 项、exit 1）。
- 该阻塞属**结缘（好感）系统窗口** WIP；文档 `整改交付_2026-08-29_信号与门禁加固.md` 已记录修复点（第 20 行补一行 `pass`）。不影响背包 / 物品。
- 建议：结缘窗口补齐 `pass` 后，整体 run_all 即全绿。

## 六、交付物

- 修改：`data/configs/items/weapons.json`、`pills.json`、`equipment.json`、`materials.json`
- 文档：`docs/变更通告_2026-08-29_武侠物品扩张.md`
