# P4–P7 两套工业化实现冲突对比（2026-08-29）

> 背景：本机存在两份互不兼容的 P4–P7 工业化扩容实现，均派生自 Gitee 基线 `a7dc664`（仅 P1–P3）。
> 盲目推送任一份都会覆盖/分叉另一份工作，故先对比厘清、再定权威。

## 一、两份实现的家底

| 项 | `D:\武侠游戏`（acquire API） | `D:/wuxia-game-fresh`（load_cached API） |
|---|---|---|
| 当前 HEAD | `97a7637`（含 P5/P6/P7 + flaky 修复） | `5691625`（P4–P7，仅本地、未推 Gitee） |
| 门禁 | GATE1 零硬错；GATE2 **31 套件 0✗**（连跑 3 次墙钟种子均绿，确定性已证） | GATE2 32 套件 0✗（仅本地验证） |
| 是否推过 Gitee | P1–P3 已推（基线 a7dc664）；P4–P7 未推 | 从未推（无凭据，push fatal） |

## 二、组件与文件清单（实查）

| 组件 | `D:\武侠游戏` | `D:/wuxia-game-fresh` |
|---|---|---|
| 全局资源管理器 | `core/resource_manager.gd`（`class_name ResourceManager`，**静态**，无 Autoload） | `autoload/resource_manager.gd`（`extends Node`，**注册为 Autoload**） |
| 流式媒体加载 | `core/streaming_media_loader.gd`（**静态**） | 无独立文件，内置于 Autoload ResourceManager（`stream_resource/poll_stream`） |
| 战斗对象池 | `core/combat_entity_pool.gd`（**静态**，P4 实战引用） | 无；改用 `core/utils/pool_manager.gd`（Autoload）+ `object_pool.gd` + `pop_text_label.tscn` |
| LRU 辅助 | 自研分级回收（L1 常驻/L2 温存/L3 冷），无 LruCache | `core/utils/lru_cache.gd` + LruCache |
| 校验/契约工具 | `tools/validate_project.gd`（**新增校验器**）+ `tools/gen_contract.gd`（契约生成，SOP 引用） | `tools/gen_contract.gd`（契约生成） |
| PoolManager Autoload | 注册于 `project.godot:38`，但**全代码零引用（孤儿，未使用）** | 注册于 `project.godot:38`，**实际使用**（P4 经 ObjectPool） |

> 两仓库的 `core/utils/pool_manager.gd` / `object_pool.gd` **内容不同**（diff 非一致），但 `D:\武侠游戏` 侧完全没调用它。

## 三、接口差异（以资源管理器为例）

`D:\武侠游戏`（静态）：
```
ResourceManager.acquire_sync(path, hint) -> Variant
ResourceManager.acquire_async(path, hint, cb)   # 后台线程 + process_frame 信号泵驱动
ResourceManager.release(path)
ResourceManager.reclaim_all() / reclaim_idle() / force_reclaim_all()
ResourceManager.set_cap / set_warm_ttl / get_resource / has / get_refcount / reset / pump
```

`D:/wuxia-game-fresh`（Autoload）：
```
ResourceManager.load_cached(path[, loader]) -> Variant
ResourceManager.release(path) / evict(path)
ResourceManager.pin_resource / unpin_resource
ResourceManager.stream_resource / poll_stream
ResourceManager.is_cached / get_cached_count
```

## 四、规范契合度（项目铁律：JSON-first / Service(RefCounted) / **不增新 Autoload** / 共享地基只读追加）

| 铁律 | `D:\武侠游戏` | `D:/wuxia-game-fresh` |
|---|---|---|
| 不增新 Autoload | ✅ ResourceManager/StreamingMediaLoader/CombatEntityPool 全为静态 class；仅有的 PoolManager 注册项是**孤儿未使用**，可清理 | ❌ 新增 **2 个** Autoload（ResourceManager + PoolManager） |
| 与既有静态类风格一致 | ✅ 与 `PortraitCacheManager` 同一范式（静态工具 + class_name 全局调用） | ❌ 改走 Autoload 单例范式 |
| 功能完整度 | ✅ 超集：含 StreamingMediaLoader、CombatEntityPool、**validate_project.gd 校验器** | ⚠️ 缺 StreamingMediaLoader / CombatEntityPool / validate_project.gd |
| 大场景切换统一回收口 | ✅ `GameManager` 在 5 个切场景点调 `ResourceManager.reclaim_all()` | 未见等价集中回收口 |
| 异步不卡帧 | ✅ `acquire_async` + `process_frame` 泵（无 Autoload） | ✅ `stream_resource` 异步（但依赖 Autoload） |

## 五、结论与推荐

**以 `D:\武侠游戏`（acquire API）为 Gitee 权威版本。** 理由：

1. **合规**：P4–P7 核心组件全为静态类，符合「不增新 Autoload」铁律；fresh 版新增 2 个 Autoload 直接违规。
2. **完整**：是功能超集（多 StreamingMediaLoader / CombatEntityPool / validate_project.gd 校验器），且与既有 `PortraitCacheManager` 风格统一。
3. **可追溯**：是已推 Gitee 基线 `a7dc664` 的直接后代，推 `97a7637` 为**干净快进**，不丢历史。
4. **门禁可信**：GATE2 31 套件 0✗ 已连跑 3 次不同种子证明确定性（含本次 flaky 修复）。

`D:/wuxia-game-fresh` 工作**不推 Gitee**，作为本地参考保留或丢弃；若有个别可取思路（如 LruCache 实现、`pop_text` 场景化），应**以后续任务移植进静态范式**，而非连 Autoload 一并引入。

## 六、后续步骤（待你确认后执行）

1. **推送**：`git push origin master`（a7dc664 → 97a7637 快进）。需要 Gitee 凭据/令牌；本环境无凭据，建议你在自己电脑双击 `wuxia-git-sync.bat`，或提供令牌由我嵌入式 URL 推送并即时擦除。
2. **清理孤儿 Autoload（建议，单独小任务）**：删除 `D:\武侠游戏/project.godot:38` 的 `PoolManager` 注册行 + 未使用的 `core/utils/pool_manager.gd`、`core/utils/object_pool.gd`（先全仓 grep 确认确实零引用），使「不增新 Autoload」彻底干净。
3. **fresh 处置**：保留 `D:/wuxia-game-fresh` 作参考，不推送；如需其独有思路，另开移植任务。

## 七、风险与待确认

- ⚠️ 推送前务必确认权威版本，避免覆盖。本报告推荐 `D:\武侠游戏`。
- ⚠️ 清理 PoolManager 孤儿 Autoload 属改动 `project.godot`（敏感共享配置），需单独确认再动。
- 当前 Gitee 远端 `origin/master` 本地引用为 `gone`（无跟踪引用），但 `git push origin master` 仍可正常快进，不影响。
