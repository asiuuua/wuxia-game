# 变更通告 2026-09-02 · 工作室工具：操作日志改为左上角固定可滑动浮窗

> 模块：tools/desktop_studio　改动范围：tools/desktop_studio/index.html　commit：（待提交后补）

## 变更项
将「系统运维」页签内的操作日志块移出，改为固定在左上角的小型浮窗（#opLogFloat）：默认缩小（仅标题栏），点标题展开/收起，内含可滚动日志列表 + 刷新按钮；新增 opLogToggle 与 30 秒静默自动刷新（opLogAutoRefresh），初始化即加载。移除原 tab-sys 内的 logBox 块，logLoad 改为渲染到浮窗。纯前端改造，不影响后端逻辑。

## 变更原因
（为何要改；修复了什么 BUG / 满足什么需求）

## 影响面
（哪些模块/功能/数据会受影响；是否动共享地基）

## 回滚方案
（如何回退：git revert <commit> / 改回哪几个文件）

## 协同方需知
（其他 AI 窗口 / 模块需注意什么；是否要重跑契约总表 / 双闸门）

## 关联
- commit：（待提交后补）
- changelog：docs/更改日志.md
