# 变更通告 · UI 图标接线（菜单 / 对话立绘 / HUD 头像）

**日期**：2026-08-29
**窗口**：UI 窗口（主权）
**前置**：`docs/图标替换与美术接入规范.md`、提交 `2471691`（背包/武学/结缘三界面图标接线）
**本轮回溯提交**：待 git 收口后补

---

## 1. 改动文件清单（均属 UI 窗口主权）

| 文件 | 改动 |
|---|---|
| `scenes/ui/components/menu_item/MenuItem.gd` | 新增 `_icon` TextureRect + `_icon_id` + `set_icon(icon_id)`；`_build` 时若有图标则创建并把 `_label` 右移留位；`set_selected` 选中/未选中 x 坐标按"有图标态"区分；**不设 `set_icon` 时布局与旧版完全一致（向后兼容）** |
| `scenes/ui/screens/esc_menu/EscMenu.gd` | 创建 `MenuItem` 后调 `item.set_icon("menu/" + key)` |
| `scenes/ui/screens/main_menu/MainMenu.gd` | 同上（主菜单 6 项） |
| `scenes/ui/overlays/dialog/DialogOverlay.gd` | `_refresh_portrait` 改**双通道**：`npc/<id>`（经 `has_icon` 判断真实存在）优先，旧 `portrait` 资源路径作兼容兜底——**修复硬编码 `load()` 违规，零破坏** |
| `scenes/ui/overlays/hud/Hud.gd` | 头像加 `_avatar_tex` 层：`npc/player` 有真实图显图、否则显首字（`has_icon` 双通道） |
| `tests/unit/test_icon_wiring.gd` | 扩展至 6 项：新增 `test_menu_item_icon_wired` / `test_dialog_overlay_portrait_no_crash` / `test_hud_builds` |
| `resources/icons/menu/.gitkeep` | 新增菜单图标投放目录占位 |
| `docs/图标替换与美术接入规范.md` | 第 2/5/7/9 节更新：菜单按钮、对话立绘、HUD 头像标记为已接；`menu/` 目录纳入 |

---

## 2. 图标 id 派生约定（零数据改动）

| 接线点 | 图标 id | 美术投放位置 |
|---|---|---|
| 主菜单 / ESC 菜单项 | `menu/<key>`（key 取自 `MENU_ITEMS` 的 `key` 字段） | `resources/icons/menu/<key>.png` |
| 对话立绘 | `npc/<npc_id>`（优先） | `resources/icons/npc/<npc_id>.png` |
| HUD 玩家头像 | `npc/player` | `resources/icons/npc/player.png` |

美术按此命名把 PNG 丢进对应子目录即生效，**无需修改任何数据配置或代码**。

---

## 3. 共享地基增量

**无**。本次全部为 UI 窗口主权内改动，未触碰 `core/enums`、`autoload/EventBus.gd`、`autoload/ConfigManager.gd`、`strings.csv` 等冻结地基。

---

## 4. 跨窗 / 派单

- 无新增派单。战斗界面技能/敌人图标仍走既有派单 **`f11a954808c2`（UI→战斗）**，由战斗窗主权认领。
- `DialogOverlay` 虽修了硬编码 `load`，但保留旧 `portrait` 资源路径作为存量 NPC 配置兼容层，不影响任何它窗数据。

---

## 5. 双闸门结果

- **gate1** `--headless --quit`：零 `SCRIPT ERROR / Parse Error / Compile Error`。
- **gate2** `res://tests/unit/run_all.tscn`：15 套件 · 通过 · **失败 0 · 无 ✗**。
  - 新增 3 项全绿：`test_menu_item_icon_wired`、`test_dialog_overlay_portrait_no_crash`、`test_hud_builds`。
- 本次为纯 UI 主权改动，未卷入它窗 WIP（背包 `ConfigManager` / 共享数据 `player_state` / 战斗 `battle/*` / 结缘 `services/bond/*` / `tools/gen_contract` / `.gitignore` 均未入库）。

---

## 6. 遗留（待后续轮次）

- `BattleScene` 技能/敌人图标：派单 `f11a954808c2` 待战斗窗认领。
- `Hud` 技能栏格子：待 v2 HUD 落地（当前 HUD 无技能栏面板）。
- 状态卡 / 战斗 buff 条：待接（需 `status/` 目录与状态配置）。
