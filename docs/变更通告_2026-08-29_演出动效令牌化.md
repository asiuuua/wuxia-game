# 变更通告 · M3-3 演出动效令牌化（战斗窗口主权）

> 日期：2026-08-29　·　窗口：战斗　·　上游：M1 战斗逻辑内核 / M2 演出编排 / M3-1 敌人 AI 选技能 / M3-2 状态引擎扩护盾·反弹·复活
> 提交策略：**git 提交统一由 UI 模块执行**（用户治理规则），本窗口只产代码 + 本通告 + 对话内精确文件清单，不自 `git commit`。

## 一、目标
把 M3-2 在逻辑层落地的三个机制（护盾 / 反弹 / 复活）**可视化**，并完成战斗演出层的**动效令牌化**（M2 起 `ui_anim.json` 的 `battle` 令牌块一直缺失，Director 靠 `_FALLBACK` 兜底）。

约束：本 M3-3 **纯演出层**，不增任何战斗逻辑（逻辑 + 事件契约已由 M3-2 交付并单测覆盖 23/23 绿）；演出层对未知事件 `match` 静默跳过（M2 已验证安全），本 M3-3 显式消费 `SHIELD_ABSORB / REFLECT / REVIVE`。

## 二、改动清单（战斗窗口主权）
| 文件 | 改动 |
|---|---|
| `scenes/gameplay/battle/unit_hud.gd` | 加 `_shield_fill`（血条上方青色细盾条，y=18/高4/初始宽0）；`set_shield(shield)` 按 `shield/max_hp` 比例 tween；`play_revive()` 叠金色闪光 `ColorRect` 淡出 0.35s（服从 `_instant`） |
| `scenes/gameplay/battle/battle_view.gd` | `match` 加 `SHIELD_ABSORB→_play_shield`（设盾条+青飘字「盾N」）/ `REFLECT→_play_reflect`（按 `target_hp_after` 更新攻击者血条+紫飘字「反弹N」）/ `REVIVE→_play_revive`（设血条+`play_revive`+金飘字「复活!」）；顺带修 `_play_damage`：加 `elif ev.value > 0` 守卫，护盾全吸收时 `DAMAGE.value==0` 不再飘难看的「-0」 |
| `scenes/gameplay/battle/battle_director.gd` | `_token_for` 加 `SHIELD_ABSORB→"shield_absorb"` / `REFLECT→"reflect"` / `REVIVE→"revive"`；`_FALLBACK` 补战斗全量令牌意图时长（`damage_pop/hit_shake/turn_gap/death_fade/shield_absorb/reflect/revive`，使 UI 窗口补 `battle` 块前手感也正确） |
| `docs/M3设计_演出动效令牌化.md` | 本 M3-3 设计稿（现状/方案/验收/文件清单/风险） |

## 三、共享地基（只增不改）
- `ui_anim.json` 的 `battle` 令牌块：**本窗口不编辑**（UI 窗口主权），仅在下文给规格，交 UI 模块纯增（不触碰 `durations/easings/...` 既有键）。
- `EventBus / ConfigManager / screens.json / strings.csv / combat_event.gd(枚举) / combat_enums.gd` 均不动。
- 不增公开方法/信号 → `契约总表.md` 无需同步（gen_contract 只扫公开接口；且本 M3-3 未动任何公开接口）。

## 四、ui_anim.json · `battle` 令牌块规格（交 UI 模块纯增）
```json
"battle": {
  "damage_pop": 0.12,
  "hit_shake": 0.12,
  "gentle": 0.18,
  "fast": 0.08,
  "turn_gap": 0.1,
  "instant": 0.0,
  "death_fade": 0.4,
  "shield_absorb": 0.12,
  "reflect": 0.14,
  "revive": 0.35
}
```
（`_token_for` 对 HEAL 返回 `"gentle"`、QI/状态返回 `"fast"`，故块内需含 `gentle`/`fast`；这些在 `durations` 也已存在，重复键以 `battle` 块优先。）

## 五、双闸门状态（M3-3 完成，红的是外部阻断）
- ① `Godot --headless --quit` → 退出码 **0**，**零 SCRIPT/PARSE/COMPILE 错误**（证明全工程含本 M3-3 三个演出脚本均编译通过）。
- ① 补充：`--check-only --script` 对 `battle_view.gd` / `unit_hud.gd` / `battle_director.gd` 三个文件**逐一零错误**，排除任何 autoload 误报。
- ② `run_all.tscn` → **当前为红，但失败 100% 在背包窗口的未提交 WIP，与 M3-3 无关**：
  - 报 `SCRIPT ERROR: Parse Error: Could not parse global class "InventoryService"` from `res://services/inventory/inventory_service.gd`。
  - 精确错误：`Parse Error: Expected indented block after "for" block`（背包窗口半截编辑遗留的 `for` 循环体缺失缩进）。
  - 级联：`InventoryService` 不能解析 → `GameManager`/`save_validator`/`defeat_handler` 等 autoload 全部加载失败 → **所有测试**（含战斗套件）连带崩。
  - **本 M3-3 的三个文件不引用 `InventoryService`/`GameManager`，零牵连**；逻辑正确性已由 M3-2 的战斗 23/23 单测覆盖。
  - **处置**：该文件**不归战斗窗口**，我未改它（越界且可能与背包窗口进行中工作冲突；git 本就由 UI 模块统一收口）。请 UI 模块修复/恢复 `services/inventory/inventory_service.gd` 后重跑 `run_all`，预计战斗套件 **23/0 全绿**（M3-3 纯演出无新逻辑，无新增单测需求）。
- ③ 双闸门前已清 `user://saves` 残留。

## 六、交 UI 模块的精确文件清单（战斗窗口不自提交）
```
scenes/gameplay/battle/unit_hud.gd
scenes/gameplay/battle/battle_view.gd
scenes/gameplay/battle/battle_director.gd
docs/M3设计_演出动效令牌化.md
docs/变更通告_2026-08-29_演出动效令牌化.md
```
> ⚠️ 提交时**只 add 以上 5 个文件**；盘上另有背包窗口未提交的 `inventory_service.gd`（当前**解析错误，需 UI 模块修复**）、`item_instance.gd`、`test_inventory_service.gd` 与本窗口无关，切勿 `git add -A` 一并卷入。
> 提交信息建议：`[战斗窗口] M3-3 演出动效令牌化：护盾条/反弹飘字/复活特效 + Director 代币映射(battle 块未补前用_FALLBACK兜底)；影响：三机制可视化、修护盾全吸收飘-0；不影响：共享地基(EventBus/enums/ConfigManager/screens.json/strings.csv/ui_anim.json 只增令牌块由UI模块补) 与背包/UI 窗口`
> 另请 UI 模块**一并纯增** `data/configs/ui/ui_anim.json` 的 `battle` 令牌块（规格见第四节），完成「动效令牌化」闭环。

## 七、配合约定（给 UI / 演出窗口）
1. **`ui_anim.json` 的 `battle` 令牌块仍待 UI 窗口补**（M2 起就用 Director `_FALLBACK` 兜底，至今未补；本 M3-3 已在 `_FALLBACK` 补齐三机制意图时长，补块后无缝接管）。
2. **护盾条位置约定**：UnitHud 把盾条画在血条正上方一条 4px 青色细条（`unit_hud.gd` 内 `BAR_W` 同宽、比例按 `shield/max_hp`），若 UI 窗口有统一 HUD 规范可后续对齐。

## 八、下一步
- M3 全部收口（逻辑 M1/M3-1/M3-2 + 演出 M2/M3-3）。可进入 **Phase 3 内容扩张**（扩充敌人/技能/状态配置；补 `battle_bandit_001` 编成含头目 `bandit_002` 以接 M3-1 条件门控样例），或回头打磨 `BattleScene` 布局/美术。
- 阻塞项：请 UI 模块先修复 `services/inventory/inventory_service.gd` 的解析错误，再统一收口各窗口提交。
