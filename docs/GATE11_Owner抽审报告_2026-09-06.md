# GATE11 Owner 抽审报告（Q-1 收口，2026-09-06）

- 抽审窗口：AI-架构窗口(kernel) ｜ 样本：12 信号（覆盖战斗/背包/任务/内容/货币/时间/命令/结缘/交易/存档/对话/通知十二域）
- 抽审规则：①声明唯一性（EventBus 单点，结构保证）②emit 方=Owner 域或装配层（谁产生谁 emit）③connect 跨模块走事件契约 ④载荷类型（GATE21 已查，本轮不重复）

## 抽样结果：12/12 架构合法，1 项观察发现

| 信号 | emit 归属 | connect 归属 | 判定 |
|---|---|---|---|
| combat_finished | combat_service ✓ | GameManager/game_state（装配消费） | 合法 |
| inventory_item_added | inventory_service ✓ | GameManager + InventoryScreen（UI 只读展示） | 合法 |
| quest_completed | quest_service ✓ | 广播 | 合法（良性） |
| content_ready | ConfigManager 代 Registry 发（注入设计，Registry 零 EventBus 依赖）✓ | 广播 | 合法 |
| player_money_changed | **defeat_handler 发（见观察项）** | UI 三处 | ⚠ 观察项 |
| world_day_advanced | weather_time_service ✓ | 广播（B6 后转注册制+广播） | 合法 |
| cmd_start_combat | DialogOverlay（UI 发命令=Command 语义）✓ | GameManager 执行 | 合法 |
| bond_relationship_changed | bond 域三服务（同域多发）✓ | UI 三处 | 合法 |
| notify_trade_failed | shop_service ✓ | ShopScreen | 合法 |
| game_saved | SaveManager ✓ | GameManager | 合法 |
| dialogue_event_triggered | dialogue_service ✓ | dialogue_event_executor（同域） | 合法 |
| notification_show | 三源（error_handler/GameManager/ui_manager，横切面多源一收）✓ | ui_manager 聚合 | 合法 |

## 观察发现（1 项，登记不当场改）

**OBS-1：defeat_handler.gd:94 直写 `ps.silver = 0` 绕过 money API**

- 现象：死亡扣钱「先扣光现有银两」直接赋值 `ps.silver = 0` 后手动补发 `player_money_changed`——绕过 `spend_money/add_money` 标准路径。
- 风险：①绕过 API 的未来校验/日志/事务化钩子 ②B2 跨模块直写扫描的已知静态盲区（局部变量 `ps` 中转持有后直写，OWNERS 名单不含局部别名）③死亡扣钱按 0-C 语义应走事务（Mutation+Journal）。
- 建议：随死亡惩罚域批次做 0-C 事务化收编（扣钱进 TransactionRuntime，走 Mutation 登记），同时把「局部别名直写」纳入 B2 扫描器增强（别名解析或启发式）。
- 归属：defeat 域主权未明确登记——施工前先 handoff 认领。

## 结论

- GATE11 抽审**通过**：86 信号声明唯一、Owner 归属清晰、跨模块消费全部走事件契约。
- 全量基线核对：0 新增无生产方信号 / 0 未声明引用（pre-commit 0b 门禁每次提交实证）。
- 观察项 OBS-1 转入 backlog（death 域事务化 + B2 别名扫描增强）。
