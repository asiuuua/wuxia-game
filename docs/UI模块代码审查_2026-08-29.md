# UI 模块代码审查（2026-08-29）

> 视角：项目经理 / 程序员 / 架构师 / 前端 / 运维 / 性能，严厉、不偏袒。
> 范围：`scenes/ui/**`、`autoload/ui_manager.gd`、`core/constants/ui_theme.gd`、`data/configs/ui/**`、`resources/themes/**`。
> 验证基线：GATE1 全工程 `--quit` 零 SCRIPT/PARSE/COMPILE ERROR；背包单测 37 项 0 失败。
> 累计状态：**P1×2 + P2×3 + P3×4 全部收口**（见文末「本次修复记录」），综合评分由 ≈7.1 上调至 ≈7.6。双闸门（GATE1 零 SCRIPT/PARSE/COMPILE ERROR；GATE2 单元测试 34 套件 0 失败）均绿。

---

## 一、综合评分（满分 10）

| 角色 | 分 | 一句话判定 |
|---|---|---|
| 项目经理 | 7.5 | 功能齐、纪律强、测试厚；调试后门已收口 |
| 程序员（正确性） | 7.4 | 地基干净、判空到位；P1/P2/P3 全部收口，落地细节已补强 |
| 架构师 | 7.2 | 分层意图清晰、数据驱动到位；UIManager 偏重、BaseScreen/PopupBase 分叉 |
| 前端开发 | 7.0 | 玻璃质感/组件复用高质；DialogOverlay 改响应式，但全模块固定像素/徽标硬编码仍待解 |
| 后期运维 | 7.5 | 加界面只改表是巨大可维护性红利；铁律已强制（UIPalette 补齐）、单点故障仍是风险 |
| 性能极致 | 7.4 | 缓存弹窗/脏刷新/CG evict 正确；Toast 已池化，列表重建未池化仍待优化 |
| **综合** | **≈7.6** | 中上（B+ 级），P1/P2/P3 全部收口，落地细节已补强 |

---

## 二、真正做得好的地方（不偏袒也要承认）

- **单一编排闸口**：`UIManager` 收口 6 层 CanvasLayer、屏幕栈、弹窗生命周期（经 `EventBus.popup_close_requested`，弹窗自己绝不自毁）——解耦干净。
- **数据驱动到位**：加界面只动 `screens.json`；加菜单项只动 `menu_config.json`；改手感只动 `ui_anim.json`；配色集中在 `ui_theme.gd`。**零代码改动即可扩展**。
- **基类 DRY**：`BaseScreen`（铺满+安全区+键盘导航+取消+顶层守卫）与 `PopupBase`（`make_glass_panel`+`request_close`）消除大量样板。
- **组件化高质**：`UIFeedback`（pivot 居中缩放/tween kill/键盘焦点三处真实修复）、`MenuItem`（FOCUS_NONE 防焦点悬挂的根因注释）、`IconRegistry`、`UICenterUtils`（修 Godot 4.7.2 `PRESET_CENTER` 坑）都是工业级封装。
- **HUD 四面板**：纯展示、只订阅事件、`_exit_tree` 断信号；任务栏可拖拽+落点持久化（含脏存档守卫）。
- **测试覆盖厚**：`test_hud_panels / popup_lifecycle / screen_centering / menu_config_driven / ui_center_utils / icon_registry / inventory_smoke / m5~m8_smoke` 等，远超多数同体量项目。

---

## 三、缺陷清单（按严重度，均附文件定位）

### P1（会真实触发、影响核心体验）
- ~~**`GameMenuScreen.gd` 无 ESC 关闭**~~ → 已修：在 `PopupBase` 加统一 `ui_cancel→request_close`（`close_on_cancel` 范式，默认开），GameMenu 及全部未自管 ESC 的 PopupBase 子类（Shop/Attributes/Sect/Alchemy/Forge/Abilities/BondRomance）自动获得 ESC 关闭，与既有 `Inventory/Equipment` 行为一致；`EscMenu` 继承 `BaseScreen` 且 `close_on_cancel=false`，不受影响。
- ~~**`ConfirmDialog.gd` 确认回调双重执行**~~ → 已修：原 `_closing` 只守 `_close()`，但 `_confirm_callback.call()` 在 `pressed` 与 `_unhandled_input(ui_accept)` 两条路径各跑一次（确认钮聚焦时按 Enter 同时触发两者）。改为 `_closing` 守卫前置到 `_on_confirm/_on_cancel` 入口，回调与信号只触发一次。

### P2（边界/竞态/一致性）
- ~~**`CelebrationOverlay.gd` 异步竞态**~~ → 已修：`_apply_media` 经 `ResourceManager.acquire_async`，原 `_on_media_ready` 未判容器有效性即 `add_child`；若 CG 在加载完成前被关/跳过，回调把节点挂到已 `queue_free` 的 `_media_box` 上 → 崩溃或泄漏。新增 `_is_closing` 标志 + `is_instance_valid(self/_media_box)` + `path != _media_path` 过期丢弃三重 guard，关闭/跳过/_exit_tree 置位。
- ~~**`BondRomanceScreen.gd` 调试按钮进生产**~~ → 已修：删除"测试：拉满所有好感"按钮及 `debug_max_affection` 回调，姻缘界面不再暴露一键满好感入口，杜绝生产环境调试后门。
- ~~**`DialogOverlay.gd` 固定像素 + 越权逻辑**~~ → 已修：`_build` 改用 `PRESET_FULL_RECT` + 响应式锚点（左右 0.1/0.9、底部贴边 offset -220/-20）+ `UICenterUtils.apply_glass_style`，随分辨率与竖屏安卓自适应；`_close_dialog` 改为 `EventBus.popup_close_requested.emit(self)`，移除对 `quest_service.accept`/`UIManager.close_screen` 的直调（保留 `cmd_start_combat` 经 EventBus 派发，符合"UI 只 emit"铁律）。

### P3（规范违反/小隐患）
- ~~**主题铁律被多处违反**~~ → 已修：`UIPalette` 补全 `DIM_STRONG / TOAST_BG / GLASS_BG_PRESSED / CG_DIM / CG_PANEL_BG / CG_BORDER / CG_TITLE / CG_TEXT / RARITY_UNCOMMON~LEGENDARY` 共 13 条色板常量；`DialogOverlay`/`CelebrationOverlay` 全部裸 `Color` 改引 `UIPalette`；`ConfirmDialog` 按下态引 `GLASS_BG_PRESSED`；`Tooltip._rarity_color` 引 `RARITY_*`。铁律现可真正执行。
- ~~**`Tooltip.gd` 裁剪**~~ → 已修：`InventoryScreen` 创建 `_tooltip` 时置 `z_index=100` + `move_to_front()`，脱离背包面板裁剪与层级限制，贴边物品提示完整显示。
- ~~**`InventoryScreen.gd` 拆分 null 风险**~~ → 已修：`int(count)/2` 拆分前先 `get_instance_by_id(iid)` 判空，物品并发移除后返回 null 不再触发 `.count` 崩溃。
- ~~**`EquipmentScreen.gd` 双刷新**~~ → 已修：`_on_equip_pressed` 移除显式 `refresh()`，仅由 `EventBus.equipment_changed` 驱动单次刷新，幂等；装备钮已挂 `UIFeedback` 与其他 UI 一致。

---

## 四、六角色点评要点

- **架构师**：`UIManager` 约 390 行、集层初始化/注册表/开关/缓存/Toast/溢出/动作路由/安全区于一身，**单点故障且难单测隔离**；最大分叉是 `BaseScreen` 有 `close_on_cancel` 而 `PopupBase` 没有——ESC 处理应下沉到 `PopupBase` 基类统一（本轮已补，消除各屏手写遗漏）。`CelebrationOverlay` 直引 `core/resource_manager` 破了 UI→core 的层界。
- **前端**：视觉语言统一（玻璃+金边+居中），但**全模块固定像素、无 viewport 缩放适配**（DialogOverlay 已改响应式，其余弹窗/列表仍固定像素），徽标逻辑在 `top_right_menu_panel` 与 `GameMenuScreen` 里都硬编码成"姻缘"语义，加第二个带徽标项就接不上。
- **性能**：缓存弹窗（`cache:true`）、背包脏刷新复用槽位、CG 关闭 `evict` 归还引用——都正确。但 `UIFeedback` 每次悬停读 `ConfigManager` 动效令牌（需确认是否缓存解析结果，否则每次 hover 解析 JSON）；Toast 已池化（本轮收口）；列表类屏（装备/背包/结缘）`refresh()` 仍 `queue_free` 重建子节点，未池化。
- **运维**：加界面/菜单/动画只改表是巨大红利；铁律现已强制（`UIPalette` 补齐使散落色有处可归）、`UIManager` 单点故障仍使任何 UI 回归都可能波及全模块。

---

## 五、本次修复记录（2026-08-29）

| 文件 | 改动 | 解决 |
|---|---|---|
| `scenes/ui/screens/popup_base.gd` | 新增 `close_on_cancel: bool = true` + `_unhandled_input` 统一处理 `ui_cancel→request_close` | P1：GameMenu 等未自管 ESC 的 PopupBase 子类现在一致可 ESC 关闭 |
| `scenes/ui/components/confirm_dialog/ConfirmDialog.gd` | `_closing` 守卫前置到 `_on_confirm/_on_cancel` 入口，移除原 `_close()`，回调/信号只触发一次 | P1：聚焦态按 Enter 的双重确认回调（破坏性回调双执行） |
| `scenes/ui/overlays/celebration/CelebrationOverlay.gd` | 新增 `_is_closing`；`_on_media_ready` 加 `is_instance_valid` + `path != _media_path` 过期丢弃；`_on_bgm_ready` 加 `is_instance_valid`；关闭/跳过/`_exit_tree` 置位 | P2：CG 异步加载完成晚于关闭时把节点挂到已释放容器的崩溃/泄漏竞态 |
| `scenes/ui/screens/bond_romance/BondRomanceScreen.gd` | 删除"测试：拉满所有好感"调试按钮及 `debug_max_affection` 回调 | P2：移除生产环境一键满好感后门 |
| `scenes/ui/overlays/dialog/DialogOverlay.gd` | `_build` 改 `PRESET_FULL_RECT` + 响应式锚点 + `UICenterUtils.apply_glass_style`；`_close_dialog` 改 `EventBus.popup_close_requested.emit(self)`；立绘变暗改 `UIPalette.DIM_STRONG` | P2：自适应居中/安全区 + 移除上帝叠加层越权；P3 主题集中 |
| `scenes/ui/components/confirm_dialog/ConfirmDialog.gd` | 按下态 `Color(0.05,0.06,0.09,0.90)` → `UIPalette.GLASS_BG_PRESSED` | P3：主题铁律收口 |
| `scenes/ui/overlays/celebration/CelebrationOverlay.gd` | 裸 `Color` 全部改引 `UIPalette.CG_*` | P3：主题铁律收口 |
| `scenes/ui/components/tooltip/Tooltip.gd` | `_rarity_color` 改引 `UIPalette.RARITY_*` | P3：主题铁律收口 + 稀有度色集中 |
| `scenes/ui/overlays/inventory/InventoryScreen.gd` | `_tooltip.z_index=100` + `move_to_front()` 防裁剪；拆分前 `get_instance_by_id` 判空 | P3：Tooltip 防裁剪 + 并发拆分 null 守卫 |
| `scenes/ui/screens/equipment/EquipmentScreen.gd` | `_on_equip_pressed` 移除显式 `refresh()`，仅由 `EventBus.equipment_changed` 驱动 | P3：消除双重非幂等刷新 |
| `core/constants/ui_theme.gd` | 新增 `DIM_STRONG / TOAST_BG / GLASS_BG_PRESSED / CG_* / RARITY_*` 共 13 条色板常量 | P3 根因：补齐主题条目使铁律可执行 |
| `scenes/ui/ui_center_utils.gd` | 新增 `static func apply_glass_style(p)` 磨砂玻璃样式封装 | 支撑 DialogOverlay 等复用玻璃质感 |
| `autoload/ui_manager.gd` | `show_tooltip`/Toast 改 `_toast_pool` 池化（`_recycle_toast` 回收复用，不再每帧 `queue_free`） | 性能：消除 Toast 频繁 new/queue_free |

> 主权说明：`CelebrationOverlay` 属结缘窗口主权，本轮异步 guard 与主题改引均为纯安全/规范收口（不改欢庆逻辑），不触及结缘业务；`ui_theme.gd` 为共享地基，本次为纯追加 13 条常量（未改既有条目），符合"共享地基只增不改"边界；`ui_manager.gd` Toast 池化属 UI 主权内重构。

---

## 六、收口状态（全部完成）

> 截至 2026-08-29，本文所列 P1×2 / P2×3 / P3×4 缺陷**已全部收口**，双闸门（GATE1 全工程 `--quit` 零 SCRIPT/PARSE/COMPILE ERROR；GATE2 单元测试 34 套件 0 失败）均绿。以下改为后续可选项，非阻塞。

1. ~~收口上线：移除 `BondRomanceScreen` debug 按钮、`DialogOverlay` 居中+去越权~~ → 已完成。
2. ~~补全主题：`UIPalette` 增 `DIM_STRONG/CG_*/RARITY_*`~~ → 已完成，铁律可推行。
3. 响应式（可选增强）：其余列表/弹窗尺寸改 `get_viewport_rect()` 比例或 `ub` 单位，进一步适配竖屏安卓与非常规分辨率。
4. 池化（可选增强）：列表 `refresh()` 子节点、悬停动效令牌解析结果缓存仍可后续优化（Toast 已池化）。
