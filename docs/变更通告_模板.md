# 变更通告 <YYYY-MM-DD> · <主题>

> 模块：<顶层模块>　改动范围：<含子路径的范围>　commit：<sha，待提交后补>
> 责任窗口：AI-<窗口>　关联：docs/更改日志.md 对应行

## 变更项
<改了什么：文件 + 行为 + 接口签名变化>

## 变更原因
<为何要改；修复了什么 BUG（编号）/ 满足什么需求 / 性能或架构动机>

## 影响面
<哪些模块/功能/数据会受影响；是否动共享地基（EventBus/ConfigManager/core/enums/screens.json/strings.csv/GameManager/GameState）>

## 回滚方案
<如何回退：git revert <commit> / 改回哪几个文件 / 数据是否需回滚>

## 协同方需知
<其他 AI 窗口 / 模块需注意什么；是否要重跑契约总表（gen_contract.gd）/ 双闸门（GATE1+GATE2 全量）>

## 验证
<GATE1 rc=? / GATE2 套件 通过?·失败?·✗=?；相关单测>

## 关联
- commit：<sha>
- changelog：docs/更改日志.md
- 派单：<handoff id，若有>

---
> 生成方式（推荐，自动进 changelog）：
> `python tools/change_log.py notice --title "<主题>" --module <模块> --scope <范围> --what "<改了什么>" --impact "<影响>"`
> 手写方式：复制本模板到 `docs/变更通告_<YYYY-MM-DD>_<主题>.md`，并在 `docs/更改日志.md` 补一行（用 `change_log.py add`）。
