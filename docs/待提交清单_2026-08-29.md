# 待提交清单 · 2026-08-29 06:50

> 由 **UI/设置窗口** 整理，供 **背包模块** 执行提交。
> 提交权已收口到背包模块（用户 2026-08-29 06:45 拍板「git 提交交给背包模块，其他窗口只列文件清单」）；UI/战斗/背包各窗口只产代码 + 通告 + 清单，不自 `git commit`。
> **铁律**：逐窗口 `git add <本窗口文件>`，**严禁 `git add -A` / `git add .`**；提交信息格式 `[窗口名] 改动；影响：xxx；不影响：xxx`。

## 一、战斗窗口（M3 敌人 AI 进行中）— 8 项

| 状态 | 文件 | 归属 |
|---|---|---|
| M（修改） | `data/configs/npcs/enemies.json` | 战斗主权（M3 敌人配置） |
| M（修改） | `data/runtime/combat_character.gd` | 战斗主权 |
| M（修改） | `services/combat/combat_core.gd` | 战斗主权 |
| M（修改） | `services/combat/combat_service.gd` | 战斗主权 |
| ??（新增） | `docs/M3策略_敌人AI权重选技能.md` | 战斗主权（M3 策划文档） |
| ??（新增） | `scenes/gameplay/battle/battle_director.gd.uid` | 战斗主权（生成 uid） |
| ??（新增） | `scenes/gameplay/battle/battle_view.gd.uid` | 战斗主权 |
| ??（新增） | `scenes/gameplay/battle/unit_hud.gd.uid` | 战斗主权 |

**建议**：战斗窗口先跑双闸门（① `Godot --headless --quit` 零错误 ② `res://tests/unit/run_all.tscn` 零失败）确认 M3 无回归，再单独 `git add` 上述 8 项提交（建议 `[战斗] M3 敌人AI权重选技能…`）。

## 二、背包模块自身（committer）未提交改动 — 1 项

| 状态 | 文件 | 说明 |
|---|---|---|
| M（修改） | `.workbuddy/memory/2026-08-29.md` | 背包模块在 `712435c` 后追加的今日日志（未提交）；本对话(UI)对 `MEMORY.md` 的微调（命名提交权为「背包模块」）也叠加于此文件、未提交 |

**建议**：背包模块统一收口进一个记忆提交（或单独提交，随你）。**注意**：此文件当前处于「背包模块改了一半未提交」状态，其他窗口勿碰，交背包模块自己收尾。

## 三、UI/设置窗口（本对话）— 0 项待提交

设置弹窗整改已全部入库，无需再提：
- 代码 / 测试：基线 `bc70be1`
- 收尾文档 / 记忆：本对话提交 `99e2196`

## 四、本次新增的协调文档（本对话产出，待背包模块提交）

| 状态 | 文件 | 说明 |
|---|---|---|
| ??（新增） | `docs/待提交清单_2026-08-29.md` | 本清单文档本身（UI/设置窗口产出） |

**建议**：由背包模块 `git add docs/待提交清单_2026-08-29.md` 一并提交（可并入记忆提交，亦可单独 `[工程记忆]` 提交）。

## 五、提交顺序建议

1. **战斗窗口跑双闸门** → 背包模块 `git add` 战斗 8 项 → 提交（`[战斗]` 前缀）。
2. **背包模块** `git add` 记忆文件（`.workbuddy/memory/2026-08-29.md`）+ 本清单文档（`docs/待提交清单_2026-08-29.md`）→ 提交（`[背包]` 或 `[工程记忆]` 前缀）。
3. **严禁 `git add -A`**，避免跨窗口混提、丧失按窗口可追溯性。

## 七、UI 窗口 P2-1 背包界面（本对话产出，待背包模块提交）

> P2-1 已通过双闸门 + inventory_smoke（12 项全绿），变更通告见 `docs/变更通告_2026-08-29_P2-1背包界面.md`。
> 文件跨越「UI 窗口主权」与「背包窗口主权」两组，因提交权已收口到背包模块，全部由背包模块 `git add` 提交（仍须逐文件，严禁 `-A`）。

### 7.1 UI 窗口主权（其他窗口勿改）
| 状态 | 文件 | 说明 |
|---|---|---|
| ??（新增） | `scenes/ui/components/item_slot/ItemSlot.gd` | 64×64 物品槽组件（含 setup 幂等保护） |
| ??（新增） | `scenes/ui/components/tooltip/Tooltip.gd` | 悬停浮窗组件 |
| M（重写） | `scenes/ui/overlays/inventory/InventoryScreen.gd` | 背包界面实例网格化 + 右键菜单/拖拽/悬停/整理 |
| M（修改） | `data/configs/localization/strings.csv` | +5 i18n key（整理/使用/装备/丢弃/拆分） |
| ??（新增） | `tests/ui/inventory_smoke.gd` | 背包界面冒烟测试 |
| ??（新增） | `tests/ui/inventory_smoke.tscn` | 背包界面冒烟测试场景 |

### 7.2 背包窗口主权（UI 强依赖，勿改名/改参）
| 状态 | 文件 | 说明 |
|---|---|---|
| M（修改） | `services/inventory/inventory_service.gd` | 新增 3 公开方法 `split_instance`/`move_instance`/`sort_bag`（+私有 `_bag_by_name`）；并修复 `remove_instance`/`consume_instance` 并发编辑缩进损坏 |
| M（修改） | `docs/契约总表.md` | **背包窗口重跑 `tools/gen_contract.gd` 后更新**（inventory_service 段纳入 3 新增公开方法） |

### 7.3 P2-1 协调文档
| 状态 | 文件 | 说明 |
|---|---|---|
| ??（新增） | `docs/变更通告_2026-08-29_P2-1背包界面.md` | 跨窗口变更通告（P2-1） |

**建议**：背包模块提交顺序——先 `git add` 7.1 全部 + 7.3 通告 → 提交（`[UI] P2-1 背包界面实例网格化…`）；再 `git add` 7.2 的 `inventory_service.gd` → 提交（`[背包] P2-1 增补 split/move/sort 三公开方法`）；最后重跑 `gen_contract` 后 `git add docs/契约总表.md` → 提交（`[背包] 刷新契约总表`）。**接口变更须重跑 gen_contract**（见 7.2 与通告第二节 B）。

## 八、当前仓库 HEAD 参考

- `712435c` [背包窗口·协同规则] git 提交权收口到本对话(AI)，其他窗口不自行 commit；作废 UI 统一版通告
- `9e243c7` [背包窗口·代改UI] InventoryScreen 负重条接 get_max_weight()，闭合真负重链路（UI授权）
- `99e2196` [UI] 设置弹窗整改收尾：更正通告'未提交'表述 + 移交 git 提交权；入库 m8_smoke.gd.uid
