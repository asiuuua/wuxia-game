# 架构契约总表（自动生成，禁止手改）

> 生成时间：2026-08-29T06:53:13　·　生成器：`tools/gen_contract.gd`（`--headless --script res://tools/gen_contract.gd`）
>
> **代码是唯一真源**：改了任何信号/公开方法，必须重跑生成器同步本表。
> 跨模块约定：读走服务公开方法；写完广播 EventBus 事件；禁止调用 `_` 私有成员；禁止传递节点引用。

## 全局事件总线（唯一跨模块消息通道；notify_*=事实通知，cmd_*=指令）
- 文件：`autoload/EventBus.gd`

### 信号（谁产生谁 emit，谁关心谁 connect）

| 信号 | 参数 | 用途 |
|---|---|---|
| `inventory_item_added` | item_id: String, count: int | 背包模块 |
| `inventory_item_removed` | item_id: String, count: int | 背包模块 |
| `inventory_weight_changed` | current: float, max_weight: float | 背包模块 |
| `inventory_add_overflow` | item_id: String, lost_count: int | 背包模块 |
| `item_used` | item_id: String, effect: Dictionary | 背包模块 |
| `combat_started` | combat_id: String | 战斗模块 |
| `combat_ended` | combat_id: String, result: int | 战斗模块 |
| `combat_character_died` | character_id: String | 战斗模块 |
| `combat_finished` | combat_id: String, victory: bool, escaped: bool, unit_snapshots: Array | 蓝图中枢事件（严格区分 NOTIFY 通知 / CMD 指令） |
| `quest_failed` | quest_id: String, reason: String | 蓝图中枢事件（严格区分 NOTIFY 通知 / CMD 指令） |
| `unit_downed` | unit_id: String, is_non_lethal: bool | 蓝图中枢事件（严格区分 NOTIFY 通知 / CMD 指令） |
| `cmd_start_combat` | attacker_list: Array, defender_list: Array | 蓝图中枢事件（严格区分 NOTIFY 通知 / CMD 指令） |
| `cmd_set_unit_faction` | unit_id: String, faction_id: int | 蓝图中枢事件（严格区分 NOTIFY 通知 / CMD 指令） |
| `cmd_apply_story_buff` | unit_id: String, buff_id: String | 蓝图中枢事件（严格区分 NOTIFY 通知 / CMD 指令） |
| `cmd_set_difficulty` | difficulty_id: String, is_new_game: bool | 难度系统（阶段1 骨架） |
| `notify_difficulty_changed` | new_difficulty_id: String | 难度系统（阶段1 骨架） |
| `notify_difficulty_change_rejected` | difficulty_id: String | 难度系统（阶段1 骨架） |
| `notify_player_party_wiped_out` |  | 难度系统（阶段1 骨架） |
| `notify_escape_success` |  | 难度系统（阶段1 骨架） |
| `notify_escape_fail` |  | 难度系统（阶段1 骨架） |
| `notify_defeat_cg` | text_id: String | 难度系统（阶段1 骨架） |
| `ability_learned` | ability_id: String | 武学模块 |
| `ability_used` | ability_id: String, caster_id: String | 武学模块 |
| `combat_skill_equipped` | ability_id: String, slot: int | 武学模块 |
| `equipment_equipped` | slot: String, item_id: String | 装备模块（Phase 2） |
| `equipment_unequipped` | slot: String, item_id: String | 装备模块（Phase 2） |
| `equipment_changed` |  | 装备模块（Phase 2） |
| `alchemy_refined` | recipe_id: String, output_item_id: String, count: int | 炼药模块（Phase 2） |
| `alchemy_failed` | recipe_id: String, reason: String | 炼药模块（Phase 2） |
| `quest_accepted` | quest_id: String | 任务模块 |
| `quest_objective_updated` | quest_id: String, objective_id: String, progress: int | 任务模块 |
| `quest_objective_completed` | quest_id: String, objective_id: String | 任务模块 |
| `quest_completed` | quest_id: String | 任务模块 |
| `quest_ready_to_turn_in` | quest_id: String | 任务模块 |
| `quest_turned_in` | quest_id: String | 任务模块 |
| `quest_phase_changed` | phase: int | 任务模块 |
| `player_level_up` | new_level: int | 玩家模块 |
| `player_exp_changed` | current: int, max_exp: int | 玩家模块 |
| `player_hp_changed` | current: int, max_hp: int | 玩家模块 |
| `player_mp_changed` | current: int, max_mp: int | 玩家模块 |
| `player_stats_changed` |  | 玩家模块 |
| `player_died` |  | 玩家模块 |
| `player_money_changed` | silver: int, copper: int, gold: int | 玩家模块 |
| `dialogue_started` | dialogue_id: String, npc_id: String | 对话模块 |
| `dialogue_ended` | dialogue_id: String | 对话模块 |
| `game_started` |  | 游戏流程 |
| `game_saved` | slot: int | 游戏流程 |
| `game_loaded` | slot: int | 游戏流程 |
| `scene_changed` | scene_name: String | 游戏流程 |
| `bootstrap_started` | total_steps: int | 启动流程（Bootstrap，规范 §4.1） |
| `bootstrap_step_started` | step_name: String, index: int | 启动流程（Bootstrap，规范 §4.1） |
| `bootstrap_step_completed` | step_name: String, index: int | 启动流程（Bootstrap，规范 §4.1） |
| `bootstrap_completed` |  | 启动流程（Bootstrap，规范 §4.1） |
| `game_error` | level: int, module: String, message: String | 错误处理（规范 §4.2） |
| `notification_show` | text: String | 错误处理（规范 §4.2） |
| `patch_applied` | patch_id: String, version: String | 补丁/热更新（规范 §4.7） |
| `config_loaded` | success: bool | 配置（规范 §4.4） |
| `config_validation_failed` | errors: Array | 配置（规范 §4.4） |
| `ui_screen_opened` | screen_name: String | UI |
| `ui_screen_closed` | screen_name: String | UI |
| `world_day_advanced` | day: int | 世界环境（阶段A 基础设施） |
| `world_weather_changed` | weather: int | 世界环境（阶段A 基础设施） |
| `world_time_changed` | day: int, time_of_day: float, season: int, weather: int | 世界环境（阶段A 基础设施） |
| `cmd_forge` | recipe_id: String, count: int | 锻造系统（Phase 2 系统填充 · 契约层） |
| `notify_forge_completed` | recipe_id: String, output_item_id: String, count: int | 锻造系统（Phase 2 系统填充 · 契约层） |
| `notify_forge_failed` | recipe_id: String, reason: String | 锻造系统（Phase 2 系统填充 · 契约层） |
| `cmd_buy` | shop_id: String, item_id: String, count: int | 商店系统（Phase 2 系统填充 · 契约层） |
| `cmd_sell` | shop_id: String, item_id: String, count: int | 商店系统（Phase 2 系统填充 · 契约层） |
| `notify_trade_completed` | shop_id: String, item_id: String, count: int, is_buy: bool | 商店系统（Phase 2 系统填充 · 契约层） |
| `notify_trade_failed` | shop_id: String, item_id: String, reason: String | 商店系统（Phase 2 系统填充 · 契约层） |
| `cmd_join_sect` | sect_id: String | 门派系统（Phase 2 系统填充 · 契约层） |
| `cmd_contribute_sect` | sect_id: String, amount: int | 门派系统（Phase 2 系统填充 · 契约层） |
| `notify_sect_joined` | sect_id: String | 门派系统（Phase 2 系统填充 · 契约层） |
| `notify_sect_reputation_changed` | sect_id: String, new_reputation: int | 门派系统（Phase 2 系统填充 · 契约层） |
| `notify_sect_rank_up` | sect_id: String, new_rank: int | 门派系统（Phase 2 系统填充 · 契约层） |
| `notify_sect_join_failed` | sect_id: String, reason: String | 门派系统（Phase 2 系统填充 · 契约层） |


## 装配中枢：Service 容器 + 场景切换/存读档入口
- 文件：`autoload/GameManager.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `start_new_game()` -> `void` | | 新游戏：重置全部状态并装配初始武学（规范 §4 学习） |
| `load_game(slot: int)` -> `void` | | 读取存档并进入游戏（主菜单"继续江湖路"调用，M2 新增） |
| `start_battle(battle_id: String)` -> `void` | | 开战：记录待打战斗并切换到战斗场景 |
| `return_to_town()` -> `void` | | 回城：从战斗/其他场景返回城镇 |
| `return_to_title()` -> `void` | | 返回标题：清理 UI 栈并重新加载启动入口（Bootstrap 会再次打开加载界面并进入主菜单） |
| `return_to_safe_point()` -> `void` | | 回安全点（EASY 团灭）：切换到城镇场景并恢复队伍状态（由 DefeatHandler 调用） |

## 多槽存档管理（ISaveable 注册制）
- 文件：`autoload/SaveManager.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `register_saveable(saveable: Variant)` -> `void` | |  |
| `get_saveable_count()` -> `int` | | 已注册的可存档对象数量（Bootstrap 启动序列查询） |
| `has_any_save()` -> `bool` | | 是否存在任意存档（主菜单"继续江湖路"可用性判断，M2 新增） |
| `get_latest_save_slot()` -> `int` | | 最新存档槽位（编号最大者）；无存档返回 -1（M2 新增） |
| `list_saves()` -> `Array` | | 手动存档列表：返回固定 MAX_SLOTS 个槽位摘要（含空槽位）；索引 0 存 slot 1 摘要 dict 字段：slot / exists / is_auto / player_name / level / faction / playtime / save_time / scene / thumbnail_path |
| `list_auto_saves()` -> `Array` | | 自动存档列表：最近 3 个（auto_N.json）；只读，不可手动删除（M3 自动存档系统尚未接入，通常返回空） |
| `delete_save(slot: int)` -> `bool` | | 删除手动存档；成功返回 true（M3 新增） |
| `quick_save()` -> `bool` | | 一键快速存档（存到 auto_1.json，与 list_auto_saves 兼容）；成功返回 true 用于游戏内 ESC 菜单「保存游戏」，避免弹复杂选槽界面 |
| `save_to_slot(slot: int, custom_name: String = "")` -> `bool` | | 手动存档到指定槽位；custom_name 非空时写入 meta，供卡片显示自定义存档名 |
| `load_from_slot(slot: int)` -> `bool` | | 手动读档（save_N.json） |
| `load_auto_save(slot: int)` -> `bool` | | 自动读档（auto_N.json）：quick_save 的对称操作。此前缺失导致自动存档「能存不能读」 slot 传 -1 表示非手动槽位（GameManager 据此不记录 current_slot） |

## 全部 JSON 配置查询 + 容错校验
- 文件：`autoload/ConfigManager.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `is_loaded()` -> `bool` | | 配置是否加载完成（Bootstrap 启动序列查询） |
| `get_anim_duration(token: String, fallback: float = 0.12)` -> `float` | | 取时长令牌（秒） |
| `get_anim_trans(token: String, fallback: int = Tween.TRANS_QUAD)` -> `int` | | 取缓动曲线类型（Godot Tween.TransitionType） |
| `get_anim_ease(token: String, fallback: int = Tween.EASE_OUT)` -> `int` | | 取缓动进出方向（Godot Tween.EaseType） |
| `get_anim_preset(preset: String)` -> `Dictionary` | | 取整组动效预设（hover / press / focus / screen / toast） |
| `get_anim_value(preset: String, key: String, fallback: float = 1.0)` -> `float` | | 取预设里的数值项，如 get_anim_value("hover", "scale") |
| `get_anim_preset_duration(preset: String, fallback: float = 0.12)` -> `float` | | 取预设里的时长（自动从 durations 令牌解析成秒） |
| `get_ui_sfx(event: String)` -> `Dictionary` | | 取音效事件配置；事件不存在返回空 Dictionary（调用方据此静默跳过） |
| `get_ui_sfx_bus()` -> `String` | | 音效总线名 |
| `get_ui_sfx_pool_size()` -> `int` | | 音效播放器池大小：避免鼠标快速划过多个按钮时后一个音切断前一个 |
| `get_ability(id: String)` -> `Dictionary` | |  |
| `has_ability(id: String)` -> `bool` | |  |
| `get_all_ability_ids()` -> `Array[String]` | |  |
| `get_item(id: String)` -> `Dictionary` | |  |
| `has_item(id: String)` -> `bool` | |  |
| `get_enemy(id: String)` -> `Dictionary` | |  |
| `has_enemy(id: String)` -> `bool` | |  |
| `get_battle(id: String)` -> `Dictionary` | |  |
| `has_battle(id: String)` -> `bool` | |  |
| `get_quest(id: String)` -> `Dictionary` | |  |
| `has_quest(id: String)` -> `bool` | |  |
| `get_npc(id: String)` -> `Dictionary` | |  |
| `has_npc(id: String)` -> `bool` | |  |
| `get_all_npc_ids()` -> `Array[String]` | |  |
| `get_dialog(id: String)` -> `Dictionary` | |  |
| `has_dialog(id: String)` -> `bool` | |  |
| `get_difficulty(id: String)` -> `Dictionary` | |  |
| `has_difficulty(id: String)` -> `bool` | |  |
| `get_all_difficulty_ids()` -> `Array[String]` | |  |
| `get_recipe(id: String)` -> `Dictionary` | |  |
| `has_recipe(id: String)` -> `bool` | |  |
| `get_all_recipe_ids()` -> `Array[String]` | |  |
| `get_forge_recipe(id: String)` -> `Dictionary` | |  |
| `has_forge_recipe(id: String)` -> `bool` | |  |
| `get_all_forge_recipe_ids()` -> `Array[String]` | |  |
| `get_shop(id: String)` -> `Dictionary` | |  |
| `has_shop(id: String)` -> `bool` | |  |
| `get_all_shop_ids()` -> `Array[String]` | |  |
| `get_sect(id: String)` -> `Dictionary` | |  |
| `has_sect(id: String)` -> `bool` | |  |
| `get_all_sect_ids()` -> `Array[String]` | |  |
| `get_world_config()` -> `Dictionary` | |  |
| `get_config_version()` -> `String` | |  |

## 难度管理器（团灭死亡行为/惩罚系数/丢物规则配置入口；零难度 if）
- 文件：`autoload/difficulty_manager.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `get_current_difficulty_id()` -> `String` | |  |
| `get_player_damage_scale()` -> `float` | | 玩家输出伤害倍率 |
| `get_enemy_damage_scale()` -> `float` | | 敌人伤害倍率 |
| `get_enemy_hp_scale()` -> `float` | | 敌人血量倍率 |
| `get_enemy_armor_scale()` -> `float` | | 敌人防御/抗性倍率 |
| `get_escape_bonus()` -> `float` | | 逃跑成功率修正值（正数提升，负数降低；HELL 通过 allow_escape=false 直接禁止） |
| `get_allow_escape()` -> `bool` | | 是否允许逃跑（HELL 关闭） |
| `get_allow_non_lethal()` -> `bool` | | 是否允许非致命击倒（NIGHTMARE/HELL 关闭） |
| `get_enemy_ai_profile_id()` -> `String` | | 当前难度对应的敌方 AI 行为配置 ID（战斗据此加载不同 AI 模板） |
| `get_player_defeat_behaviour()` -> `int` | | 团灭基础动作（RESPAWN_CHECKPOINT / LOAD_LATEST_SAVE / DELETE_SAVE 等） |
| `get_defeat_lose_money()` -> `int` | | 团灭丢失银两数（0 表示不扣） |
| `get_defeat_lose_items()` -> `bool` | | 团灭是否丢失少量非稀有道具 |
| `get_defeat_debt_if_broke()` -> `bool` | | 没钱支付时是否转为负债 |
| `get_defeat_cg_text_id()` -> `String` | | 团灭触发隐藏 CG / 对话的 text_id（空串表示无） |
| `get_defeat_lose_item_count()` -> `int` | | 团灭丢失的非稀有物数量（0 表示不丢） |
| `get_defeat_lose_rarities()` -> `Array` | | 团灭可丢弃的稀有度档位（默认 ["common"]；空数组=全部保护，一件不丢） |
| `get_defeat_lose_include_material()` -> `bool` | | 团灭是否可丢弃材料栏杂物（默认 false：材料栏不动，保护 crafting 资源） |
| `get_defeat_lose_include_quest()` -> `bool` | | 团灭是否可丢弃任务栏物品（默认 false：任务栏不动，保护进度物） |
| `get_defeat_cg_when_broke_only()` -> `bool` | | 团灭 CG 是否仅在「没钱支付」分支触发（true 时只在 broke 时播 CG） |
| `is_difficulty_locked(difficulty_id: String)` -> `bool` | | 某难度是否游戏内锁定（供 UI 决定是否灰显） |

## UI 屏幕栈（screens.json 名->脚本）
- 文件：`autoload/ui_manager.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `get_layer(layer: int)` -> `CanvasLayer` | |  |
| `get_safe_area_margins()` -> `Vector4` | | 安全区边距（安卓刘海/挖孔/圆角）：返回 Vector4(left, top, right, bottom)，单位像素。 桌面平台与无挖孔设备返回全 0，无副作用。 用途：界面根容器按此值留白，避免标题/底部按钮被刘海或系统手势条切掉。 注：窗口尺寸变化后需重新取值（配合 NOTIFICATION_RESIZED 或每帧读取）。 |
| `apply_safe_area(root: Control)` -> `void` | | 把安全区边距应用到某个 Control（改其 offset，需该 Control 已用锚点预设铺满父容器） 用法：界面根节点在 _ready 里调用 UIManager.apply_safe_area(self) |
| `get_current_screen()` -> `Control` | | 当前置顶的全屏界面（供界面在 _unhandled_input 里判断自己是否处于顶层，避免被遮挡时仍响应输入） |
| `is_any_screen_open()` -> `bool` | | 是否存在任意已打开的界面（游戏内 overlay 用此门控移动输入） |
| `get_open_screen(screen_name: String)` -> `Control` | | 按注册名查找当前已打开的界面实例（未打开返回 null）；供同键开关判断 |
| `open_screen(screen_name: String, layer: int = Layer.FULLSCREEN, init_data: Variant = null)` -> `Control` | |  |
| `close_screen(screen: Control = null, on_closed: Callable = Callable())` -> `void` | |  |
| `show_popup(popup_name: String)` -> `Control` | |  |
| `close_all_screens()` -> `void` | | 关闭栈内全部界面（读档/新游戏时调用：连同底层主菜单一并释放，避免残留遮挡） |
| `is_popup_open()` -> `bool` | | 是否有弹窗（POPUP 层级界面）处于打开中；供底层界面在 _unhandled_input 判断是否让权 |
| `show_tooltip(text: String)` -> `void` | | 轻量通知 Toast（EventBus.notification_show 自动接入）：顶部居中淡入，2.2s 后淡出 |
| `hide_tooltip()` -> `void` | |  |

## 玩家运行时状态（气血/属性/金钱/存档）
- 文件：`data/runtime/player_state.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `init_default(unit_name: String, start_level: int)` -> `void` | |  |
| `gain_exp(amount: int)` -> `bool` | | 获得经验，返回是否升级（升级后回满气血内力） |
| `recalculate_stats()` -> `void` | | 基础属性 + 装备加成 -> 战斗属性（规范 §1.4 重算流程的核心） 装备加成从 equipment_bonuses 读取，由 EquipmentService 装卸时维护 |
| `take_damage(amount: int)` -> `int` | |  |
| `heal(amount: int)` -> `int` | |  |
| `restore_mp(amount: int)` -> `int` | | 恢复内力（丹药/休息），返回实际恢复量（上限夹紧） |
| `consume_mp(amount: int)` -> `bool` | |  |
| `spend_money(amount: int)` -> `bool` | | 花钱：优先扣银两；不足则不改动并返回 false（调用方据返回值决定是否放行） |
| `add_money(amount: int)` -> `void` | | 赚钱：增加银两并广播变化（钓鱼/打造产出、任务奖励等调用） |
| `add_debt(amount: int)` -> `void` | | 负债：累计债务并广播（团灭惩罚「无钱则负债」路径调用） |
| `get_hp_percent()` -> `float` | |  |
| `is_dead()` -> `bool` | |  |
| `get_save_key()` -> `String` | |  |
| `save()` -> `Dictionary` | |  |
| `load(data: Dictionary)` -> `void` | |  |

## 背包服务（三栏/堆叠/负重/事务API/用药）
- 文件：`services/inventory/inventory_service.gd`

### 信号（谁产生谁 emit，谁关心谁 connect）

| 信号 | 参数 | 用途 |
|---|---|---|
| `inventory_item_added` | item_id: String, count: int |  |
| `inventory_item_removed` | item_id: String, count: int |  |
| `inventory_weight_changed` | current: float, max_weight: float |  |

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `add_item(item_id: String, count: int, source: String = "")` -> `bool` | | 添加物品：按类型自动归入对应栏位（材料->材料箱，任务物品->任务栏，其余->主背包） |
| `query_add(item_id: String, count: int)` -> `Dictionary` | | 纯计算预检：count 个 item_id 能装入对应栏位多少个（不改任何状态，不发光） 返回 { "added": int, "overflow": int }；shop/forge/alchemy 产出前必调 |
| `can_add(item_id: String, count: int)` -> `bool` | | 是否装得下 count 个（事务两段式的预检依据） |
| `try_consume(items: Array, source: String = "")` -> `bool` | | 原子扣料（事务语义）：items 为 [{ "item_id": String, "count": int }, ...] 先全量校验、再统一扣除；任一不足则整体不扣返回 false。锻造/炼药/批量消耗一律走此接口 |
| `remove_item_by_id(item_id: String, count: int)` -> `bool` | |  |
| `get_item_count(item_id: String)` -> `int` | |  |
| `is_full()` -> `bool` | |  |
| `get_instance_by_id(iid: String)` -> `ItemInstance` | | 按实例 ID 查找物品实例（装备系统装卸用） |
| `remove_instance(iid: String)` -> `bool` | | 按实例 ID 从背包移除（装备时把物品抽离到装备槽，不再占背包格） |
| `use_item(instance_id: String, context: String = "town")` -> `Dictionary` | | 使用消耗品：pill 类型或 flags 含 CONSUMABLE 的物品，按配置 heal_hp/heal_mp 生效 context: "town" / "battle"（当前结算一致，预留战斗限制扩展位） 返回 { "ok": bool, "reason": String, "item_id": String, "effect": {hp, mp} } |
| `consume_instance(iid: String)` -> `bool` | | 公共扣件：扣除指定实例 1 个数量；耗尽则回收格子。城镇用药/战斗用药共用 返回 false 表示实例不存在 |
| `get_equippable_instances()` -> `Array` | | 返回背包主栏中所有可装备的物品实例（配置带 equip_slot 的） 注意：返回 Array 而非 Array[ItemInstance]。main_slots 是普通 Array， 迭代元素在编译期是 Variant，硬标 ItemInstance 会触发类型不匹配。 UI 处用 var inst: ItemInstance = items[i] 取即可。 |
| `get_weight()` -> `float` | |  |
| `get_max_weight()` -> `float` | | 负重上限（真负重）：基础值 + 力量 × 系数 UI 窗口显示负重进度条必须调此接口，禁止再用 BASE_MAX_WEIGHT 常量（否则力量成长不反映） |
| `lose_some_non_rare_items(n: int)` -> `Array` | | 团灭惩罚：丢失 n 件「符合难度表 defeat_lose_rarities 档位」的物品，返回丢失的 item_id 列表 规则已全部配置化（数值进 JSON，去除硬编码 rarity=="common"）： - 可丢稀有度档位：DifficultyManager.get_defeat_lose_rarities()（默认 ["common"]，空数组=全部保护） - 槽位范围：主栏恒含；材料栏/任务栏由 get_defeat_lose_include_material()/include_quest() 决定（默认均不含） 设计：只丢杂物，保护进度物；同一类丢多件也只记一次 id 供抵押物清单去重 注意：用带索引的 while 遍历（而非 for-in），确保堆叠物品（count>1）能在本轮被彻底扣空， 不会因 for-in 每实例只访问一次而少丢（例如 2 个丹药应丢 2 件而非 1 件） |
| `reset()` -> `void` | |  |
| `get_save_key()` -> `String` | |  |
| `save()` -> `Dictionary` | |  |
| `load(data: Dictionary)` -> `void` | |  |

## 装备服务（装配/卸下/加成重算）
- 文件：`services/equipment/equipment_service.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `equip(instance_id: String)` -> `bool` | | 装备：从背包抽出实例装入对应槽，重算加成 换装顺序保证不丢物（P0 修复）：先抽新装备（腾出 1 格）→ 再卸旧装备退包（必成功） |
| `unequip(slot: String)` -> `bool` | | 卸下：先退回背包（满包则失败，装备留在槽上不丢），成功后移除加成 |
| `get_equipped(slot: String)` -> `String` | |  |
| `is_slot_filled(slot: String)` -> `bool` | |  |
| `get_save_key()` -> `String` | |  |
| `save()` -> `Dictionary` | |  |
| `load(data: Dictionary)` -> `void` | |  |
| `reset()` -> `void` | |  |

## 商店服务（买卖/预检）
- 文件：`services/shop/shop_service.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `buy_price(shop_id: String, item_id: String)` -> `int` | | 买入价 = 配置 price × buy_rate |
| `sell_price(shop_id: String, item_id: String)` -> `int` | | 卖出价 = 配置 price × sell_rate |
| `buy(shop_id: String, item_id: String, count: int)` -> `int` | | 玩家买入：扣银两 + 入背包 |
| `sell(shop_id: String, item_id: String, count: int)` -> `int` | | 玩家卖出：扣背包 + 加银两 |
| `can_buy(shop_id: String, item_id: String, count: int = 1)` -> `bool` | | UI 用：银两是否够买 count 个 |
| `can_sell(shop_id: String, item_id: String, count: int = 1)` -> `bool` | | UI 用：背包是否够卖 count 个 |

## 锻造服务（配方/材料事务）
- 文件：`services/forge/forge_service.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `forge(recipe_id: String, count: int)` -> `int` | | 锻造：校验等级与材料 -> 扣除材料 -> 产出物品 -> emit 结果事件 返回 ForgeEnums.ForgeResult |
| `can_forge(recipe_id: String, count: int = 1)` -> `bool` | | UI 用：材料与等级是否都满足（count 与 forge() 的批量数保持一致，默认 1） |
| `describe_inputs(recipe_id: String)` -> `String` | | UI 用：把配方材料渲染成 "铁石 2/3" 形式 |

## 炼药服务（配方/材料事务）
- 文件：`services/alchemy/alchemy_service.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `refine(recipe_id: String)` -> `bool` | | 炼制：成功返回 true 并发 alchemy_refined；材料不足/背包装不下产出发 alchemy_failed 并返回 false |
| `can_refine(recipe_id: String)` -> `bool` | | UI 用：当前材料是否满足配方 |

## 战斗服务（回合制/结算/奖励回写/战斗内用药）
- 文件：`services/combat/combat_service.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `get_core()` -> `CombatCore` | | 取内核（测试可注入固定 seed 验证确定性） |
| `start_combat(battle_id: String)` -> `void` | | 开始战斗：从配置构建玩家与敌人快照 |
| `player_attack(target_id: String = "")` -> `Dictionary` | | 玩家普通攻击（门面：委托内核，返回首个伤害事件摘要供旧调用方兼容） |
| `player_cast(slot: int, target_id: String = "")` -> `Dictionary` | | 玩家施展快捷栏武学（门面：委托内核技能结算） |
| `player_rest()` -> `Array[CombatEvent]` | | 玩家调息（rest）：回血回真气 |
| `player_use_item(instance_id: String)` -> `Dictionary` | | 玩家战斗内使用物品（消耗品）：作用于战斗内玩家快照，finalize 时随 hp/mp 回写 PlayerState 注意：不可直接调 InventoryService.use_item（那会结算到 PlayerState，战斗面板不会刷新， 且结算回写时快照会覆盖掉 PlayerState 上的恢复量——吃过药等于白吃） |
| `run_enemy_turns()` -> `void` | | 敌人回合（门面：委托内核，逐事件写日志） |
| `player_attack_events(target_id: String = "")` -> `Array[CombatEvent]` | | 玩家普攻：返回完整事件流，M2 演出层 play_events 消费（替代 player_attack 的兼容摘要） |
| `player_cast_events(slot: int, target_id: String = "")` -> `Array[CombatEvent]` | | 玩家施展快捷栏武学：返回事件流 |
| `player_rest_events()` -> `Array[CombatEvent]` | | 玩家调息：返回事件流 |
| `enemy_phase_events()` -> `Array[CombatEvent]` | | 敌人阶段：返回事件流 |
| `use_item_events(instance_id: String)` -> `Array[CombatEvent]` | | 玩家战斗内用药：返回事件流（HEAL/QI_GAIN，带行动后直设值），供 Director 播放 |
| `is_over()` -> `bool` | |  |
| `get_result()` -> `int` | |  |
| `get_state()` -> `CombatState` | |  |
| `try_escape()` -> `bool` | | 尝试逃跑：读难度配置（allow_escape / escape_bonus），成功则结束战斗并广播，失败则让敌人反击 返回 true 表示已脱身（调用方应停止后续行动并显示结果） |
| `finalize()` -> `void` | | 结算：写回玩家 hp/mp、发放奖励、发事件（只调用一次） 不再直接调用任务模块——改为对外广播 combat_finished 快照事件，由任务系统订阅判定 |

## 任务服务（接取/推进/发奖）
- 文件：`services/quest/quest_service.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `can_accept(quest_id: String)` -> `bool` | |  |
| `accept(quest_id: String)` -> `bool` | |  |
| `get_active_quest_ids()` -> `Array[String]` | | 返回进行中任务 ID 列表（供 SaveValidator 遍历校验） |
| `fail_quest(quest_id: String, reason: String = "FAILED")` -> `void` | | 将任务置为失败（多失败分支由 reason 区分）；并发出失败通知 |
| `turn_in(quest_id: String)` -> `bool` | | 交付并发放奖励：经验 -> 玩家；物品 -> 背包；武学 -> 武学服务 |
| `get_tracked()` -> `Array[QuestState]` | |  |
| `is_active(quest_id: String)` -> `bool` | |  |
| `get_phase()` -> `int` | |  |
| `set_phase(value: int)` -> `void` | |  |
| `advance_phase()` -> `int` | |  |
| `reset()` -> `void` | |  |
| `get_save_key()` -> `String` | |  |
| `save()` -> `Dictionary` | |  |
| `load(data: Dictionary)` -> `void` | |  |

## 武学服务（学习/装备/施展）
- 文件：`services/ability/ability_service.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `learn(ability_id: String)` -> `bool` | |  |
| `is_learned(ability_id: String)` -> `bool` | |  |
| `equip_combat_skill(slot: int, ability_id: String)` -> `bool` | |  |
| `unequip_combat_skill(slot: int)` -> `void` | |  |
| `use_combat_skill(slot: int, caster: CombatCharacter, target: CombatCharacter)` -> `Dictionary` | | 战斗中施展快捷栏武学：返回伤害结果字典 {hit, damage, crit, dodged} |
| `reset()` -> `void` | |  |
| `get_save_key()` -> `String` | |  |
| `save()` -> `Dictionary` | |  |
| `load(data: Dictionary)` -> `void` | |  |

## 门派服务（声望/阶位）
- 文件：`services/sect/sect_service.gd`

### 公开方法（跨模块只允许调这些；`_` 开头为私有，禁调）

| 方法 | 签名 | 说明 |
|---|---|---|
| `get_save_key()` -> `String` | |  |
| `save()` -> `Dictionary` | |  |
| `load(data: Dictionary)` -> `void` | |  |
| `reset()` -> `void` | |  |
| `join(sect_id: String)` -> `int` | | 加入门派：校验未入门派 / 声望足够 / 入门道具，置 current_sect_id 并初始化声望阶位 返回 SectEnums.JoinResult |
| `contribute(sect_id: String, amount: int)` -> `void` | | 贡献声望：累加并检查是否跨过阶位阈值，跨过则晋升并 emit |
| `get_reputation(sect_id: String)` -> `int` | | 当前门派声望 |
| `get_rank(sect_id: String)` -> `int` | | 当前门派阶位（SectEnums.Rank） |
| `get_rank_name(rank_value: int)` -> `String` | | UI 用：阶位显示名（ranks[].rank 字符串 -> 中文） |

---
统计：信号 79 个 · 公开方法 195 个 · 纳入契约文件 16 个