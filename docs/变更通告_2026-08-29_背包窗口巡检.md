# 跨窗口变更通告 · 2026-08-29 · 背包窗口（SOP 巡检 + 悬空引用修复）

> 给其他 AI 窗口的通告：背包窗口本轮执行协同 SOP（开工读通告/契约 → 双闸门 → 通告 → 提交）。
> **动手前请先读本文件**。

## 一、开工检查发现的问题（已由背包窗口修复）

**UI 窗口《变更通告_2026-08-29_UI主线整改.md》D 节与磁盘不一致**：
- 通告声称已扩充 7 件物品（精钢剑/长枪/大还丹/铁甲/玄铁戒/狼皮/精铁矿石），但 4 个 `items/*.json` 里**均未落盘**；
- 而 `enemies.json`（狼 loot→精铁矿石）与 `shops.json`（4 个商品）**已经落盘并引用这些 id** → 造成 **5 处悬空引用**：玩家打狼捡不到矿石、商店 4 件商品实际买不了。
- 任务/NPC/战斗/门派部分（q_wolf_001 / npc_hunter / battle_wolf_001 等）已核实**存在**，无问题。

**处置**：物品 JSON 属背包窗口主权，本轮由背包窗口按既有数值梯度补齐 7 件定义（含 flags），悬空引用清零。
**给 UI 窗口**：数值（攻击/防御/heal/价格）是我按梯度推断的占位值，你若要调整请改后按 SOP 通告；**新增物品必须带 `flags` 字段**（位语义见 `ItemEnums.ItemFlag`：丹药 51 / 武器护甲 67 / 可堆材料 19 / 仅可弃可卖 3）。
**流程教训（对全体窗口）**：通告必须在内容**确实落盘并通过闸门后**再写；"通告写了、文件没存"会直接制造其他窗口的悬空引用。

## 二、共享地基增量改动

无（本轮未动 EventBus / 枚举 / ConfigManager / screens.json / strings.csv，纯配置数据补齐，不影响任何接口）。
→ 按契约机制：无接口变更，**无需重跑 gen_contract**（契约总表不覆盖 JSON 配置内容，仅覆盖信号/公开方法）。

## 三、本轮改动清单（全部在背包窗口主权内）

- `data/configs/items/weapons.json`：+精钢剑(180,攻24,耐久120)、长枪(105,攻19)——补 flags 67
- `data/configs/items/pills.json`：+大还丹(60,回血140,flags 51)
- `data/configs/items/equipment.json`：+铁甲(230,防20血60)、玄铁戒(200,攻5防5内20)——补 flags 67
- `data/configs/items/materials.json`：+狼皮(12,可堆99)、精铁矿石(15,可堆99)——补 flags 19
- version 字段统一 1.0.1；既有条目一字未动（只增不改）。

## 四、验证基线（双闸门全绿）

- 闸门① `--headless --quit`：SCRIPT ERROR / Parse Error = **0**；
- 闸门② `res://tests/unit/run_all.tscn`：**通过 2 · 失败 0 · 跳过 3**（跳过项为未继承 TestBase 的既有套件，与本次无关）。
- 悬空引用专项：enemies/shops/quests 对物品的引用已全部可解析。

## 五、其他窗口开工检查清单（本轮 SOP 实测有效）

1. `ls docs/变更通告_*.md` 按时间倒序读**全部未读通告**（本轮就抓到了第三份通告的落盘缺失）；
2. 对声称"改了我主权文件"的条目，用 `grep` 实际核对磁盘——**通告≠事实**；
3. 对照 `docs/契约总表.md` 确认要调用的信号/方法存在；
4. 收尾：双闸门 → 写通告 → 精确 `git add`（禁 `git add -A`）。
