# 变更通告 · 2026-08-29 · 背包窗口（应 UI 窗口授权代改一行）

> 类型：代改 UI 窗口主权文件（低风险，已获用户授权）
> 触发：背包窗口负重真化（`get_max_weight()` 已落地，提交 6c1fd02）需要 UI 侧消费接口

## 改动内容
- 文件：`scenes/ui/overlays/inventory/InventoryScreen.gd`（**UI 窗口主权**）
- 位置：第 68 行 `_refresh()`
- 旧：`_weight_label.text = tr("ui_inventory_weight") % [inv.get_weight(), InventoryService.BASE_MAX_WEIGHT]`
- 新：`_weight_label.text = tr("ui_inventory_weight") % [inv.get_weight(), inv.get_max_weight()]`
- 性质：仅把负重条上限从写死常量改为调背包提供的 `get_max_weight()`（= 50 + 力量 × 2.5），使负重条随力量实时变化。这就是评审点名的"假负重漏到 UI"的根因修复。

## 影响范围
- 影响模块：无接口/信号变更；仅 UI 显示数值正确化（玩家力量提升后负重条上限同步上涨）。
- 不影响模块：背包服务内部、战斗、商店、存读档、任务、门派。
- 风险：极低——`get_max_weight()` 已在 6c1fd02 通过双闸门且有 3 条回归用例锁住，本次只换取值来源。

## 为什么是背包窗口代改
UI 窗口本轮活跃改动 `InventoryScreen` 但未接该接口；用户直接授权背包窗口改这一行以闭合"真负重"链路。属主权越权但已获明确授权，特此通告留痕。

## 验证
- `--quit` 脚本错误：0
- `run_all.tscn`：5 套件 50 用例，通过 50 · 失败 0 · 跳过 0

## 给 UI 窗口的备注
- 若后续重写 `InventoryScreen` 的 `_refresh()`，请勿再把上限写回 `InventoryService.BASE_MAX_WEIGHT` 常量；负重上限统一走 `inv.get_max_weight()`（契约总表第 249 行已标注）。
- 本次代改已提交，UI 窗口下次开工 `git pull`/同步时可见，无需重复修改。

## 下一步（背包窗口）
- 团灭丢物规则配置化（数值进 JSON），待排期。
- 超重的玩法级惩罚（禁用移动/进入战斗）需知会地图窗口，待 UI 接好负重条后评估。
