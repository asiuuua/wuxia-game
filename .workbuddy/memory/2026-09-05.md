# 2026-09-05 工作日志

- **【模拟平衡性能域基线】17 Simulation / Balance / Performance Hardening 施工图 V1.2 产出（22:08~22:30）**：产出 `docs/architecture/17_Simulation_Balance_Performance施工图_V1.2.md`（FROZEN CANDIDATE，SBP 命名空间已验证不撞号，实锤前缀 P-SB 与 13 存档 P-S 不同名）。宪法锚点：§93 性能原则（七件套+Domain 禁为性能依赖 Godot）/ §94 大世界 NPC（三禁令+Simulation Level 四档）/ §94A 13 项预算+7 规则 / §121 Simulation Fast Headless / §216 Performance Regression（五字段基线+永不删除+双 PASS）/ §230 YAGNI 禁项。冻结 8 件：①**SBP-1 确定性纪律**（业务概率禁全局 randf/randi；**回退分支即裂缝**——rng 缺失应 FATAL 禁静默降级；表现层装饰豁免清单 2 处：GameManager L193 加载提示/TownScene L205 相位）；②**SBP-2 Simulation Tick**（无 tick 显式推进=标准形态收编 weather_time_service L5；NPC 四档 Full/Reduced/Background/Dormant 只冻定义；Interest Area 禁实装 §230；world_day_advanced 消费注册制）；③**SBP-3 Balance 六表真源**（difficulty/attribute/world_config 权重/battles.reward_exp+enemies.loot/npc_stats/经济）；④**SBP-4 难度系统收编**（difficulty_manager 138 行宿主零 if 升全库规则；defeat_* 走 10 EC-7；ai_behavior_profile 对接 11 AB-3）；⑤**SBP-5 预算机制**（首批 3 项 Boot/Save/Combat Turn；Benchmark 五字段 {input_scale,environment,result_distribution,allowed_variance,version}；数值不写宪法由 Profile 实测建立）；⑥**SBP-6 性能回归门禁**（Benchmark 永不删除；双 PASS=FUNCTIONAL≠RELEASE；TownScene 15NPC 首基准）；⑦**SBP-7 每帧纪律**（三禁令机器化；每帧入口登记制；现状 12 处基线）；⑧**SBP-8 预埋配置声明制**（_doc 声明启用阶段；derive_mode 开关范式）。**实锤 P-SB1~P-SB10**：P-SB1【确定性裂缝 5 处】combat_service.gd L417 逃跑回退 `randf()<chance` + data/runtime/combat_character.gd L72/78 闪避/暴击回退（**注释 L66 自认「回退全局 randf()（不可复现）」明知故犯**）+ weather_time_service L140 天气 randi（07 WT 归属）+ romance_service L435 受孕判定 + math_util L14（**全库零调用方死代码**）；P-SB2 GameManager._process L230-240 四职责混合（冷却 tick+buff 每秒清理+异步加载轮询+加载动画）；P-SB3 TownScene._physics_process L390-393 每帧 O(N) NPC 距离扫描（§94 字面对照；15 NPC 规模无瓶颈→§93 水位监控项非整改项）；P-SB4 attribute_table 预埋死配置（judgment/growth/weapon_range 三段全库零消费点，_doc 自认「阶段A 仅存档/阶段B 启用」的有意预埋——登记冻结非删除）；P-SB5 npc_stats.json martial_arts 存中文显示名（村中拳法）违 I-5；P-SB6 difficulty_table version "1.0"（P-CP4 同族）；P-SB7 奖励发放绕事务（_grant_rewards L456 gain_exp/L460 add_item 直调，但数据真源 ✓：battles.reward_exp+enemies.loot+drop:留痕格式正确——11 P-C3 同源）；P-SB8 性能资产丰富 vs 预算零建立（**全库 189 处 cache 文化**：对话 LRU/resource_manager 引用计数/条件 TTL/Toast 池/SFX 流缓存/立绘 LRU 预热/count 索引/异步加载/分帧释放/流式视频——§93 七件套已实践五件；§94A 13 项预算零 benchmark 零基线零 Suite）；P-SB9 孕期双时间源混用（pregnancy.start_day 存 Unix 墙钟秒 L383/437 但字段叫 day，实际推进走 advance_days 游戏天数 L602——07 Time 单调精神违反，08 RF 核查归属）；P-SB10 每帧全景 12 处（GameManager 1+TownScene 2+UI 9）+零 TODO/FIXME/HACK+零 set_process 动态切换。**SBP-R01~R12（E0=0，GATE06 为主+GATE40+ 基准）**。好消息（§171 收编）：difficulty_manager 零 if 宿主=Balance 最佳实践/seeded_rng 正主+CombatCore 确定性主力已就位/无 tick 显式推进设计/derive_mode flat↔five_attr 管线已实装（combat_character L33-146）/189 处缓存文化/13 处优化注释文化/世界日消费面单一（仅 GameManager L221 扇入孕期）。开放问题 SBP-1 NPC 四档实装时机（推只冻定义，§230）/SBP-2 math_util 死函数（推直接删，零调用方零成本）/SBP-3 首批预算 3 项（推 Boot/Save/Combat Turn，测试宿主现成）/SBP-4 Benchmark 宿主（推 --script 基准+JSON 五字段基线+verify_all 可选 tier，GATE40+）。**勘误：上轮 16 号 MEMORY 条目 Edit 报成功但内容实际未落盘（本轮 python 校验发现），已随本轮补写**。**本轮只产出文档，未动任何生产源码。**

- **【内容生产域基线】16 Content Production 施工图 V1.2 产出（22:20~22:40）**：产出 `docs/architecture/16_Content_Production施工图_V1.2.md`（FROZEN CANDIDATE，CP 命名空间已验证不撞号——05 图开放问题前缀 `C-` 不同名）。冻结 8 件：①**CP-1 退役名单落地**（`_retired_ids.json` Schema {id,reason,retired_at,successor}；town_npcs 体系首登记；GATE5 由脚本硬编码升级读名单，Phase4 后退役）；②**CP-2 ID 收敛路线**（新内容禁发明前缀 03§3.3 现状 C；存量 `tools/id_baseline.json` 基线只减不增；域归属映射表九行 armor_/acc_/material_/pill_/weapon_/demo_item_→item_、sword_/blade_/inner_/staff_/qinggong_/xinfa_/huti_/jingci_/buqu_→abil_、pojia 系 10 裸名→stat_、bandit 系 8 敌人→enemy_、q_→quest_、forge_→recipe_、tactical_/demo_→battle_、nv_/mt_→按域拆、区域裸名→region_、player→豁免；引用同步红线+存档红线 CP-2e）；③**CP-3 真源分片**（_map_index 唯一区域注册表+三同铁律机器化 CP-R06；分片 {id,lines} 纯数据、归属单点 _index（与 05 C-3 模块主权兼容）；懒加载+pin 冻结）；④**CP-4 版本契约**（实体库 version 必须 x.y.z；3 个 "1.0" 补齐 1.0.0；豁免清单 13 文件冻结）；⑤**CP-5 校验器群**（五检对应 05 VA-2 五层；规则表 JSON 双端同源 VA-4；挂 GATE06）；⑥**CP-6 DataSink 六步唯一写入口**（承接 15 ST-2 原文）；⑦**CP-7 Pack 生产端**（打包=五检+manifest+指纹输入；Mod 禁代码前置校验 DM-3）；⑧**CP-8 本地化资产**（display 键必须存在于 strings.csv，接 LN-G18）。**实锤 P-CP1~P-CP10**：P-CP1 `_retired_ids.json` 不存在（03§3.3 契约零落地，I-1 无机器兜底）；P-CP2【全景量化·本图最大产出】内容 ID 违例 **202 处/36 文件/0 解析失败**（13 域白名单正则），四大家族=发明前缀（skills 11 处 sword_/blade_/inner_ 等 L5~143、equipment 15 处 armor_/acc_ L4~18、materials 18 处 material_ L5~120、pills 17 处 pill_ L4~20、weapons 19 处 weapon_ L4~22、forge_iron_sword L5、q_ 3 处、scenes/battles 9 处全 tactical_/demo_ L15~162）+裸名（status_effects 10 个全裸拼音 L5~109、enemies 8 个定义 bandit_001/demo_enemy/wolf_001 等、player L2）+区域前缀 nv_/mt_ 双区域全量（npcs 8/enemies 3/items 3/quests 4/battles 2/dialogs 3）+内部行 ID 约 40 处（对话行 dg_1/l1/opt_*、objective defeat/obj1、graph 节点 good/neutral——归 05 C-3 与 12 图不接管；enemies.json 20 处中 12 处为技能引用行连坐）；P-CP3 区域注册表双风格并存（newbie_village/misty_town 裸名 L6/L14 vs region_sect_gate 等 8 个 region_ 前缀 L22~75——**好消息=三同铁律已落地真源统一成果**）；P-CP4 SemVer 双缺口（dialogue_events/difficulty_table/_index 三文件 "1.0"；20 文件无 version=13 分片+player+2 grids+2 bond+maps/town+npc_stats）；P-CP5 分片顶层无 npc_id 归属靠 _index（9 有 4 空，05 C-3 不接管）；P-CP6 内容量基线（items 69/skills 11/状态 10/敌人 11/quests 8/战斗配置 11/shops 2/sects 2/recipes 3/分片 13/strings.csv 453 行）；P-CP7 town_npcs 退役靠 GATE5 脚本硬编码非名单数据；P-CP8 ref_index 未挂写路径（15 P-ST7 同源）；P-CP9 grids/maps 工具数据与实体库同住未分层；P-CP10 demo/tactical 测试内容混入生产真源（scenes/battles 9 个全 demo 系+区域 NPC 3 个 demo_npc/tactical_demo_master/event_demo_npc+demo_item_jade+demo_quest）。**CP-R01~R12（E0=0，GATE06 为主）**。好消息（§171 收编 16 项）：_map_index v2 真源统一成果/_index 懒加载+pin/town_npcs 事故教育留档/ref_baseline「修一个删一条」范式可复用/items 四文件 1.0.1 标杆/_doc 注释文化。开放问题 CP-1 违例收敛方式（推基线冻结只拦新增，一次性改名留 ADR-0002 后）/CP-2 nv_/mt_ 与区域裸名终形态（推迁白名单 local 嵌区域标识如 npc_nv_chief·region_newbie_village，随 ADR-0002 合并裁决）/CP-3 对话归属真源（推维持 _index 单点，分片纯数据）/CP-4 version 豁免清单（推实体库强制、13 文件豁免）。**本轮只产出文档，未动任何生产源码。**

- **【工具域基线】15 Studio / Authoring / Validator / Preview 施工图 V1.2 产出（21:40~22:00）**：产出 `docs/architecture/15_Studio_Authoring_Validator_Preview施工图_V1.2.md`（FROZEN CANDIDATE，ST 命名空间已验证不撞号；含 §2.2 LN/物理门禁映射登记 GATE16↔物理GATE7 等）。冻结 8 件：①**ST-1 Studio 红线**（宪法§67/01§79 原文：Authoring System 非 JSON 编辑器；写 Authoring 数据禁碰 Runtime State；编辑器必须建在 Schema/Definition/Validation/Registry 上）；②**ST-2 写路径 DataSink 六步**（ID 白名单统一入口→Schema 校验→_backup→落盘→**ref_index 增量反查悬空即回滚**→change_log 留痕；抽象先行，ADR-0003 落位只换根路径）；③**ST-3 安全五件套冻结+新端点准入清单**（127.0.0.1/CSRF `_origin_allowed` 三入口/`_is_valid_id`/限根限扩展名/selftest 回归，新端点全过才合入）；④**ST-4 模块化拆分**（studio_core 3091 行 God Module 十域拆 modules/，core 降 facade，server 禁业务逻辑，每模块自测注册 self_test，搬移不改行为）；⑤**ST-5 工具清单与退役制**（tools manifest 登记制；`_` 前缀一次性脚本迁 _archive/；运行产物规范位；**同名工具禁双份**）；⑥**ST-6 校验器统一注册表**（9 门禁/审计工具注册 {名称,LN Gate,tier,REPORT}，arch_lint.py 为 01§92 七校验器 py 宿主，物理槽冻结新校验走注册表+LN/GATE40+）；⑦**ST-7 预览单源**（UIPreview COMPONENT_PATHS 退役并入注册表；_editor_preview() 协议+owner=null 冻结）；⑧**ST-8 生成器统一+AI 上下文双出口冻结**（gen_* 五器统一入口；export_ai_context 与 /api/experience 单源双投禁第三出口）。**实锤 P-ST1~P-ST10**：P-ST1【同名双份漂移】compress_textures.py 两份已 diff 分叉（tools/ 223 行全功能 vs desktop_studio/ 111 行精简，tscn_assets.py 实际只在 desktop_studio/ 616 行——记忆「顶层」认知修正）；P-ST2 studio_core.py 3091 行 ≥10 域 God Module（ GameManager 17Service 同族）；P-ST3 modules/ 空壳（仅 __init__.py）；P-ST4 7 个 `_` 前缀一次性脚本残留顶层；P-ST5 运行产物混源码目录（3 log+__pycache__+build）；P-ST6 预览组件双登记（UIPreview L18-24 硬编码 5 组件平行 screens.json）；P-ST7 编辑器写路径零契约（_is_valid_id 只管 NPC、写后无 ref_index 反查悬空等 GATE6 兜底）；P-ST8 校验器 9 件分散手工注册；P-ST9 gen_* 五散；P-ST10 AI 上下文双出口无第三出口禁令。**ST-R01~R12（E0=0，LN Gate）**。好消息（§171 收编 16 项）：verify_all GATE1~9+USERPROFILE 自愈 / studio_smoke 编辑→游戏同源冒烟 / 安全五件套+15 断言 / ref_index --who=Dependency Graph 现状等价物 / 多 AI 三件套 / 分层投喂 / tscn_assets+compress 双保险 / pre-commit 三道门 / UIPreview @tool / **trash 回收站五件=创作撤销底座** / i18n_upsert=PV-8 作者侧。开放问题 ST-1 Authoring 落位（推随 ADR-0003 延 Phase5，DataSink 先行）/ST-2 compress 双份合并方向（推 tools/ 版唯一真源，合并前全量 diff 111 行版无独有逻辑）/ST-3 12 件套缺口排期（推 Phase5 前不加编辑器先打地基，宪法§164）/ST-4 发行形态（推源码模式日常+exe 仅发行，skill 已固化）。本次只产出文档，未动任何生产源码。

- **【表现层基线】14 Presentation / Input / ViewModel 施工图 V1.2 产出（22:00~22:20）**：产出 `docs/architecture/14_Presentation_Input_ViewModel施工图_V1.2.md`（FROZEN CANDIDATE，PV 命名空间已验证不撞号）。冻结 8 件：①**PV-1 ViewModel 契约**（宪法 L2097「UI-local transient display state」锚定；RefCounted 纯投影；三禁=禁写业务状态/禁持 Node/业务判定禁读表现数据；§31 读链 Query→ViewModel→UI 中段补齐）；②**PV-2 输入链契约**（§92 四源归一 Command；迁移期三级=数值类已禁零命中基线/流程类 6 处 Phase3 Command 化走白名单过渡/UI-toggle 不 Command 化但 _toggle_overlay 收口 UIManager.toggle_screen）；③**PV-3 PresentationEffect 执行器**（承接 12 图 QD-2：services 只产指令，执行器落 scenes 演出段持 Tween/相机/音频；11 图 combat_event_renderer/entity_pool 归属一并裁决）；④**PV-4 层级与生命周期**（7 层 CanvasLayer 枚举值显式冻结；.tscn-only；cache 语义；弹窗禁自毁走 popup_close_requested 收口；open_screen 唯一入口）；⑤**PV-5 键位单真源**（settings_manager 三合一收口 REBINDABLE_ACTIONS/DEFAULT_BINDINGS/InputMap 注册，动作全集 8 个补 toggle_menu；TownScene 默认表退役只消费）；⑥**PV-6 配置面与 IO**（11 个 UI json 键域冻结；screens.json 启动期 schema 强校验禁静默空表；IO 收敛 JSONUtil+settings.json 升原子写）；⑦**PV-7 主题与图标**（UIPalette 禁裸字面量升扫描项；IconRegistry 依赖反转冻结为标准解法禁再新增基础层→上层 preload）；⑧**PV-8 本地化**（strings.csv 唯一真源；locale 三枚举；CSV 解析必须处理引号逗号）。**实锤 P-V1~P-V12**：P-V1 键位双真源（TownScene L35-44 8 动作默认表 vs settings L12 7 键可重绑，toggle_menu 永不可绑）；P-V2 音量双真源（AudioManager L10-12 硬编码 vs settings L63 默认表）；P-V3【最大欠账】ViewModel 全缺（全库零命中；28 文件 ~230 处直引 autoload；status_card_panel _ATTRS key=PlayerState 成员名）；P-V4 UI 直连流程单例 6 处（MapScreen L85 goto_region 等）；P-V5 UI 直引 Service（DialogOverlay L113-253 六处 dialogue_service 直连为最大单点）；P-V6 防御性 has_method 探测（localization L13 恒真死防御，QD-6 纪律一致性）；P-V7 手写 CSV split(",") 无引号处理（文案含逗号断列）；P-V8 IO 四分裂蔓延 UI 域（ui_manager/settings/UILayout/localization 四种读法，settings.json 写入无原子写无 .bak）；P-V9 core 层驻 4 个表现辅助（ui_layout/ui_theme/combat_event_renderer/combat_entity_pool）；P-V10 转场 6 枚举仅 3 实装（SCROLL/PAGE/SLASH 静默黑屏）；P-V11 TownScene 约 10 处 is_any_screen_open 门控散布与 BaseScreen 双轨；P-V12 screens.json 23 项零校验（解析失败静默全 UI 不可开）。**PV-R01~R12（E0=0，LN Gate）**。好消息（§171 收编 14 项）：UIManager 7 层+Toast 池+BUG-02 防重 / **IconRegistry 依赖反转=全项目唯一完成的方向反转治理** / BaseScreen 五件套+首点吞修复 / hud_draggable_panel mouse_filter 纪律示范 / UIFeedback 全表驱动 / UIPalette 主题集中 / UILayout 等比缩放 / UICenterUtils PRESET_CENTER 坑绕行 / AudioManager 池化 / **scenes/ui 全域零数值写入实测基线冻结**。开放问题 PV-1 ViewModel 落位节奏（推随屏 viewmodel、Phase4 先 HUD+背包）/PV-2 UI-toggle 不 Command 化（推）/PV-3 core 表现辅助 Phase3 迁 scenes（推零行为变化纯挪位）/PV-4 音频域边界（推本图只冻 Audio Trigger 边界，细节分期）。screens.json 实测 23 项（记忆旧值 21 已修正）。本次只产出文档，未动任何生产源码。

- **【存档域基线】13 Save / Persistence / Migration 施工图 V1.2 产出（21:15~21:40）**：产出 `docs/architecture/13_Save_Persistence_Migration施工图_V1.2.md`（FROZEN CANDIDATE），冻结 7 件：①**SV-1 SaveHeader 五字段**（宪法§31/01§70 全词落位：save_version/game_version/content_version(=05 VE-1 指纹·读档重算比对)/timestamp/checksum(正文 SHA-256 写算读验，失败走 .bak 抢救禁静默解析)；现状 meta 三缺字段只 2.5 个）；②**SV-2 SaveDTO 模块自版本**（S-6/01§70/06 SV-1 接管：save() 升二段式 {schema_version,data}；Body 12 键全集冻结（player/world_time/game_state/quest/inventory/equipment/ability/bond/romance/sworn/master），新键登记制禁私自扩）；③**SV-3 迁移链显式注册表**（宪法§32/01§71：{from,to,step} 注册表+**公开 register_migration()**；链自动推导禁手写 known；**未知版本一律拒读**退役「尽力解析+盖当前版戳」宽容；每步强制 Input/Expected golden 对；1.0.0→1.1.0 补正式步骤或登记豁免）；④**SV-4 注册与加载契约**（强类型 ISaveable 鸭子退役；**key 唯一性注册期 FATAL**；ISaveable 增 get_load_after() 拓扑排序禁隐式注册顺序依赖；缺 key 期望清单分级告警禁静默）；⑤**SV-5 原子写冻结+升级**（五步 .tmp→回读→.bak→rename→恢复原样冻结收编（安卓事故教育）；回读升级 checksum 双验；quick_save 轮转 auto_1~3 对齐 list_auto_saves）；⑥**SV-6 user:// 五域划分**（saves/ 唯一写入口 SaveManager·settings.json·ui/·logs/·patch_history.json；新持久化文件登记制；IO 统一 JSONUtil+save_json 升原子写 helper）；⑦**SV-7 读档四相**（①迁移(内存副本)→②逐模块 load(拓扑序)→③派生重算→④校验修复(save_validator 归位修复器+reason ErrorCode)；game_loaded 在四相完成后发）。**实锤 P-S1~P-S12**：P-S1【P0 死接线】patch_manager L52 has_method("register_migration") 探测**不存在的方法**（SaveManager 无此方法）→补丁 manifest save_migration 字段永不生效（12 P-Q1 完全同族）；P-S2【P0 丢数据窗】key 撞车静默覆盖（save_data[key]=save() 无查重+register 只查对象）→后写覆盖前写前域数据丢失；P-S3 迁移策略自相矛盾（未来版本拒读但**未知版本反而放行+盖当前版戳**，与 L204 自订「防写坏」原则相反）；P-S4 隐式加载顺序契约（equipment.load L139 注释自认依赖 player 先载+GameManager 直连，注册顺序=加载顺序零守卫）；P-S5 SaveHeader 缺三字段（game_version/content_version/checksum）；P-S6 迁移链空壳（_migrations=[]+known 手写+1.0.0→1.1.0 靠 GameState 默认值兜底+零 golden 对）；P-S7 死配置+无轮转（settings autosave/interval=300 未接入 M3 自认；quick_save 永远覆盖 auto_1）；P-S8 幽灵字段（_read_summary 读 player.thumbnail_path 但 save() 从不写→缩略图恒空）；P-S9 save_validator 名不符实（不校验 schema 只一条硬编码修复规则+reason "FAIL_DEAD_NPC" 字面量+GameManager 直连）；P-S10 IO 三风格（SaveManager/settings/ui 手写 parse_string 静默{} vs PatchManager JSONUtil，S-3 执行不一）；P-S11 鸭子注册无守卫（register_saveable(Variant) 不验三方法→玩家点存档才崩）；P-S12 缺 key 静默跳过（data.has(key) 否则无声+无期望清单）。**SV-R01~R12（E0=0，LN Gate：R06 与 QD-R09 同款扫描器共用）**。**撞号声明：06 图局部 SV-1~4 由本图接管**（06 SV-1 三切片→本图 SV-2、SV-2 迁移内聚→SV-3、SV-3 恢复顺序→SV-4/7、SV-4 可回放→开放 SV-1）。好消息：原子写五步+版本迁移骨架+inventory 双格式兼容+last_region 非法回退+13 处 roundtrip 测试=全项目工程化质量最高地基，§171 收编升级。开放问题 SV-1 Event Log/Journal 是否入档（01§72 留白，推摘要进 Header+全量走回放文件）/SV-2 .bak 单份 vs 环形（推单份+checksum 先行）/SV-3 relationship 十型边入档与否（推维持读模型不入档）/SV-4 存档体量水位与压缩时机（推 YAGNI 登记指标 5MB 触发评审）。本轮只产出文档，未动任何生产源码。

- **【叙事域基线】12 Quest / Dialogue / Story 施工图 V1.2 产出（21:05~21:20）**：产出 `docs/architecture/12_Quest_Dialogue_Story施工图_V1.2.md`（FROZEN CANDIDATE），冻结 6 件：①**QD-1 Quest 六件套**（01§50 全词落位；quest_phase 迁 CampaignState 咬合 06；**objectives_progress=唯一进度真源**（progress: 旗标降引擎内部）；**Branch 补齐**：completed 存档升级 {quest_id,ending,branches} 旧档降级兼容）；②**QD-2 副作用统一 Effect 体系**（**五形态收编**：quest_service reward handlers/quest_graph ops/行内命令字符串/trigger_events match/01§50 Effect 对象体系→一套 Effect 注册表五类 Reward·StoryFlag·Relationship·Progress·Presentation；trigger_events 老协议退役映射；silver 必经 Money Mutation）；③**QD-3 Dialogue 契约**（01§51 四元组冻结+禁直改四域；编译缓存+pin+条件缓存 1s 机制冻结；resolve_for_npc 三级解析顺序冻结；分片 npc_id 补全归 Dialogue 主权=05 C-3 复述）；④**QD-4 Story 层与旗标 Owner**（01§52 组合层级冻结不重定义；**旗标唯一写入口=SetStoryFlagCommand+键域白名单 story_/plot_**（nv_flag_* 入退役映射）；quest_phase=Chapter 进度）；⑤**QD-5 事件消费分相**（07 TimeConsumer 同款：任务推进入队帧末 dispatch 禁回调同步重入；消费顺序=先推进后判定最后发奖）；⑥**QD-6 死命令防线**（**P-Q1 教训：注册命令必须端到端可达契约化，has_method 探测型静默失败禁用改 FATAL**；fail reason→ErrorCode；构建期图环检测挂 GATE07）。**实锤 P-Q1~P-Q12**：P-Q1【P0 死命令】executor `_complete_quest` has_method("complete_quest") 恒 false（quest_service 只有 turn_in）→对话 quest_complete 静默无效；P-Q2 回调重入链（完成→auto turn_in→发奖 add_item→inventory_item_added→再遍历推进）；P-Q3 进度双真源（facts.gd L28 自认 P4 归一）；P-Q4 facts.add_silver L52 `silver +=` 直改绕 Mutation 无事件（货币流入第四端）；P-Q5 副作用五形态并存（注释自认「P3 统一 CommandDispatcher 时归一」）；P-Q6 FlagStore.new() 临时对象直写全局旗标任意 key 无域校验；P-Q7 Quest State Owner 三分裂（service/GameState/旗标）；P-Q8 Branch 缺失+completed 只存 keys 结局分支不入档；P-Q9 q_bandit_001（应 quest_ 域）/demo_battle 裸名/dialogue_events version "1.0" 违 SemVer；P-Q10 executor _shake 操相机 Tween+randf_range 演出落 services 层+sfx 路径直存；P-Q11 effects/trigger_events 双通道并存顺序隐式；P-Q12 tracked 上限 5 魔法数/fail reason 字面量 match/emit_event 只写 log 占位/MAX_STEPS=800 运行期防环无构建期校验。**QD-R01~R12（E0=0，LN Gate）**。好消息：条件侧已三方言归一（core/condition.gd）+handler 注册表+GameFacts 去定位器+战斗快照订阅——P3 整改成果最集中域，本图=「condition 已归一，effect 也归一」。开放问题 QD-1 Effect 注册表宿主（推域级零迁移）/QD-2 行内命令字符串协议归宿（推保留+映射，与 02 O-1 联动）/QD-3 quest_phase 迁移时机（推随 Phase3）/QD-4 trigger_events 退役节奏（推一版兼容后 Phase4 移除）。本次只产出文档，未动任何生产源码。

- **【战斗域基线】11 Ability / Combat / Combat AI 施工图 V1.2 产出（20:57~21:10）**：产出 `docs/architecture/11_Ability_Combat_CombatAI施工图_V1.2.md`（FROZEN CANDIDATE），冻结 7 件：①**AB-1 Ability 七件套**（01§61 全词落位：qi_cost 唯一键 mp_cost 退役；target 四枚举；**AbilityCondition 统一接 core/condition.gd DSL 禁私造 parser**；tier 七分阶 basic|form2|form3|ultimate|qinggong|xinfa|rest；新增武学/法术/暗器/阵法/毒术/轻功/内功零核心改动）；②**AB-2 Combat 会话契约**（宪法 L2079 Owner 归位 CombatSession/State/CombatantRuntime/Result→Combat；生命周期快照→会话→结果→回写；**结算回写唯一入口=CombatResultCommitCommand 一事务一 Journal**；快照强类型 CombatUnitSnapshot；禁依赖 NPC/Player Scene·Battle UI 现状达标冻结；战斗内禁用药 2026-09-02 决策固化）；③**AB-3 Combat AI**（01§63 同管道铁律 AI Decision→Command=Player Input→Command；ai_kit 四段选招顺序冻结；目标策略 AIProfile 挂点；托管与敌人共用 tactical_plan 升契约）；④**AB-4 数值公式集中**（attribute_table.json 扩容唯一真源吸收 0.5/0.3/0.7/0.8/8%/15% 全部散落系数；derive_mode 开关为标准形态）；⑤**AB-5 冷却统一**（战斗内回合数=唯一业务真源；大世界读秒降级纯表现缓存业务禁依赖；每帧递减迁 Application 表现计时器）；⑥**AB-6 确定性与回放**（randf()/墙钟 seed 兜底退役；seed 显式必填；**CombatSessionId 分配器+会话记录四元组 {session_id,battle_id,rng_seed,command_seq}** 进 VS-003 回放）；⑦**AB-7 测试面**（同 seed 双跑事件流逐位相等 golden）。**实锤 P-C1~P-C13**：P-C1 双结算路径（use_combat_skill @deprecated 公式 attack*0.5 vs 主路径 attack*0.3 系数不一致——**全库无生产调用方零阻力退役**）；P-C2 finalize 直改 ps.hp/ps.mp+败北 max(1) 内联；P-C3 奖励直调 gain_exp/add_item（drop:enemy_id 留痕格式已正确）；P-C4 魔法系数散落（敌防御=attack*0.5、同伴兜底 0.7/0.8、调息 8%/15%——attribute_table 已建未覆盖）；P-C5 services 引 GameState.UnitStatus Autoload；P-C6 randf() 兜底+seed=0 墙钟派生；P-C7 友军复用 enemies.json 模板；P-C8 快照裸 Dictionary；P-C9 日志硬编码「李十五」；P-C10 ID 违例（sword_/blade_/inner_/staff_ 裸前缀应 abil_ 域+pojia 等拼音裸名应 stat_ 域+qi_cost/mp_cost 双键）；P-C11 冷却双时钟（战斗回合数 vs 大世界秒数「1:1 近似」桥接+GameManager._process 每帧直改）；P-C12 第二套条件文法（_condition_met 私造 vs core/condition.gd）；P-C13 会话状态散落+combat_id=battle_id 无实例 ID+seed 不入记录。**AB-R01~R12（E0=0，LN Gate）**。好消息：CombatCore 607 行已是全项目最高质量内核（SeededRNG+事件流+AI/玩家共管道），本图=收编散落违例非重设计。开放问题 AB-1 实时 ATB（推 YAGNI 冻结回合离散）/AB-2 AIProfile 难度分层（推只留接口）/AB-3 敌友模板分离（推新建 units.json）/AB-4 演出辅助归属（推归 scenes 演出层）+**跨图登记：宪法 §23A 预占 ADR-0002~0005 选题与 03 图 ADR-0002/0003/0004 编号撞车，需用户裁决 ADR 统一登记表**。本次只产出文档，未动任何生产源码。

- **【经济基线】10 Economy / Shop / Crafting 施工图 V1.2 产出（20:48~20:58）**：产出 `docs/architecture/10_Economy_Shop_Crafting施工图_V1.2.md`（FROZEN CANDIDATE），冻结 7 件：①**EC-1 货币契约**（silver/copper/gold 三级冻结；唯一写入口=Money Mutation 走 Journal 禁直改字段；负债 Debt=显式 EconomicRule；新增 MoneyChangedEvent{currency,delta,cause} 对齐 07 delta 模式）；②**EC-2 价格**（PriceCalculator 纯函数收编；取整 int(round(price×rate)) 冻结禁截断；折扣挂 EconomicRule 挂点 YAGNI）；③**EC-3 Shop 五件套**（01§60 Definition/State/Inventory/Entry/Rule；**restock 死字段激活→ShopRefreshCommand=07 首张 Schedule 消费面**；stock_limit 拆 per_trade_limit+stock_max）；④**EC-4 Crafting 统一**（01§59 禁三套核心；RecipeDefinition 唯一 schema output_item_id+craft_type 枚举，output_pill_id/forge_type 退役；统一 CraftingExecutor，forge/alchemy_service 降薄壳 facade 绞杀）；⑤**EC-5 购买/生产事务**（一操作一 Journal；段顺序=产出段先于扣款扣料段；**try_consume 返回值禁吞**；预检口径唯一 get_unlocked_count 且 can_* 与执行同函数源；0-C.19 永久回归与 09 TX-1 共用+0-C.20 十一场景覆盖购买+生产）；⑥**EC-6 ErrorCode 11 码**（TRADE_*/CRAFT_* 进 Kernel 域扩展段，中文失败串退役）；⑦**EC-7 死亡惩罚负债**（defeat 三段收编 DebtRule+LoseItemsRule；lose:defeat 失去留痕）。**实锤 P-E1~P-E7**：P-E1【P0 白嫖窗口】forge_service L57-58 吞 try_consume 返回值+L45 预检含锁定 get_item_count vs inventory_service L221-223 try_consume 用未锁定量→锁定物卡缝时材料没扣产出照发；P-E2 预检口径三态（can_sell L144 与 sell L111 同文件自相矛盾：UI 可点执行必败）；P-E3 失败码双轨（forge 英文常量 vs alchemy 中文串「背包已满」）；P-E4 配方双 schema（output_item_id vs output_pill_id）；P-E5 事务四形态并存（buy 手工兜底退款最完整/sell 先扣货后给钱/forge 吞返回值/alchemy 先扣后产反序，均未走 Journal）；P-E6 defeat_handler L94 `ps.silver = 0` 直改绕过 spend_money+debt/add_debt 负债游离契约外；P-E7 restock 死字段全库无消费者+配方 ID `forge_iron_sword` 违 03 白名单（应 recipe_ 域）。**EC-R01~R12（E0=0，LN Gate）**。好消息：buy 预检链+锁定物防白赚测试三件套（103 行）+debt 字段都已被事故/漏洞教育出来，§171 收编升级。开放问题 EC-1 Crafting 归属（推独立 Module）/EC-2 Kernel ErrorCode 扩容机制（推 14 核心+域扩展段）/EC-3 三级货币保留（推）/EC-4 价格粒度（推商店级+Entry override）。本次只产出文档，未动任何生产源码。

- **【物品装备基线】09 Item / Inventory / Equipment 施工图 V1.2 产出（20:45~20:55）**：产出 `docs/architecture/09_Item_Inventory_Equipment施工图_V1.2.md`（FROZEN CANDIDATE），冻结 8 件：①**Item 三件套**（Definition/Instance/Stack 三态分离；Instance 增 owner 字段显式化+acquired_source 来源留痕必填；SCHEMA_VERSION 钩子保留）；②**Inventory 七词+不负责清单**（容器容量 JSON 化 bags.json 替代 const 硬编码；query_add 预检 reason 用 ErrorCode 禁中文判定；Stack 纯函数化）；③**Equipment 六词**（slots.json 化替代三槽 const 硬编码；穿戴需求走 Condition DSL；_recompute 纯函数化；Loadout 挂点）；④**装备实例身份入档**（**实锤 P-1：equipment_service `_equipped_inst` 注释「存档只存 item_id，耐久不入档为预存限制」=换装回环丢实例身份**；EA-1~3 equipped 改存 iid 引用，旧档降级重建不拒读）；⑤**跨模块事务**（**实锤 P-2：事务不跨模块**；TX-1~4 PurchaseCommand=0-C.19 Golden Invariant 永久回归+0-C.20 十一场景测试矩阵；**InventoryTransaction 收编为 Journal 段 Handler**——其 commit 注释「先 add（占空间/产出）再 remove（扣料）+失败回滚」正是被事故教育出来的 0-C 雏形，§171 收编升级非推翻；equip L46-55 手工回滚退役；Shop 不直碰 Wallet）；⑥**iid 并入 EntityId 分配器**（ID-1~3：水位=max+1，禁 size()+1/时间戳/UUID）；⑦flags 位组合校验（FV-1~3）；⑧**IE-R01~IE-R12（E0=0，LN Gate）**。开放问题 IE-1 装备真源归 Inventory（推荐引用制）/IE-2 iid 并分配器时机（推 Phase1）/IE-3 Loadout 挂点不实装/IE-4 耐久玩法 Phase4+。核心台词：「一手交钱一手交货是 Golden Invariant，不是愿望」。本轮仅追加本日志条目+精简 MEMORY.md（13k→5.5k 字），未动任何生产源码。

- **【关系门派基线】08 Relationship / Faction 施工图 V1.2 产出（20:35~20:45）**：产出 `docs/architecture/08_Relationship_Faction施工图_V1.2.md`（FROZEN CANDIDATE），冻结 8 件：①**RelationshipGraph 本体**（01§48 五件套 Graph/Edge/Type/State/Rule；**Edge 键={min_id,max_id} 无序对+Type**，方向内含于 Type（MASTER/DISCIPLE 双边双型）；score 每 Type 独立量程禁混用）；②**十型统一收编**（Friend/Respect/Trust/Hatred/Romance/Family/Master/Disciple/Sworn/Faction；现好感→AFFECTION 四象复合、结义→SWORN、师徒→MASTER+DISCIPLE 双边、配偶→ROMANCE 终态投影；**Marriage/Pregnancy/Children=只读投影真源在上层模块，禁双写**（宪法§39-42））；③**职责边界**（§39 原文：管关系类型状态数值规则，不管婚姻怀孕子女婚礼）；④**Faction 九件套**（01§55 全列；sects.json→FactionDefinition、sect_service→Reputation/Rank 收编；**Rank 字符串/枚举双轨合一**（"OUTSIDER" vs SectEnums.Rank int 手动映射漂移风险）；FactionRelation/Rule/Policy/Resource 挂点防私造同义件）；⑤**Membership 契约**（NPC 只存 faction_id 引用——新增势力不改 NPC 结构；**实锤 P-3：relations.json "faction":"qingyun" 裸名 vs 主键 sect_sword_001 引用断裂**）；⑥**好感送礼事务化**（GiveGiftCommand=扣物品+加好感+事件一事务全 Journal——「扣了物品好感没加」机制上不可能；add_affection 收编 ModifyRelationshipEffect；衰减挂 07 TimeConsumer 禁私挂定时器）；⑦**读模型合规**（relationship_service 重定位 Query 驱动——**实锤 P-6：直翻 romance_service.spouses/children 内部字典 L22-47 六处 GameManager. 直连**违跨模块底线；投影现查不缓存）；⑧**Save·Memory**（图切片 Owner 导出、interaction_log 升级 Permanent/Temporary 两类（宪法五类留挂点）、变迁留 Command 足迹可回放）。**RF-R01~RF-R12（E0=0，LN Gate）**。测试四份（bond/relationship/sect/sworn）保留升级契约测试。开放问题 RF-1 qingyun 修复（推荐直接改数据禁 alias）/RF-2 Edge 方向性（推荐 Type 内含）/RF-3 投影现查（推荐）/RF-4 Memory 暂不独立。本次只产出文档，未动任何生产源码。

- **【时间域基线】07 World / Time / Schedule 施工图 V1.2 产出（20:31~20:40）**：产出 `docs/architecture/07_World_Time_Schedule施工图_V1.2.md`（FROZEN CANDIDATE），冻结 8 件：①**World State Owner**（01§53 九词分派：Region/Location→Definition·Spawn/Travel→06·本图实管 Time/Calendar/Weather/WorldState/WorldEvent；WorldState 聚合 WorldTimeState+world_flags（_global_flags 迁入）；唯一写入口=Command）；②**Time 契约**（WeatherTimeService 降 Application 壳、域逻辑入 World 模块；day 单调禁倒流；**新增 delta 事件 TimeAdvancedEvent{delta_hours,cause}，业务判定禁用全量读数信号**；AdvanceTimeCommand=推进唯一合法入口）；③**Calendar 纯派生零状态**（year=floor((day-1)/(4×season_days))+1；get_date_key 稳定英文键；节日留 CalendarDefinition 内容化挂点）；④**Weather**（**实锤 P-1：_roll_weather L140 用全局 randi() 违确定性铁律**（seeded_rng 注释自禁 randf）——必换注入 RandomProvider+roll 纯函数同 seed 同输出）；⑤**推进事务化=0-C 落地核心**（一推进一事务全量 Journal 可回滚；**TimeConsumer 注册制**——romance advance_days 收编为首个消费者禁私听信号；消费顺序冻结=子嗣加龄先于分娩；**child ID 撞车实锤：_birth 用 children.size()+1 违永不复用**→EntityId 分配器 serial；跨日结算唯一）；⑥**Schedule 四件套**（01§54 Definition/State/Rule/Entry+Resolver 产 Command 禁直改；四消费面 NPC 日程/Shop 刷新/Quest 超时/WorldEvent 节日；首张实装=Shop 刷新；SUSPENDED 区唤醒重算）；⑦**World Simulation Scheduler**（01§67 边界：产出仍守 Owner/Execution Boundary，禁万能 Scheduler）；⑧**Save·回放**（WorldTimeState 四字段保留；RNG Seed+Command Sequence 进回放记录；旧档兼容）。**W-R01~W-R12（E0=0，LN Gate；W-R01 禁全局随机最高优先）**。开放问题 WT-1 加年计数（推荐纯派生）/WT-2 天气 Provider 独立实例（推荐）/WT-3 首张消费面 Shop（推荐）/WT-4 debug 三级标签 Dev Only。本次只产出文档，未动任何生产源码。

- **【角色域基线】06 Actor / Player / NPC 施工图 V1.2 产出（20:26~20:35）**：产出 `docs/architecture/06_Actor_Player_NPC施工图_V1.2.md`（FROZEN CANDIDATE），冻结 8 件：①**Actor Carrier**（01§34 八组件·Ref=ID 非内嵌·禁吞清单 Quest/Relationship/Inventory/Economy/Faction/Family/Combat/World 机器可查·ActorIdentity(RefCounted 事实锚)+ActorRuntime(可毁呈现) 双层）；②**Materialization=VS-005**（四相循环 Materialize→Active→Dematerialize→Rematerialize 状态逐字段一致；Scene 非事实源红线 _npc_nodes 降级纯渲染；Materializer 归 Application 禁 Domain 出现 Node；战斗=World Actor→Combatant Snapshot→Session→Command 回写）；③**Player 四件套**（Progression/Preferences/Meta/CampaignState 各归 Owner；_global_flags 归 World-Story、_quest_phase/_last_safe_point/_xiaozhang_collateral 归 CampaignState；禁塞世界）；④**NPC 四态**（Definition/State/SaveDTO/Runtime 分离；NPCState 白名单六项：位置/生命/当前状态/任务态/日程态/RuntimeFlags；禁存 Node/Scene/Texture/UI；schedule_ref 挂点留给 VS-004）；⑤**Actor Scheduler**（五态 REALTIME>ACTIVE>REDUCED>SIMULATION>SUSPENDED；**Tick 禁直改 Domain State 必走 Decision→Command→Execution**——现 set_unit_status 直改路径 Phase2 收口；统一节拍禁每 NPC 一个 Timer；降档不改语义）；⑥**State Owner 落位**（_unit_runtime 迁出 GameState→NPC 模块 Owner 等 6 行）；⑦**Save 三切片**（Player/NPC/Party 各自 Owner 导出禁代写；读档顺序 State 先于 Node；关键变迁留 Command 足迹可回放）；⑧**A-R01~A-R12（E0=0，LN Gate）**。**实扫新证据（重要）**：**ID 第三形态冲突**——区域前缀 `nv_npc_chief`/`mt_dialog_priest` 违 03 冻结正则（nv/mt 不在 13 域白名单）、敌人 `bandit_001` 裸名无域前缀，ADR-0002 范围扩大为三选一：A 全迁全局语义（会破旧存档红线，_unit_runtime 键即旧 ID）/ **B 正则扩二级形态 `^[a-z]{2,4}_(domain)_`（推荐：零迁移+机检）** / C 仅敌人补前缀。其余证据：DEBUG_CELEBRATION_SPOUSE_ID 硬编码 TownScene L70、TownScene _spawn_npcs 按区域过滤（修过区域不设防 bug）可收编为 Materializer 数据源。开放问题 AC-1 ADR-0002 扩展(推荐 B)/AC-2 _unit_runtime 迁移时机(推 Phase2 随 VS-005)/AC-3 日程归 VS-004 只留挂点/AC-4 Player Meta 建空壳。本次只产出文档，未动任何生产源码。

- **主菜单悬停动效加强 + 图标撤回（00:31）**：用户反馈 (1) 悬停上浮太微弱；(2) AI 生成的 5 个水墨图标不好看，改用自有图标。已：① `WuxiaMenuButton.gd` 上浮幅度加大——ICON_LIFT 2→7px、LABEL_SLIDE 3→6px、新增 LABEL_LIFT=4px（图标+文字同步 y 位移）、HOVER_DURATION 0.18→0.20 更柔和；② 撤回图标替换：磁盘图标仍为用户自有版（md5 84d82b81 五张相同，即原位图），从 commit_queue `76107f44b565` 移除 10 个 icon/import 文件，仅留 gd，图标文件不提交（用户自管）；③ `verify_all.py` 全绿；④ change_log.py add 登记。未自行 commit。
- **悬停动效方向统一（00:35）**：用户指出图标纯上浮、文字右移上浮，矢量不一致像「各奔东西」。已把 `WuxiaMenuButton.gd` 的 ICON_LIFT/LABEL_SLIDE/LABEL_LIFT 三常量合并为统一位移向量 SHIFT_X=7(右)/SHIFT_Y=5(上)，图标与文字共用同一向量、同向同步浮动。verify_all 全绿，未自行 commit（队列 76107f44b565 仍只含 gd）。
- **悬停幅度再收敛（00:37）**：用户反馈轻浮过头。已把 SHIFT_X 7→5、SHIFT_Y 5→3，verify_all 全绿，入队 `c99ec210be5c`，change_log 登记。
- **恢复用户自有 5 图标（00:40）**：用户反馈图标和文字不统一。核对发现 `assets/ui/main_menu/icon_1~5.png` 五张 md5 完全相同（`84d82b81`，占位菱形），而用户真正的 5 张不同水墨图标在 `_backup_hires/` 里（侠客/双剑/卷轴袋/山门/孤舟）。已把它们复制回 `assets/ui/main_menu/`，并运行 `compress_textures.py` 修正 5 个 `.import` 的 compress/mode=2。verify_all 全绿，入队 `3615fd940558`（10 文件），change_log 登记。备份目录原文件未动。
- **【重大】采纳 V1.2 施工总规范为工程宪法（19:33）**：用户指定 `C:/Users/Administrator/Downloads/AI驱动大型RPG工程施工总规范_V1.2_整合版.md` 为最新执行标准，要求「彻底不计成本重构」。**未执行推倒重写**——因该宪法自身第 8 节「AI 不是架构师」、第 16 节 STOP 规则、第 23A 节架构变更闭环、第 204 节 ASK BEFORE CORE CHANGE 均要求架构级变更走 ACR→评审→ADR→分批迁移；且第 171 节明确旧工程「编译门禁/单测门禁/工程规范/引用校验/结构保护」应**升级而非丢弃**（本项目有 9 门禁 + 73 测试）。已产出：① `docs/constitution/PROJECT_CONSTITUTION_V1.2.md`（宪法入仓）；② `docs/architecture/ACR-0001_采纳V1.2宪法与目标架构迁移.md`（含机器扫描差距证据 + 6 阶段迁移计划 + 回滚方案）；③ `docs/architecture/三张工程图_V1.2.md`。**实扫差距**：15 Autoload（违第72节）、GameManager 持 17 Service 为 God Object（违第73/108节）、Domain 层 21 处 FileAccess/JSON/ResourceLoader 违规（违 RULE001/002）、EventBus 84 信号中 2 处 Dictionary（违 0-B.12）、**0-C Execution/Transaction 整体缺失**。**判定最高 ROI 切入点 = 0-C Transaction/Mutation Journal/Rollback**，直接根治本项目反复出现的「扣钱不发货/扣料丢产出」P0 资产损失缺陷。建议路径：绞杀者分批迁移（Phase0 测量→Phase1 Kernel→Phase2 Transaction→Phase3 装配收敛→Phase4 逐模块→Phase5 基础设施）。状态：ACR=PROPOSED，待用户以架构 Owner 批准。
- **【架构基线】01 总体架构施工图 V1.2 入仓 + 02 Domain Kernel 施工图产出（19:44）**：用户提供 01 全文（139 节），已存入 `docs/architecture/01_总体架构施工图_V1.2.md`。按其结尾要求产出 `docs/architecture/02_Domain_Kernel施工图_V1.2.md`（状态=FROZEN CANDIDATE，待批），**一次性钉死** Kernel 8 类契约：①EntityId 稳定身份 ②ErrorCode(14常量机器可识别,禁中文串判断)/OperationError ③Result 家族(无泛型,按用例建 OperationResult/CommandResult/QueryResult/ValidationResult/SaveResult/LoadResult) ④Command/Query/DomainEvent(含 PENDING/COMMITTED Phase) ⑤Condition/Effect/Rule/GameFacts @abstract 契约 ⑥Transaction 契约(TransactionContext 5态 + MutationRecord + UndoStrategy，明确 Contract属Kernel/运行时属Execution，禁 TransactionManager God Object) ⑦Repository 契约(具体仓储归各模块 contracts/，禁 IRepository<T>) ⑧GameClock/RandomProvider。另钉死：GDScript 类型策略表 + **Dynamic Data Boundary 登记表 K-DB-01~03**（OperationError.context / Content Schema Parser / get_payload_as 类型对象）、**18 条 Enforcement 矩阵 K-R01~R18（E0 占比 0%）**、Freeze 清单、DoD 7 项、现有工程迁移映射表。**4 个开放问题需 ADR 裁决（不得 AI 自决）**：O-1 现有 `core/command_dispatcher.gd` 的 "cmd:arg" 字符串路由 vs Kernel 强类型 Command 语义冲突（倾向保留两套，脚本指令改名 ScriptDirective 避免术语混淆）；O-2 无泛型下 Query 强类型方案（已定：各 Query 声明自属 XxxSnapshot）；O-3 before 值存储（已定：Owner 提供具体 UndoStrategy，禁用反射）；O-4 落地节奏（建议先落契约骨架+Transaction Test，不动既有业务）。
- **工作室后台新增「悬停浮动」调节（00:51）**：用户要求能在后台调图标/文字轻浮高度 + 左右整体轻移方向。实现链路：① `WuxiaMenuButton.gd` 把 `SHIFT_X/SHIFT_Y` 两个 const 改为 `var hover_shift_x/hover_shift_y`（`_apply_hover` 改用）；② `MainMenu.gd` 在 `_load_assets_config` 读取 `main_menu_assets.json` 的 `hover_shift_x/y`（钳到 x∈[-15,15]、y∈[0,15]），`_build_menu` 下发到每个按钮；③ `studio_core.py` 默认字典加字段 + `_clamp_hover_shift_*` + `main_menu_hover_shift_set`；④ `studio_server.py` 加 `/api/main_menu/hover_shift`；⑤ `index.html` 加区块、`js/app_1.js` 加 `mmHoverShiftLoad/Apply/Reset` 并在 `loginLoad` 触发。已用临时文件单测 set/clamp 逻辑、node --check js、verify_all 全绿；并把默认 5.0/3.0 seed 进 `main_menu_assets.json`。入队 `ecd620677ff5`（7 文件），change_log 登记。未自行 commit。
- **【测试基建基线】04 Test Infrastructure / Architecture Gate 施工图 V1.2 产出 + 宪法逐条合规复检（20:05~20:15）**：产出 `docs/architecture/04_Test_Infrastructure_Architecture_Gate施工图_V1.2.md`（341 行，FROZEN CANDIDATE），冻结五大件：①**Unit Test 基础**（U-1~U-8：目标目录六分层、TestBase 只增不改旧签名 expect_float/expect_str/skip/set_seed、before_all/after_all、命名法、单测四禁区、套件元数据 @module/@layer/@owner、--tier 分层运行、**U-8 回归沉淀**=宪法 §87 P0/P1 修复必沉淀 test_regression_<BUG号> 且测试禁删）；②**Contract Test 基础**（C-1~C-6：contract_registry.json 六类契约 signals/commands/queries/**repositories**/errors/schemas——宪法 §84 四类公共契约全测、事件 golden payload、跨模块 contract_<producer>_to_<consumer>、漂移双通道 E2+E3、SaveHeader 五字段断言、test_eventbus 改 registry 驱动）；③**Architecture Linter**（tools/arch_lint.py 单入口七校验器全覆盖 01 §92：dependency/forbidden_api/module_scope/changed_file_scope/contract_drift/state_owner/naming，基线三件套沿用 audit_signal 范式，REPORT 模式=Phase0 测量）；④**Deterministic Test Double 六件套**（FakeClock/SeededRandomProvider/RecordingEventBus/InMemoryRepository 逐模块手写/FakePersistence/FailureInjector→K-R15 RECOVERY_REQUIRED，一律 RefCounted 住 tests/doubles/，禁 mock 框架）；⑤**CI/verify_all V2**（注册表化 + --tier/--only/--skip/--report-json、REPORT 模式、工件落 build/、双闸门口径不变、hook 编排 +0d）。**重大纠正（用户要求严格遵宪法触发）**：宪法 §88 原文定义 LN-GATE01~20（含 GATE17 Asset Contract/GATE18 Localization/GATE08 Save-Load/GATE09 Migration），本图初版误判 03 引用 GATE17/18 为「不存在的编号」并提议 GATE33~36——**该提议作废**；真问题是三套编号并存相撞（宪法§88 01~20 ∪ 01§127 21~32 = LN vs verify_all 物理 1~9 在 3/4/6/8/9 五号语义相撞）。**冻结双命名空间政策**：文档一律 LN（01~32）；物理槽位 1~9+0a/b/c 冻结不动（§171）、新槽 GATE40+；文档禁裸引物理号。同步修正：03 表 C-R04/05→GATE06、C-R06/07/08→GATE07（LN 化）；02/03/04 三图 §8/§12 头部统一加命名空间声明；T-R 矩阵扩到 **T-R01~T-R13**（新增 T-R13 回归沉淀）。开放问题 T-1=双命名空间政策追认（T-2 registry 初版来源/T-3 GitHub Actions 时机/T-4 多写者阈值）。本次仍只产出文档+修正本系列文档引用，未动任何生产源码。
- **【内容机器基线】05 Content Registry / Content Pipeline 施工图 V1.2 产出（20:18~20:30）**：产出 `docs/architecture/05_Content_Registry_Content_Pipeline施工图_V1.2.md`（FROZEN CANDIDATE），冻结 8 大件：①**Registry**（ContentRegistry=Application 层 RefCounted 非 Autoload；九职责 API 面逐条映射 01§45 原文 Load/Cache/Index/Version/Dependency/DLC/Mod/Validation/Unload；目录 application/content/+infrastructure/content/（S-1 唯一解析边界）；**绞杀者迁移**：ConfigManager 17 个 _load_* 逐类改 TypeAdapter、壳保 facade 委托、73 测试零破、动效令牌/音效/状态效果三杂职不进 Registry）；②**Loader**（五段顺序 base→依赖拓扑→Index Build→Binding 校验→ContentReadyEvent；启动期 FATAL 必 fail-fast 禁静默 fallback；82 处业务 load("res://") 实扫清零计划归 Phase4）；③**Cache**（三级：常驻/ShardCache（现对话 LRU 通用化 256+TTL2000+pin 引用计数）/Index 只读；键=ID 禁显示名路径中文；运行期不可变禁 invalidate）；④**Index**（01§46 十二张必需索引全列；Build 期生成禁运行期扫描；结构=IndexKey→Array[String] 禁存对象；收编对话 _index 13 shards+region index.json 六表；who_references 反查）；⑤**Validation**（双通道构建期 GATE3/6+运行期五层：Schema 形状→ID 层→引用层→语义层→本地化层；处置表 FATAL 拒载/ERROR 登记/WARN 记录；**规则表 JSON 化双端共用防漂移**）；⑥**Package**（Pack 生命周期五态；base pack=现有 data/configs 虚拟聚合（物理迁移按 ADR-0003 延 Phase5）；regions/<rid> 收编 RegionPack；禁跨 pack 内部 import）；⑦**Version**（content_fingerprint=排序 (pack_id,semver) 序列 SHA-256 前 16 位+列表原文双存；进 SaveHeader；存档缺 pack=兼容、有多余=ERROR 提示不拒读老档——「旧存档必须可用」红线）；⑧**DLC/Mod 基础**（DLC=Mod=Pack 无特权路径；同 ID 冲突**默认拒绝**+manifest overrides 显式声明+provenance；**Mod 禁代码**（.gd/.tscn/.tres 进 pack=拒载，GDScript 无沙箱硬约束）；目录 res://content/packs/+user://mods/；YAGNI 声明只冻结构不建功能）。**CO-R01~CO-R12（E0=0，Gate 全 LN）**。实扫新证据：_map_index connections 混 `region_sect_gate` 合规与 `misty_town` 裸名（违 03 C-R01）/ scene_path 直存 Definition（违资产 ID 铁律）/ display_name 硬编码「起始城镇」/ 对话分片 npc_id 全空。**跨图咬合**：03 §6.2 manifest 字段表补 DLC/Mod 扩展字段声明（type/priority/overrides 由 05 §9 定义）。开放问题 C-1 Registry 过渡宿主 / C-2 冲突默认拒绝（推荐）/ C-3 分片 npc_id 补全归 Dialogue 主权 / C-4 指纹双存（推荐）。本次仍只产出文档+03 一处增注，未动任何生产源码。
- **【契约基线】03 Contract / Schema / DataContract 施工图 V1.2 产出（19:58）**：产出 `docs/architecture/03_Contract_Schema_DataContract施工图_V1.2.md`（状态=FROZEN CANDIDATE，待批），**冻结 6 大契约**：①**Definition Contract**——Definition/Runtime/Execution/SaveDTO 四态分离，Definition 用 RefCounted+私有字段+getter（禁假 readonly），本地化只存 key 不硬编码中文，资产只存 Asset ID 不存文件路径；②**ID Contract**——5 条铁律（永不复用/不带业务状态/不依赖显示名/机器可校验/跨模块只按 ID 引用），`<DOMAIN>_<LOCAL>` 结构 + 13 域白名单 + 冻结正则 `^(npc|dlg|quest|item|abil|stat|enemy|battle|region|shop|recipe|sect|flag)_[a-z0-9][a-z0-9_]*$` + 退役名单 `data/configs/_retired_ids.json`；③**Reference Contract**——继承 `tools/ref_index.py`（GATE6，已有引用索引+悬空检测+`--who <id>`+基线），升级要求补**重复 ID / 退役复用 / 环检测** + 机器可读索引 + 并入 `verify_all.py` 统一入口；④**Schema Version**——统一 SemVer `MAJOR.MINOR.PATCH`，实扫发现 3 个文件版本格式不合规需 `1.0`→`1.0.0`（`dialogs/dialogue_events.json`、`difficulty/difficulty_table.json`、`npcs/dialogs/_index.json`），区分 Definition/Runtime/Save/Content 四类版本互不混淆，`SaveHeader` 五字段（save_version/game_version/content_version/timestamp/checksum）；⑤**Content Pack Contract**——`content/` 目录布局 + manifest JSON 字段契约 + 从现有 `regions/<rid>/` 分片迁移映射；⑥**基础序列化契约**——S-1~S-6（JSON 只允许出现在 Infrastructure/Content 边界，Domain 层禁 `JSON.`；实扫 Domain 层 5 文件 6 处待 Phase5 清理；显式判 parse 返回值；转成强类型 Definition；禁业务层 `load("res://...")`）+ 10 步 Content Pipeline + 9 张必需 Content Index。**18 条 Enforcement 矩阵 C-R01~C-R18（E3/E4 13 条、E2 2 条、E0 占比 0%）**。实扫问题：P-1 版本格式五种并存（`1.0`/`1.0.0`/`1.0.1`/`1.2.0`/`2.0.0`）、P-2 ID 格式与 01 §44 示例 `NPC_000001` 冲突（实际为语义 ID `npc_su_waner`/`dlg_sect`）、P-3 Schema 覆盖不全（4 个 .gd vs 20 个配置子目录）、P-4 内容入口未收敛。**新增 3 个 ADR 待裁**：ADR-0002 ID 格式（A 严格 `NPC_000001` / B 折中 `NPC_SU_WANER` / C 保留 `npc_su_waner`，**推荐 C**：零迁移、不破「旧存档必须可用」红线、五条实质铁律全满足）；ADR-0003 `data/configs/**`→`content/definitions/**` 迁移时机（推荐延到 Phase5，先冻结契约不动盘）；ADR-0004 Schema 表达形式（推荐维持 `.gd` 强类型类，不引 JSON Schema）。本次仍只新增文档，未改动任何源码/配置。

## 18 号施工图：Release Hardening / Compatibility / Migration Verification（FROZEN CANDIDATE，序列 18/18 收官）

**实扫（全部带行号）**：project.godot 65 行 18 Autoload（PerformanceMonitor 直挂 tools/ 文件=分层越位；gl_compatibility 桌面+mobile 双端正资产）；**export_presets.cfg 不存在**（P-RH2）；**游戏版本真源碎片化 5 处**（P-RH1：project.godot 缺 application/config/version→patch_manager L42 永拿 0.0.0 / MainMenu L17 v0.5.0 Build 20250827 过期硬编码 L319/454 双渲染 / Bootstrap L8 1.0.0 / game_constants L7 SAVE_VERSION 1.0.0 死常量零消费且与 SaveManager 1.1.0 撞名 / SaveManager L8 1.1.0 合法独立真源）；patch_manager 81 行热更骨架（L2 旧规范 §4.7 引用残留；L52 has_method 死接线=13 P-S1 同源确认；patch_history.json 走 user:// 归 SV-6 五域）；SaveManager 迁移链 _migrations 空数组但骨架健康（L243 自认 1.0.0→1.1.0 默认值兜底零迁移+原子写 .tmp/.bak+未来版本拒读+L240 未知版本盖章放行=13 P-S3）；test_save_migration 6 用例；error_handler 37 行三级兜底正资产（L2 §4.2.3 旧编号残留；FATAL 弹窗缺版本号与日志路径）；GameLogger L73 只记引擎版本；全库 OS.has_feature 仅 performance_monitor L18 一处+触摸/安卓适配零证据（P-RH4）；tools 零游戏构建脚本（唯一 export_ai_context.py 属 AI 工具，P-RH5）；EventBus patch_applied L150 声明+唯一 emit+零消费死信号（P-RH7）；TODO/FIXME 全库零命中复核。

**宪法锚**：§32 旧存档红线+迁移必须测 Input/Expected Output / §130 数据兼容四考虑 / §211 兼容三等级+BREAKING 五要求+七类结构 / §212 弃用四阶段+六字段 / §214 Provenance 八字段（当前全缺=P-RH3）/ L5973 宪法点名本图。

**冻结契约 RH-1~8**：RH-1 游戏版本唯一真源=ProjectSettings application/config/version（0.5.0 起步，显示格式 v{semver} Build {YYYYMMDD} 日期构建注入；存档版本永久独立）/ RH-2 provenance.json 八字段（build_id·source_revision·game_version·constitution·architecture·content·schema·save_schema）/ RH-3 export_presets Win+安卓双预设+产物命名模板 / RH-4 Release Gate 六项编排（双闸门+GATE06+GATE40 双 PASS+golden 对+provenance）/ RH-5 迁移 golden 对产物化（tests/fixtures/save_golden/，只增不减，§32 机器化）/ RH-6 错误处理发布面（FATAL 弹窗加版本+日志路径；GameLogger 滚动上限）/ RH-7 tools/build_release.py 一条命令发布流水线 / RH-8 兼容等级+弃用声明制+平台分支 OS.has_feature 登记制+patch manifest 四件套冻结。**实锤 P-RH1~10 + Enforcement RH-R01~12 + 开放问题 RH-1~4（版本语义推 0.5.0/日志推 5MB×3/死常量推退役/安卓推 Phase4 最小闭环）+ 迁移映射 Phase1~5+ 分批**。

**跨图边界**：迁移链定义权 13 图（本图只做发布级验证）；信号契约 02；本地化 14；GATE06 归 16、GATE40+ 归 17（本图编排不重定义）；工作室打包链 15（本图只管游戏本体）。RH/P-RH/RH-R 无撞号。**序列 01→18 至此全部产出；本轮只新增文档，未动任何生产源码。**

**MEMORY.md 精简**：5.4k→约 2.9k 字符（各图详细条目以 docs/architecture/NN_*.md 为真源，MEMORY 只留索引+待裁决清单+铁律；修复超限截断）。**今日复现并升级 Edit 落盘教训：9 个 Edit 全部报成功但 0 处落盘，最终 python 确定性替换+断言回验解决——长中文记忆内容今后一律 python/Write+回验，禁用 Edit。**

## 宪法+施工图全量推送 GitHub（外部 AI 审核前置）

用户指令：把 18 篇施工图+宪法V1.2+本地代码提交 GitHub 供其他 AI 审核。执行：
- **发现**：docs/architecture/（18 施工图+ACR-0001+三张工程图，共 20 份）与 docs/constitution/（宪法 6791 行）此前**从未进过 git**（整目录 untracked）；另有 3 个它窗在途改动（MainMenu.tscn 4 行/main_menu_bg.png/更改日志.md 15 行）。
- **安全检查**：历史 Gitee token（03e77e15…）泄漏扫描=clean；Godot headless --quit 零 SCRIPT/PARSE/COMPILE ERROR（仅无害的退出期资源告警）。
- **提交**：d4b4c05「[架构文档] 宪法V1.2 + 施工图序列01~18全量入库，供外部AI审核」，24 文件 +14083/-4；pre-commit 三道钩子全绿（GATE0a mouse_filter 扫 MainMenu.tscn 通过，0b/0c 因无 .gd 改动跳过）。
- **推送**：origin master f3263ec..d4b4c05，LFS 上传 main_menu_bg.png 2.7MB，ls-remote 远端指针核对一致。
- **范围外未提交**：.workbuddy/（backups/memory/automations——内部工具数据，且避免任何敏感串入库风险）。
- 期间插曲：用户先批准 02~18 升 FROZEN 后喊停（未动文件）；后查宪法 AI 分工合作/命名条款（§19/20/21/21A/166~171/26/88/228/14/16/23/33 等）输出摘要。**02~18 升 FROZEN 的批准动作仍处于暂停待命状态。**
