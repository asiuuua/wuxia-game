# 2026-09-02 工作日志

## P2 全量收尾（用户"一起干"，本段最后一块）
- **范围**：审计报告第六章剩余深层项 #10（其余服务单测）/ #12+BUG-13（工具路径相对化）/ #13+BUG-14（town.json 死数据核查）/ #14（健壮性小修 BUG-12/19/20/21/22/23/24/25/26）。4 笔提交：`24069f5`(GameManager _deferred_change_scene 改 change_scene_to_file 修主菜单 freeze) / `6da20ec`(健壮性小修) / `a44ddc4`(7 个服务单测) / `5d941d9`(工具路径相对化)。
- **落地**：BUG-09(inventory 堆叠路径加负重上限) / BUG-12(BattleScene `_aborted`+`_battle_dead()` await 守卫) / BUG-13(`_gen_matte_175.py` discover_project_root + ffmpeg 优先 PATH/环境变量) / BUG-19(ability use_combat_skill 标 @deprecated) / BUG-21(romance `_special_portrait_cache` 静缓存，删 get_quanquan 冗余写) / BUG-22(quest+combat 缺字段 `String(obj.get("id",""))` 降级) / BUG-23(TownScene 删死监听 player_hp_changed) / BUG-25(LoadingScreen+SettingsScreen `_exit_tree` 显式断信号) / BUG-26(InventoryScreen `if slot==null: continue`) / 7 新单测(test_item_flags/quest/ability/alchemy/forge/sect/game_state 守 BUG-15/27)。
- **BUG-14 误删恢复（铁律级教训）**：初判 town.json 为死数据（区域走 regions.json，无代码 load）删之 → GATE2 退化 3 失败（tactical_mapbound_002 的 grid.width 为 Nil）。根因：town.json 是 town 战术底图几何配置，经地图绑定**间接**引用，非直接 load。→ `git checkout HEAD -- data/configs/maps/town.json` 恢复，判非死数据，保留。**审计"无代码读取"判定不可仅凭直接 load 字符串 grep，须追数据绑定链。**
- **BUG-20 跳过**：combat_core:470-471 的 `plan` 赋值在循环**外**，非冗余，语义正确，不修。
- **BUG-16/17/18 仅登记未硬改**：BUG-16 跨模块读取型 DI 留待契约总表规范；BUG-17 inventory buff/cure TODO 登记为已知未完成项；BUG-18 combat ally 快照死代码，启用 ally 前不动。
- **过程陷阱（已闭环）**：① Edit 在 `mini(put,room)` 后混入非法反引号 → GATE1 Parse Error(242 行)，grep `\x60` 定位删除；② town.json 误删（上）；③ test_sect_service 误用 `expect_eq` 比 String（基类仅 int）→ 改 `expect(...=="...")`；④ BattleScene `_on_auto_pressed` while 内 if 分支缩进错乱 → 重写。
- **验证（决定性）**：GATE1 rc=0 零错误；GATE2 **53 套件/0 失败/0✗**。tools/ 清 6 个临时文件。审计报告补第十一章；工作树无未提交改动（4 提交已落）。
- **终态**：P0+P1+P2 全绿，路线图中 #10/#12/#13/#14 闭环，工程进入"全绿待发布"。仅 BUG-16 架构规范 + BUG-17 背包 Phase 扩展两项非阻塞待办。

## 主菜单按钮：悬停选择音效（2026-09-02 凌晨）
- **需求**：给主菜单 5 个按钮（开始/继续/设置/额外/退出）增加「鼠标悬停时播放选择音效」。
- **关键发现（根因）**：
  1. 代码早已存在悬停音效链路：`WuxiaMenuButton.gd` 的 `_on_mouse_entered` → `_play_hover_sfx()` → `AudioManager.play_ui_sfx("hover")`。
  2. 但 `resources/audio/sfx/` 目录**完全是空的**——`ui_sfx.json` 里注册的 8 个音效（hover/click/confirm/cancel/back/open/close/error）全部缺失；`AudioManager.play_ui_sfx` 在 `ResourceLoader.exists(path)` 失败时空静默跳过 → 所有 UI 音效一直无声。
- **修复**：
  1. 新增 `tools/gen_ui_sfx.py`（纯标准库合成 8 个短促占位音效，16bit/44100/mono，45~200ms），生成到 `resources/audio/sfx/`。占位音；正式音频替换同名文件即可，配置表不用动。
  2. **修了一个隐藏的鼠标拦截 bug**：`WuxiaMenuButton.tscn` 里 `bg_panel`(800.8x131.6) 比按钮(286x47)大且 `mouse_filter=2(STOP)`，会拦截鼠标导致按钮本体 `mouse_entered` 不触发（连悬停高亮都可能不灵）。把 4 个装饰子节点(bg_panel/icon/label_main/label_sub)的 `mouse_filter` 全改为 `0(IGNORE)`，事件穿透到按钮本体，悬停音效/高亮/点击都可靠触发。这是 Godot 装饰子节点不拦截输入的标准做法。
- **验证**：提交 `f6a52ef`（10 files：8 wav + tscn + 生成脚本），GATE1 通过（`RC=0` 零解析/编译/加载错误）。纯游戏侧改动，无工作室工具/端口动作。
- **备注**：用户是电脑小白，无法自产音频；合成占位音是项目「占位期文件未到位就静默跳过」设计的合理落地。正式音效让用户替换 `resources/audio/sfx/` 下同名 wav 即可。

## 登录界面卡死 BUG 修复（2026-09-02，重大修正）
- **现象**：进入游戏登录界面（主菜单）直接卡死/无响应，反复多次（用户连报"又双叒叕卡死了"）。
- **⚠️ 根因更正（关键）**：首轮误判为"190MB 未压缩纹理触发显卡 TDR 超时"。**此诊断错误**——用户实测显卡是 **NVIDIA RTX 3070 Ti（8GB 独显）**，且日志显示引擎在跑（帧 1→138 递增、Bootstrap 已就绪），独显不可能因 190MB 显存 TDR。
- **真正根因（已定位+修复）**：`scenes/ui/components/ui_background/UIBackground.gd` 的 `_sample_edge_stops()` 用 `tex.get_image().get_pixel()` 采样背景图边缘色做渐变垫底。把纹理改压缩导入（mode=2）后，`CompressedTexture2D.get_image()` 返回压缩数据，对其 `get_pixel()` 疯狂报错 `Can't get_pixel() on compressed image, sorry.` **9024 条 ERROR 刷屏拖死主线程 = 用户看到的"卡死"**。这是"修压缩反而暴露 latent bug"的典型。
- **修复**：`_sample_edge_stops()` 改用 `Image.load_png_from_buffer(buf)` 直接解码源 PNG 字节取像素（跨 4.x 稳定实例方法；`Image.load_from_file` 在 4.7.2 已非实例方法，会 Parse Error）。与导入压缩解耦：既保留压缩纹理小显存/快加载，又能正常取像素。非 PNG/源缺失则静默回退硬编码绿渐变。
- **验证（决定性）**：无头复现 `godot --headless --path` 至主菜单，`get_pixel` 错误 **9024→0**，日志 **99332→61 行**，能正常进主菜单不卡。GATE1 零 SCRIPT/PARSE/COMPILE ERROR。
- **全量压缩（双保险第一层已落地）**：除登录 9 张外，工程里还躺着 **162 张未压缩纹理**（matte_* 124 张、half_body、battle_bg、resources/ 等）。本轮全量治理：145 个 `.import` mode=0→2、2 张超大源图缩放备份、3 个 resources/ 纹理压缩、修复 `preset_12x12.jpg`（实为 2048×1152 PNG 被错命名 .jpg，Godot 无法导入→改名 .png 并改 JSON 引用）。最终 **157 个纹理全部压缩**（`.s3tc.ctex`），删除 19 个孤儿未压缩 .ctex，无残留未压缩引用。
- **双保险机制（用户硬性要求"自动压缩+双保险"）**：
  1. **工作室路径**：`tools/desktop_studio/tscn_assets.py` 的 `write_import` 模板已写死 `compress/mode=2` + `process/size_limit=2048` → 今后工作室传图自动压缩。
  2. **本地/LocalSend/拖拽路径**：新增 `tools/compress_textures.py`（扫 `assets/`+`resources/` 所有 mode=0 纹理→改压缩+限速+删对应 .ctex 强制重导；支持 `--dry-run`；跳过 `_backup` 目录）。今后任何非工作室导入的图，跑一次脚本即全量兜底。
  3. **重导铁律**：手改 `.import` 参数**不会**触发 Godot 重导（仅源 md5 变化或缺 .ctex 才重导）；必须 `godot --headless --editor --quit --path` 全量重导生成 .ctex，否则仍按旧缓存加载。
  4. **项目级默认预设方案已证伪**：`.godot/imports/texture.import` 在 Godot 4.7 不识别（新图仍 mode=0），已删；可靠兜底就是上面 1+2。
- **补充修复（同会话 follow-up，已修）**：无头复现末尾曾出现 4 条 `Lambda capture at index 0 was freed` ERROR，定位在 `ui_manager.close_screen` 销毁模式 lambda 直接捕获 `target` 节点，转场期 target 已被 `close_all_screens` 等路径先释放→Godot 捕获失效刷屏（不卡死不崩溃，纯告警，与卡死无关）。改用 `target.get_instance_id()`（int）捕获、回调内 `instance_from_id` 解析再判空，告警 4→0；GATE1 零错误。`transition_manager.gd` 两条 lambda 未触发，还原即可。
- **工作室服务**：按用户"以后重启不用过问"要求，已重启 studio_server（127.0.0.1:8765）并验证 `/api/tool_version` + `/api/main_menu/assets` 正常。

## 全量代码审计（2026-09-02 深夜，用户要求牛角尖式摸盘）
- **范围**：201 .gd + 49 .tscn / 约 35k 行；骨架精读 + 4 路 Explore 并行深挖（services/gameplay/ui/架构工具）+ 双闸门实测。
- **健康度**：GATE1 零错误 ✅；GATE2 **45 套件/0 失败/0✗** ✅。非屎山（架构 7.5/10 B+），问题是局部逻辑 Bug + 测试断档。
- **最高危（已亲自核实）**：
  - **BUG-01 [Critical] UIManager 缓存屏重开未回写屏幕栈**（`ui_manager.gd:169-183` 复用分支漏 `_screen_stack.append/_screen_layer/_current_screen`，而 `close_screen:241-247` 已 erase）→ 重开缓存屏（存档/设置）`is_any_screen_open()` 误报 false、`BaseScreen.gd:112` 守卫 `get_current_screen()!=self` 成立 → 键盘上下/确认/取消全失效。
  - **BUG-04 [High] 商店售卖锁定物经济漏洞**（`shop_service.gd:109,114`）：预检 `get_item_count` 含锁定物，但 `remove_item_by_id` 跳过锁定且返回值忽略，`add_money` 按全量 → 卖 5 个(2 锁定)只扣 3 个却付 5 个钱，白赚银两。
  - **BUG-03 [High] 关系状态双写不同步**（`bond_service.gd:20-22,238,274,296` 自带 sworn/apprentices/masters 字典 vs `relationship_service.gd:43,50` 只读 GameManager 专用服务）→ 走 BondService.swear() 专用服务不知情且写入存档，脏档。
- **其他 High**：BUG-02 open_screen 不防重开（实例叠加+信号双监听）；BUG-05 契约总表过期需重跑 gen_contract.gd；BUG-06 WeddingScene 绕过 return_to_town 未回收资源。
- **测试缺口**：quest/ability/alchemy/forge/sect/shop/game_state 无单测（quest 业务断档最严重）。
- **工程卫生**：根目录 18+ 垃圾文件（_*.log/_*.html/_*.txt/_diag_*.gd.uid）；tools 脚本硬编码绝对路径；town.json 死数据。
- **派单核对**：fed3f00da584(战斗→town掉血) 已修复；acf2246fd5f2(聘礼跳过锁定) 已修复；178267684159(休息未推进天数) 部分成立——根因是 `_do_rest` 在 `is_any_screen_open()` 静默 return，结缘/背包界面内无法休息。
- **交付**：`docs/代码审计报告_2026-09-02.md`（含 P0/P1/P2 清理路线图）。后续逐一清理，每修一项重跑 GATE1+GATE2。

## P0 清理执行 + GATE2 回归修复（续，本段）
- **落地 P0 四项**：BUG-01/02（ui_manager.gd 缓存屏补栈 + 同名防重）、BUG-04（shop_service 锁定物不可售）、BUG-03（bond_service 删重复关系字典，全走专用服务）。其中 ui_manager.gd 此前误判为已提交，实际本次才 `13fd9db` 入库。
- **GATE2 回归根因**：P0 提交后 GATE2 由 45/0 退化为「通过 44·失败 1」（`test_popup_lifecycle::test_sub_screen_open_and_close_via_bus`）。
  真因**非 P0 代码缺陷**，而是潜伏测试框架缺陷：`tests/unit/_test_base.gd` 的 `run()` 只调 `before_each`、**从不调 `after_each`**，6 个套件（`test_popup_lifecycle`/`test_combat_entity_pool`/`test_inventory_service`/`test_phase2_pilot`/`test_portrait_cache`/`test_resource_manager`）的清理钩子全失效。
  叠加 BUG-01 缓存屏重开补栈 → 缓存 SettingsScreen 残留 `_screen_stack` 跨 6 个测试累积 → 关闭 ForgeScreen 后仍残留陈旧条目 → `is_any_screen_open()` 误报 true。
- **修复**：`_test_base.gd` 增 `if has_method("after_each"): call("after_each")`（has_method 守卫，不影响无该钩子套件）。commit `874e530`。
- **验证（决定性）**：**GATE1 rc=0（零 SCRIPT/PARSE/COMPILE ERROR）；GATE2 套件：通过 45 · 失败 0 · ✗=0**，零回归。审计记录追加到 `docs/代码审计报告_2026-09-02.md` 第七章。
- **教训（写入契约）**：双闸门门禁判绿须同时满足 `grep -c "✗"==0` 且 `套件：失败 0`；且凡改 `TestBase`/测试钩子必须全量重跑 GATE2，避免「框架钩子失效」类潜伏 bug 被掩盖。

## P1 清理（commit 5614633，本会话）
- **范围**：在 P0 会话（5257e0f/874e530/13fd9db）已修 BUG-01/02/03/04 + GATE2 回归的基础上，补清理剩余 P1。
- **BUG-05 契约总表过期**：重跑 `tools/gen_contract.gd`（`--headless --script res://tools/gen_contract.gd`）重生成 `docs/契约总表.md`，时间戳刷新到本会话。
- **BUG-06 WeddingScene 绕过回城钩子**：`scenes/gameplay/bond/WeddingScene.gd:_on_done` 由 `change_scene_to_file(SCENE_TOWN)` 改为 `GameManager.return_to_town()`（内含 `ResourceManager.reclaim_all()` 回收跨场景资源）。
- **BUG-07 sect 缺道具误报声望**：`sect_service.gd` 缺入门道具从 `FAIL_REPUTATION_TOO_LOW` 改为新增专属枚举 `SectEnums.JoinResult.FAIL_MISSING_ITEM`（`core/enums/sect_enums.gd` 末尾追加）；emit 字符串本就是 "MISSING_ITEM" 无需改。
- **BUG-08 forge count<=0 误报配方**：`forge_service.gd` 从 `FAIL_UNKNOWN_RECIPE` 改为新增专属枚举 `ForgeEnums.ForgeResult.FAIL_INVALID_COUNT`（`core/enums/forge_enums.gd` 末尾追加）。
  - 注：审计报告原建议的 `FAIL_MISSING_ITEM`/`FAIL_INVALID_COUNT` **原本不存在**，不能直接用，已在各自枚举文件追加后才引用（加法式，不破坏现有 switch）。
- **BUG-10 can_propose 聘礼校验**：`romance_service.gd` 抽 `_dowry_satisfied(npc_id)`（读 `dowry_required` 统计 need、按 `get_unlocked_count` 校验），`can_propose` 与 `propose` 共用单一真源；缺聘礼时求婚按钮禁用而非点击才拒。action 层白结婚已在派单 acf2246fd5f2 堵死。
- **BUG-11 TownScene 玩家归位**：`GameManager` 增 `town_player_spawn_pos: Vector2`（autoload 跨场景存活、**不进存档**）；`TownScene._exit_tree` 离镇前记录 `_player.position`，`_build_world` 据其归位，修掉每次战斗/回城传送到 (0,0)。首进城镇（无记录）仍为 (0,0)，无回归。
- **附带**：`.gitignore` 加 `_backup*/` 高清原图备份与 `/_*.html` 临时产物忽略；顺手删 `ui_manager.gd` 内 P0 会话遗留的 `[DEBUG] OPEN` 打印。
- **验证**：GATE1 rc=0 零错误；GATE2 套件通过 45·失败 0·✗=0（终验含并发提交整树）。
- **未提交（非本次范围，留给用户/相关会话定夺）**：`data/configs/localization/*`、`data/configs/ui/*.json`、`data/configs/battles/grids/*`、`data/configs/bond/celebrations.json`、`tactical_demo_master.png`、`scenes/ui/**` 若干、`tests/unit/_test_base.gd`（已被 P0 会话改）、`tools/desktop_studio/tscn_assets.py`、`tools/_gen_matte_frames.py`、`docs/代码审计报告_2026-09-02.md`(未跟踪)、各类 `dist/`/`project_root.txt`/`启动脚本.bat`。按铁律精确 `git add`，未用 `-A`，这些改动保持原状。

## 并发协同提醒
- 本会话（P1）与 P0 清理会话存在**并行提交**（5257e0f/874e530/13fd9db 由他方提交，5614633 由本会话）。两者改动文件不冲突，GATE1/GATE2 终验均绿。
- 但工作树仍有**他方会话遗留的未提交改动**（见上），提交前务必 `git status` + 精确 add，避免误带无关文件。建议用 `tools/handoff.py` 隐患传递板同步双方进度。

## P2 清理（2026-09-02 续，用户"推进"后）
- **核实**：用户说"推进"时，P0+P1 实际已全部提交（5614633 收口 P1）。审计报告生成基于更早快照，BUG-06/07/08/10/11 代码已带"修复"注释、行号过期，故初版误列为待修——已读码复核，全部已落地（第八章记录）。
- **P2-A shop 回归单测**（commit `bfa093c`）：新增 `tests/unit/test_shop_service.gd`（5 项），守住 BUG-04 锁定物经济漏洞（正常售卖/超未锁定量整体拒绝/部分锁定只扣未锁/非法数量/未知商店）。**GATE2 由 45→46 套件，仍 0 失败**。
- **P2-B/C 工程卫生**（commit `e74ebd2`）：删根目录垃圾（`_chk_town.log`/`_wt.log`/`_gamelog*.txt`/`_gen_splash.py`）+ zombie `.uid`（`_diag_matte.gd.uid`/`_diag_png.gd.uid`/`core/utils/object_pool.gd.uid`/`core/utils/pool_manager.gd.uid`，对应 .gd 已删/迁 services/）+ 死诊断 `tests/unit/_diag.gd`（无 test_ 前缀非 TestBase，run_all 永不执行）。全工程 orphaned .uid 扫描仅此 4 个，已全清；根 `_*` 垃圾已空。
- **验证**：GATE1 rc=0；GATE2 **46 套件 / 0 失败 / 0✗** 零回归。审计报告第九章记 P2 进度，并列出剩余项（#10 其余服务单测 / #12 BUG-13 工具路径相对化 / #13 BUG-14 town.json 死数据 / #14 健壮性小修）。
- **commit 序列本段**：bfa093c[测试]、e74ebd2[清理]、cd930d5[审计]。

## 继续游戏卡死修复（commit 65b6ac6）
- **现象**：用户反馈每次点击主菜单「继续游戏」后窗口冻结（截图停在继续游戏按钮）。headless 下 `GameManager.load_game(1)` 与 TownScene 单独加载均不卡，说明不是启动 freeze 或纹理压缩问题复发。
- **根因**：`MainMenu._continue_game()` → `UIManager.close_all_screens()` → `GameManager.load_game()` → `get_tree().change_scene_to_file()` 在同一帧内执行。`close_all_screens()` 批量 `queue_free()` 使场景树处于 "busy adding/removing children" 状态，此时 `change_scene_to_file` 触发 Godot 内部死锁/报错，GUI 下表现为窗口无响应冻结。`tests/unit/run_all` 与 headless 因无真实场景树 busy 竞争而不卡，故长期被掩盖。
- **修复**：`GameManager.gd` 新增 `_deferred_change_scene(path)`，用 `await get_tree().process_frame` 把 `change_scene_to_file` 推迟到下一帧；统一替换 `load_game`/`start_new_game`/`start_battle`/`start_test_*`/`return_to_town`/`return_to_title`/`return_to_safe_point`/`goto_region`/`_on_bond_wedding_started` 共 10 处切场景入口。
- **验证**：
  - GATE1 零 SCRIPT/PARSE/COMPILE ERROR。
  - GATE2：46 套件 / 0 失败 / 0✗。
  - 临时场景在 `_ready` 中调用 `load_game` 不再出现 `Parent node is busy adding/removing children` 报错。
- **commit**：65b6ac6 `[GameManager] 延迟一帧切换场景，修复 UI 关闭后 change_scene_to_file 触发的内部死锁/卡死`。

## 工作树遗留未提交改动清理（2026-09-02）
- **触发**：用户要求「刷新状态，处理遗留未提交改动」。此前并发会话留下大量 modified/untracked 文件。
- **处理原则**：精确 `git add`，禁 `-A`；按模块/主权分批提交；构建产物进 `.gitignore`；二进制 translation 无意义改动还原。
- **已提交**：
  - `65b6ac6` GameManager 继续游戏卡死修复。
  - `5a30920` memory 日志（08-31/09-01/09-02）。
  - `6b41669` 审计报告补充继续游戏修复记录。
  - `bb42fbc` tools：`tscn_assets.py` 默认压缩纹理（工作室导入双保险）、`_gen_matte_frames.py` idle 降速。
  - `682b518` UI：主菜单/加载界面布局 JSON + `MainMenu.tscn` + `top_right_menu_panel.gd`。
  - `f77c570` data：`celebrations.json`/`preset_*.json`/`tactical_demo_master.png`。
  - `eb9fd1e` studio：`project_root.txt`/`启动指定工程.bat`。
  - `d85634c` `.gitignore`：忽略 `dist/` 与 `tools/desktop_studio/dist/` 构建产物目录。
- **还原**：`data/configs/localization/strings.*.translation` 二进制自动重生成变化（strings.csv 未变），还原避免噪音。
- **终态**：`git status --short` 为空；GATE1 0 错误；GATE2 46/0/0✗。
