# 2026-09-02 工作日志

## P2 全量收尾（用户"一起干"，本段最后一块）
- **范围**：审计报告第六章剩余深层项 #10（其余服务单测）/ #12+BUG-13（工具路径相对化）/ #13+BUG-14（town.json 死数据核查）/ #14（健壮性小修 BUG-12/19/20/21/22/23/24/25/26）。4 笔提交：`24069f5`(GameManager _deferred_change_scene 改 change_scene_to_file 修主菜单 freeze) / `6da20ec`(健壮性小修) / `a44ddc4`(7 个服务单测) / `5d941d9`(工具路径相对化)。
- **落地**：BUG-09(inventory 堆叠路径加负重上限) / BUG-12(BattleScene `_aborted`+`_battle_dead()` await 守卫) / BUG-13(`_gen_matte_175.py` discover_project_root + ffmpeg 优先 PATH/环境变量) / BUG-19(ability use_combat_skill 标 @deprecated) / BUG-21(romance `_special_portrait_cache` 静缓存，删 get_quanquan 冗余写) / BUG-22(quest+combat 缺字段 `String(obj.get("id",""))` 降级) / BUG-23(TownScene 删死监听 player_hp_changed) / BUG-25(LoadingScreen+SettingsScreen `_exit_tree` 显式断信号) / BUG-26(InventoryScreen `if slot==null: continue`) / 7 新单测(test_item_flags/quest/ability/alchemy/forge/sect/game_state 守 BUG-15/27)。
- **BUG-14 误删恢复（铁律级教训）**：初判 town.json 为死数据（区域走 regions.json，无代码 load）删之 → GATE2 退化 3 失败（tactical_mapbound_002 的 grid.width 为 Nil）。根因：town.json 是 town 战术底图几何配置，经地图绑定**间接**引用，非直接 load。→ `git checkout HEAD -- data/configs/maps/town.json` 恢复，判非死数据，保留。**审计"无代码读取"判定不可仅凭直接 load 字符串 grep，须追数据绑定链。**
- **BUG-20 跳过**：combat_core:470-471 的 `plan` 赋值在循环**外**，非冗余，语义正确，不修。
- **BUG-16/17/18 仅登记未硬改**：BUG-16 跨模块读取型 DI 留待契约总表规范；BUG-17 inventory buff/cure TODO 登记为已知未完成项；BUG-18 combat ally 快照死代码，启用 ally 前不动。
- **过程陷阱（已闭环）**：① Edit 在 `mini(put,room)` 后混入非法反引号 → GATE1 Parse Error(242 行)，grep `\x60` 定位删除；② town.json 误删（上）；③ test_sect_service 误用 `expect_eq` 比 String（基类仅 int）→ 改 `expect(...=="...")`；④ BattleScene `_on_auto_pressed` while 内 if 分支缩进错乱 → 重写。
- **验证（决定性）**：GATE1 rc=0 零错误；GATE2 **53 套件/0 失败/0✗**。tools/ 清 6 个临时文件。审计报告补第十一章；工作树无未提交改动（4 提交已落）。
- **终态**：P0+P1+P2 全绿，路线图中 #10/#12/#13/#14 闭环，工程进入"全绿待发布"。仅 BUG-16 架构规范 + BUG-17 背包 Phase 扩展两项非阻塞待办。

## 主菜单按钮：悬停选择音效（2026-09-02 凌晨）
- **需求**：给主菜单 5 个按钮（开始/继续/设置/额外/退出）增加「鼠标悬停时播放选择音效」。
- **关键发现（根因）**：
  1. 代码早已存在悬停音效链路：`WuxiaMenuButton.gd` 的 `_on_mouse_entered` → `_play_hover_sfx()` → `AudioManager.play_ui_sfx("hover")`。
  2. 但 `resources/audio/sfx/` 目录**完全是空的**——`ui_sfx.json` 里注册的 8 个音效（hover/click/confirm/cancel/back/open/close/error）全部缺失；`AudioManager.play_ui_sfx` 在 `ResourceLoader.exists(path)` 失败时空静默跳过 → 所有 UI 音效一直无声。
- **修复**：
  1. 新增 `tools/gen_ui_sfx.py`（纯标准库合成 8 个短促占位音效，16bit/44100/mono，45~200ms），生成到 `resources/audio/sfx/`。占位音；正式音频替换同名文件即可，配置表不用动。
  2. **⚠️ 此条枚举写反，已更正（详见文末「主菜单按钮 mouse_filter 回归复修」段）**：原记"把 4 个子节点 mouse_filter 改为 0(IGNORE)"是**把 Godot 枚举写反**——`MOUSE_FILTER_STOP=0`、`MOUSE_FILTER_IGNORE=2`，**`0` 实为 STOP=拦截**，并非 IGNORE。该次改动让 `bg_panel/icon/label_main/label_sub` 变成 STOP，其中 `label_main` 盖在按钮中心、把按钮本体的 `mouse_entered`/`button_up` 全吞掉 → 正是后来"登录界面自动悬停继续游戏、鼠标移不动、五大功能点不了、无报错"的真凶。装饰子节点要点击穿透，**必须写 `2`(IGNORE)**，绝不可写 `0`。
- **验证**：提交 `f6a52ef`（10 files：8 wav + tscn + 生成脚本），GATE1 通过（`RC=0` 零解析/编译/加载错误）。纯游戏侧改动，无工作室工具/端口动作。
- **备注**：用户是电脑小白，无法自产音频；合成占位音是项目「占位期文件未到位就静默跳过」设计的合理落地。正式音效让用户替换 `resources/audio/sfx/` 下同名 wav 即可。

## 登录界面卡死 BUG 修复（2026-09-02，重大修正）
- **现象**：进入游戏登录界面（主菜单）直接卡死/无响应，反复多次（用户连报"又双叒叕卡死了"）。
- **⚠️ 根因更正（关键）**：首轮误判为"190MB 未压缩纹理触发显卡 TDR 超时"。**此诊断错误**——用户实测显卡是 **NVIDIA RTX 3070 Ti（8GB 独显）**，且日志显示引擎在跑（帧 1→138 递增、Bootstrap 已就绪），独显不可能因 190MB 显存 TDR。
- **真正根因（已定位+修复）**：`scenes/ui/components/ui_background/UIBackground.gd` 的 `_sample_edge_stops()` 用 `tex.get_image().get_pixel()` 采样背景图边缘色做渐变垫底。把纹理改压缩导入（mode=2）后，`CompressedTexture2D.get_image()` 返回压缩数据，对其 `get_pixel()` 疯狂报错 `Can't get_pixel() on compressed image, sorry.` **9024 条 ERROR 刷屏拖死主线程 = 用户看到的"卡死"**。这是"修压缩反而暴露 latent bug"的典型。
- **修复**：`_sample_edge_stops()` 改用 `Image.load_png_from_buffer(buf)` 直接解码源 PNG 字节取像素（跨 4.x 稳定实例方法；`Image.load_from_file` 在 4.7.2 已非实例方法，会 Parse Error）。与导入压缩解耦：既保留压缩纹理小显存/快加载，又能正常取像素。非 PNG/源缺失则静默回退硬编码绿渐变。
- **验证（决定性）**：无头复现 `godot --headless --path` 至主菜单，`get_pixel` 错误 **9024→0**，日志 **99332→61 行**，能正常进主菜单不卡。GATE1 零 SCRIPT/PARSE/COMPILE ERROR。
- **全量压缩（双保险第一层已落地）**：除登录 9 张外，工程里还躺着 **162 张未压缩纹理**（matte_* 124 张、half_body、battle_bg、resources/ 等）。本轮全量治理：145 个 `.import` mode=0→2、2 张超大源图缩放备份、3 个 resources/ 纹理压缩、修复 `preset_12x12.jpg`（实为 2048×1152 PNG 被错命名 .jpg，Godot 无法导入→改名 .png 并改 JSON 引用）。最终 **157 个纹理全部压缩**（`.s3tc.ctex`），删除 19 个孤儿未压缩 .ctex，无残留未压缩引用。
- **双保险机制（用户硬性要求"自动压缩+双保险"）**：
  1. **工作室路径**：`tools/desktop_studio/tscn_assets.py` 的 `write_import` 模板已写死 `compress/mode=2` + `process/size_limit=2048` → 今后工作室传图自动压缩。
  2. **本地/LocalSend/拖拽路径**：新增 `tools/compress_textures.py`（扫 `assets/`+`resources/` 所有 mode=0 纹理→改压缩+限速+删对应 .ctex 强制重导；支持 `--dry-run`；跳过 `_backup` 目录）。今后任何非工作室导入的图，跑一次脚本即全量兜底。
  3. **重导铁律**：手改 `.import` 参数**不会**触发 Godot 重导（仅源 md5 变化或缺 .ctex 才重导）；必须 `godot --headless --editor --quit --path` 全量重导生成 .ctex，否则仍按旧缓存加载。
  4. **项目级默认预设方案已证伪**：`.godot/imports/texture.import` 在 Godot 4.7 不识别（新图仍 mode=0），已删；可靠兜底就是上面 1+2。
- **补充修复（同会话 follow-up，已修）**：无头复现末尾曾出现 4 条 `Lambda capture at index 0 was freed` ERROR，定位在 `ui_manager.close_screen` 销毁模式 lambda 直接捕获 `target` 节点，转场期 target 已被 `close_all_screens` 等路径先释放→Godot 捕获失效刷屏（不卡死不崩溃，纯告警，与卡死无关）。改用 `target.get_instance_id()`（int）捕获、回调内 `instance_from_id` 解析再判空，告警 4→0；GATE1 零错误。`transition_manager.gd` 两条 lambda 未触发，还原即可。
- **工作室服务**：按用户"以后重启不用过问"要求，已重启 studio_server（127.0.0.1:8765）并验证 `/api/tool_version` + `/api/main_menu/assets` 正常。

## 全量代码审计（2026-09-02 深夜，用户要求牛角尖式摸盘）
- **范围**：201 .gd + 49 .tscn / 约 35k 行；骨架精读 + 4 路 Explore 并行深挖（services/gameplay/ui/架构工具）+ 双闸门实测。
- **健康度**：GATE1 零错误 ✅；GATE2 **45 套件/0 失败/0✗** ✅。非屎山（架构 7.5/10 B+），问题是局部逻辑 Bug + 测试断档。
- **最高危（已亲自核实）**：
  - **BUG-01 [Critical] UIManager 缓存屏重开未回写屏幕栈**（`ui_manager.gd:169-183` 复用分支漏 `_screen_stack.append/_screen_layer/_current_screen`，而 `close_screen:241-247` 已 erase）→ 重开缓存屏（存档/设置）`is_any_screen_open()` 误报 false、`BaseScreen.gd:112` 守卫 `get_current_screen()!=self` 成立 → 键盘上下/确认/取消全失效。
  - **BUG-04 [High] 商店售卖锁定物经济漏洞**（`shop_service.gd:109,114`）：预检 `get_item_count` 含锁定物，但 `remove_item_by_id` 跳过锁定且返回值忽略，`add_money` 按全量 → 卖 5 个(2 锁定)只扣 3 个却付 5 个钱，白赚银两。
  - **BUG-03 [High] 关系状态双写不同步**（`bond_service.gd:20-22,238,274,296` 自带 sworn/apprentices/masters 字典 vs `relationship_service.gd:43,50` 只读 GameManager 专用服务）→ 走 BondService.swear() 专用服务不知情且写入存档，脏档。
- **其他 High**：BUG-02 open_screen 不防重开（实例叠加+信号双监听）；BUG-05 契约总表过期需重跑 gen_contract.gd；BUG-06 WeddingScene 绕过 return_to_town 未回收资源。
- **测试缺口**：quest/ability/alchemy/forge/sect/shop/game_state 无单测（quest 业务断档最严重）。
- **工程卫生**：根目录 18+ 垃圾文件（_*.log/_*.html/_*.txt/_diag_*.gd.uid）；tools 脚本硬编码绝对路径；town.json 死数据。
- **派单核对**：fed3f00da584(战斗→town掉血) 已修复；acf2246fd5f2(聘礼跳过锁定) 已修复；178267684159(休息未推进天数) 部分成立——根因是 `_do_rest` 在 `is_any_screen_open()` 静默 return，结缘/背包界面内无法休息。
- **交付**：`docs/代码审计报告_2026-09-02.md`（含 P0/P1/P2 清理路线图）。后续逐一清理，每修一项重跑 GATE1+GATE2。

## P0 清理执行 + GATE2 回归修复（续，本段）
- **落地 P0 四项**：BUG-01/02（ui_manager.gd 缓存屏补栈 + 同名防重）、BUG-04（shop_service 锁定物不可售）、BUG-03（bond_service 删重复关系字典，全走专用服务）。其中 ui_manager.gd 此前误判为已提交，实际本次才 `13fd9db` 入库。
- **GATE2 回归根因**：P0 提交后 GATE2 由 45/0 退化为「通过 44·失败 1」（`test_popup_lifecycle::test_sub_screen_open_and_close_via_bus`）。
  真因**非 P0 代码缺陷**，而是潜伏测试框架缺陷：`tests/unit/_test_base.gd` 的 `run()` 只调 `before_each`、**从不调 `after_each`**，6 个套件（`test_popup_lifecycle`/`test_combat_entity_pool`/`test_inventory_service`/`test_phase2_pilot`/`test_portrait_cache`/`test_resource_manager`）的清理钩子全失效。
  叠加 BUG-01 缓存屏重开补栈 → 缓存 SettingsScreen 残留 `_screen_stack` 跨 6 个测试累积 → 关闭 ForgeScreen 后仍残留陈旧条目 → `is_any_screen_open()` 误报 true。
- **修复**：`_test_base.gd` 增 `if has_method("after_each"): call("after_each")`（has_method 守卫，不影响无该钩子套件）。commit `874e530`。
- **验证（决定性）**：**GATE1 rc=0（零 SCRIPT/PARSE/COMPILE ERROR）；GATE2 套件：通过 45 · 失败 0 · ✗=0**，零回归。审计记录追加到 `docs/代码审计报告_2026-09-02.md` 第七章。
- **教训（写入契约）**：双闸门门禁判绿须同时满足 `grep -c "✗"==0` 且 `套件：失败 0`；且凡改 `TestBase`/测试钩子必须全量重跑 GATE2，避免「框架钩子失效」类潜伏 bug 被掩盖。

## P1 清理（commit 5614633，本会话）
- **范围**：在 P0 会话（5257e0f/874e530/13fd9db）已修 BUG-01/02/03/04 + GATE2 回归的基础上，补清理剩余 P1。
- **BUG-05 契约总表过期**：重跑 `tools/gen_contract.gd`（`--headless --script res://tools/gen_contract.gd`）重生成 `docs/契约总表.md`，时间戳刷新到本会话。
- **BUG-06 WeddingScene 绕过回城钩子**：`scenes/gameplay/bond/WeddingScene.gd:_on_done` 由 `change_scene_to_file(SCENE_TOWN)` 改为 `GameManager.return_to_town()`（内含 `ResourceManager.reclaim_all()` 回收跨场景资源）。
- **BUG-07 sect 缺道具误报声望**：`sect_service.gd` 缺入门道具从 `FAIL_REPUTATION_TOO_LOW` 改为新增专属枚举 `SectEnums.JoinResult.FAIL_MISSING_ITEM`（`core/enums/sect_enums.gd` 末尾追加）；emit 字符串本就是 "MISSING_ITEM" 无需改。
- **BUG-08 forge count<=0 误报配方**：`forge_service.gd` 从 `FAIL_UNKNOWN_RECIPE` 改为新增专属枚举 `ForgeEnums.ForgeResult.FAIL_INVALID_COUNT`（`core/enums/forge_enums.gd` 末尾追加）。
  - 注：审计报告原建议的 `FAIL_MISSING_ITEM`/`FAIL_INVALID_COUNT` **原本不存在**，不能直接用，已在各自枚举文件追加后才引用（加法式，不破坏现有 switch）。
- **BUG-10 can_propose 聘礼校验**：`romance_service.gd` 抽 `_dowry_satisfied(npc_id)`（读 `dowry_required` 统计 need、按 `get_unlocked_count` 校验），`can_propose` 与 `propose` 共用单一真源；缺聘礼时求婚按钮禁用而非点击才拒。action 层白结婚已在派单 acf2246fd5f2 堵死。
- **BUG-11 TownScene 玩家归位**：`GameManager` 增 `town_player_spawn_pos: Vector2`（autoload 跨场景存活、**不进存档**）；`TownScene._exit_tree` 离镇前记录 `_player.position`，`_build_world` 据其归位，修掉每次战斗/回城传送到 (0,0)。首进城镇（无记录）仍为 (0,0)，无回归。
- **附带**：`.gitignore` 加 `_backup*/` 高清原图备份与 `/_*.html` 临时产物忽略；顺手删 `ui_manager.gd` 内 P0 会话遗留的 `[DEBUG] OPEN` 打印。
- **验证**：GATE1 rc=0 零错误；GATE2 套件通过 45·失败 0·✗=0（终验含并发提交整树）。
- **未提交（非本次范围，留给用户/相关会话定夺）**：`data/configs/localization/*`、`data/configs/ui/*.json`、`data/configs/battles/grids/*`、`data/configs/bond/celebrations.json`、`tactical_demo_master.png`、`scenes/ui/**` 若干、`tests/unit/_test_base.gd`（已被 P0 会话改）、`tools/desktop_studio/tscn_assets.py`、`tools/_gen_matte_frames.py`、`docs/代码审计报告_2026-09-02.md`(未跟踪)、各类 `dist/`/`project_root.txt`/`启动脚本.bat`。按铁律精确 `git add`，未用 `-A`，这些改动保持原状。

## 并发协同提醒
- 本会话（P1）与 P0 清理会话存在**并行提交**（5257e0f/874e530/13fd9db 由他方提交，5614633 由本会话）。两者改动文件不冲突，GATE1/GATE2 终验均绿。
- 但工作树仍有**他方会话遗留的未提交改动**（见上），提交前务必 `git status` + 精确 add，避免误带无关文件。建议用 `tools/handoff.py` 隐患传递板同步双方进度。

## P2 清理（2026-09-02 续，用户"推进"后）
- **核实**：用户说"推进"时，P0+P1 实际已全部提交（5614633 收口 P1）。审计报告生成基于更早快照，BUG-06/07/08/10/11 代码已带"修复"注释、行号过期，故初版误列为待修——已读码复核，全部已落地（第八章记录）。
- **P2-A shop 回归单测**（commit `bfa093c`）：新增 `tests/unit/test_shop_service.gd`（5 项），守住 BUG-04 锁定物经济漏洞（正常售卖/超未锁定量整体拒绝/部分锁定只扣未锁/非法数量/未知商店）。**GATE2 由 45→46 套件，仍 0 失败**。
- **P2-B/C 工程卫生**（commit `e74ebd2`）：删根目录垃圾（`_chk_town.log`/`_wt.log`/`_gamelog*.txt`/`_gen_splash.py`）+ zombie `.uid`（`_diag_matte.gd.uid`/`_diag_png.gd.uid`/`core/utils/object_pool.gd.uid`/`core/utils/pool_manager.gd.uid`，对应 .gd 已删/迁 services/）+ 死诊断 `tests/unit/_diag.gd`（无 test_ 前缀非 TestBase，run_all 永不执行）。全工程 orphaned .uid 扫描仅此 4 个，已全清；根 `_*` 垃圾已空。
- **验证**：GATE1 rc=0；GATE2 **46 套件 / 0 失败 / 0✗** 零回归。审计报告第九章记 P2 进度，并列出剩余项（#10 其余服务单测 / #12 BUG-13 工具路径相对化 / #13 BUG-14 town.json 死数据 / #14 健壮性小修）。
- **commit 序列本段**：bfa093c[测试]、e74ebd2[清理]、cd930d5[审计]。

## 继续游戏卡死修复（commit 65b6ac6）
- **现象**：用户反馈每次点击主菜单「继续游戏」后窗口冻结（截图停在继续游戏按钮）。headless 下 `GameManager.load_game(1)` 与 TownScene 单独加载均不卡，说明不是启动 freeze 或纹理压缩问题复发。
- **根因**：`MainMenu._continue_game()` → `UIManager.close_all_screens()` → `GameManager.load_game()` → `get_tree().change_scene_to_file()` 在同一帧内执行。`close_all_screens()` 批量 `queue_free()` 使场景树处于 "busy adding/removing children" 状态，此时 `change_scene_to_file` 触发 Godot 内部死锁/报错，GUI 下表现为窗口无响应冻结。`tests/unit/run_all` 与 headless 因无真实场景树 busy 竞争而不卡，故长期被掩盖。
- **修复**：`GameManager.gd` 新增 `_deferred_change_scene(path)`，用 `await get_tree().process_frame` 把 `change_scene_to_file` 推迟到下一帧；统一替换 `load_game`/`start_new_game`/`start_battle`/`start_test_*`/`return_to_town`/`return_to_title`/`return_to_safe_point`/`goto_region`/`_on_bond_wedding_started` 共 10 处切场景入口。
- **验证**：
  - GATE1 零 SCRIPT/PARSE/COMPILE ERROR。
  - GATE2：46 套件 / 0 失败 / 0✗。
  - 临时场景在 `_ready` 中调用 `load_game` 不再出现 `Parent node is busy adding/removing children` 报错。
- **commit**：65b6ac6 `[GameManager] 延迟一帧切换场景，修复 UI 关闭后 change_scene_to_file 触发的内部死锁/卡死`。
- **⚠️ 该提交实际带回归（见文末「继续游戏卡死【回归复修】」段）**：`65b6ac6` 的 `_deferred_change_scene` 延迟体尾部写成了**递归调用自身同名函数** `_deferred_change_scene(path)`，而非真正执行 `get_tree().change_scene_to_file(path)`。后果：`change_scene_to_file` 永不运行 → 所有切场景入口（继续/新游戏/战斗/回城等 10 处）全部失效，表现为「点继续游戏卡在界面、HUD 不挂载、五大功能点不了、动不了」——正是用户截图现象，也是此前「自动跳过/忽视 BUG 不闪退」那次改动的真身。headless/单测因无真实场景树 busy 竞争而不暴露，长期被掩盖。已由 `24069f5` 复修。

## 工作树遗留未提交改动清理（2026-09-02）
- **触发**：用户要求「刷新状态，处理遗留未提交改动」。此前并发会话留下大量 modified/untracked 文件。
- **处理原则**：精确 `git add`，禁 `-A`；按模块/主权分批提交；构建产物进 `.gitignore`；二进制 translation 无意义改动还原。
- **已提交**：
  - `65b6ac6` GameManager 继续游戏卡死修复。
  - `5a30920` memory 日志（08-31/09-01/09-02）。
  - `6b41669` 审计报告补充继续游戏修复记录。
  - `bb42fbc` tools：`tscn_assets.py` 默认压缩纹理（工作室导入双保险）、`_gen_matte_frames.py` idle 降速。
  - `682b518` UI：主菜单/加载界面布局 JSON + `MainMenu.tscn` + `top_right_menu_panel.gd`。
  - `f77c570` data：`celebrations.json`/`preset_*.json`/`tactical_demo_master.png`。
  - `eb9fd1e` studio：`project_root.txt`/`启动指定工程.bat`。
  - `d85634c` `.gitignore`：忽略 `dist/` 与 `tools/desktop_studio/dist/` 构建产物目录。
- **还原**：`data/configs/localization/strings.*.translation` 二进制自动重生成变化（strings.csv 未变），还原避免噪音。
- **终态**：`git status --short` 为空；GATE1 0 错误；GATE2 46/0/0✗。

## 多 AI 协同变更纪律建立（用户硬性要求，本段）
- **痛点**：多 AI 并行，已出现"某 AI 改了个小问题，另一个 AI 修了半天"——根因是看不到别人改了什么。用户要求：所有修改必须记录；出 BUG 先看之前改了什么；建详细不出问题机制（架构/PM/程序/前端/后端/运维视角 + 自查/自审核/追溯/全留痕）；回答"没 BUG 是否上 git"。
- **交付**：
  1. `tools/change_log.py`（纯标准库）：`add` 登记一行 / `notice` 写变更通告并登记 / `query --module|--keyword|--days` 查近期改动 / `backfill` 从 git 历史回填。已从 git 全量回填 **191 行**到 `docs/更改日志.md`（可机器 grep，调试前先查）。
  2. `docs/多AI协同机制_SOP.md`：五道防线 + 角色 RACI + 留痕/自查(GATE1+GATE2)/自审核/追溯四步/push 节奏（无 BUG 必 commit、push 由 PM 统一）/红线 + 新窗口接入清单。
  3. `docs/变更通告_模板.md`：变更通告标准结构（变更项/原因/影响面/回滚/协同方需知/验证）。
  4. skill `change-tracking`（user 级）：把三条铁律（改前登记、调前先查、提交/push 节奏）做成可加载纪律，任何 AI 改/提交/修 BUG 自动遵守。
  5. `MEMORY.md`「多 AI 协同」节扩写为变更纪律铁律。
- **关键结论写入 SOP**：本地 commit 必交（留痕防丢失可回滚）；远端 push 由集成窗口整树双闸门全绿后统一推，单分支禁各窗盲目 push 互覆盖。调试 BUG 铁律：先 `change_log.py query`+`git log`+`handoff dashboard` 确认非回归，再深入。
- **待办（机制落地后）**：各窗口启动设 `git config user.name "AI-<窗>"` 署名，使 `git log`/changelog author = 责任窗口，禁匿名提交，方能真追溯。

## 继续游戏卡死【回归复修】——递归自转假修复（本会话收口）
- **复现**：用户反馈「进入游戏还是卡在继续游戏上面、五大功能点不了、动不了，但静音/灯按钮有效」（静音灯在 HUD 层=50，低于 TRANSITION 遮罩=100，故遮罩说被排除）。非真实 freeze，是场景根本没切过去。
- **真根因**：上一轮 `65b6ac6` 的「修复」把 `_deferred_change_scene` 延迟体写成递归调自己：
  ```gdscript
  func _deferred_change_scene(path: String) -> void:
      await get_tree().process_frame
      if is_instance_valid(get_tree()):
          _deferred_change_scene(path)   # ← BUG：递归自转，change_scene_to_file 永不执行
  ```
  `await` 推到下一帧后，本该执行 `get_tree().change_scene_to_file(path)`，却调回自身 → 无限递归(每帧一次)且场景永远不切换。所有 10 处切场景入口统一走它 → 全废。
- **真修复**：尾行改为真正执行体 `get_tree().change_scene_to_file(path)`（与首轮设计意图一致）。已由并行会话 `24069f5`（`[GameManager] P1 收尾：_deferred_change_scene 改 change_scene_to_file 修复主菜单 freeze 死锁`）入库，本会话本地提交因与 `24069f5` 内容相同而 no-op，工作树干净。
- **headless 流程复现验证（决定性）**：挂载诊断节点于 root（call_deferred 存活过 change_scene）跑 load_game 流程 → 修复后 `current_scene = TownScene`、`TransitionManager._is_transitioning = false`、`UIManager HUD 层子节点 = Hud(Control)`（HUD 已挂载）、`is_any_screen_open() = false`（可移动）。五大功能/移动恢复。
- **双闸门（终验）**：GATE1 零 SCRIPT/PARSE/COMPILE ERROR；GATE2 **53 套件 / 0 失败 / 0✗**（测试数 46→53 因 `a44ddc4` 新增 7 服务单测，零回归）。
- **铁律级教训（写入契约）**：`await` 延迟执行后，**延迟体尾部必须是真正要干的那一行，绝不能再调用自身同名函数**（递归自转会静默吞掉功能、且不崩溃不报错，比崩溃更难查）。凡用 `await get_tree().process_frame` 等延迟模式做「下一帧真正执行 X」，必须肉眼核对尾部是 X 而非函数自身。
- **用户线索命中**：用户那句「我记得之前有个强制闪退 BUG，你当时改的是自动跳过/忽视 BUG 不闪退？当时就是修笔触背景」正是此因——那次为了不闪退，把崩溃的 `change_scene_to_file` 换成「延迟一帧」，却误写成递归自转，等于「不闪退但也不切场景」。

## 多 AI 协同 SOP 差异报告（用户贴大厂 SCM 后评估）
- 用户贴"武侠CRPG 多AI协同 风险预防&安全隔离机制(大厂标准)"，要求评估相对现有 `docs/多AI协同机制_SOP.md` 的差缺与过度项。
- 结论：90% 已覆盖。需补 = P0 零成本文档条款(角色禁止操作列化 / 模块内部私有明写 / 同tscn单窗口handoff锁 / 严重阻塞先回滚首响 / commit带#bugID) + P1 机制(轻量 feature_flags.json / 里程碑git tag+资源备份 / 大改动合并评审)。裁剪 = 飞书台账(不可grep)、硬分支(1-3AI开销大)、网游SCM外壳。
- 用户定位：真实新手，设计/架构/维护全靠AI → 报告"怎么整合"由AI给建议、用户拍板。用户选"先不整合，只出差异报告"。
- 交付：`docs/多AI协同_SOP差异报告.md`（未并入SOP、未建功能开关，待拍板）。已 changelog 登记 + 提交。

## 多 AI 协同 小白版说明（用户要"看得懂的"）
- 用户要一份新手能看懂的评估版。产出 `docs/多AI协同_小白版说明.md`：人话讲怕什么(小错大祸/找不到人/互相覆盖) → 90%已有 → 该加(A五项零成本文档条款/B三项机制) → 不该加(飞书台账/硬分支/网游外壳) → 建议。与差异报告配套。

## 主菜单按钮 mouse_filter 回归复修（最终根因 · 2026-09-02 收口）
- **现象（用户 F5 实测）**：登录/主菜单界面一加载就自动悬停在「继续游戏」那一行，鼠标移不到别项；但右下角设置/语言等普通按钮照常可点；全程**零报错**。此前已修的递归自转 bug（`24069f5`）是真实但不同的 bug，修完用户仍报"还是没修好" → 说明真凶另有所在。
- **真根因**：`scenes/ui/components/wuxia_menu_button/WuxiaMenuButton.tscn` 的 4 个子节点（`bg_panel`/`icon`/`label_main`/`label_sub`）`mouse_filter = 0`。**Godot 枚举 `MOUSE_FILTER_STOP=0`/`MOUSE_FILTER_IGNORE=2`，`0` 是 STOP=拦截**（与直觉相反）。早前 f6a52ef 想做"点击穿透"却错写成 `0`，等于让 `label_main`(STOP) 盖在按钮中心、把按钮本体的 `mouse_entered`/`button_up` 全吞掉 → 自动悬停项无法移开、点击永远不触发 `_continue_game`；而右下角普通 Button 没有拦截型子节点，故照常可点；纯事件捕获、非崩溃 → 无任何报错。
- **诊断法（关键，headless 抓不到）**：逻辑单测/headless 只跑脚本不跑真实鼠标拾取，故此类静默拦截长期被掩盖。自建"无头鼠标拾取模拟器"复刻 Godot `gui_find_control_at_pos` 语义——递归比对每个 MenuItem 中心点落在的 topmost `MOUSE_FILTER_STOP` 控件。结果：5 个按钮中心全部命中自己的 `label_main`(mf=0/BLOCKED)。换成"只返回 STOP 控件"的判断后才暴露真凶（v1 误把 PASS/IGNORE 子节点当 blocker，误报 ContentRoot）。
- **修复**：4 个子节点 `mouse_filter = 0` → `2`（`MOUSE_FILTER_IGNORE`=点击穿透）。用 Edit `replace_all` 一次性改 4 处。
- **验证（决定性）**：复跑拾取模拟器 → 5 个按钮中心全部命中自身(OK(self))；GATE1 零 SCRIPT/PARSE/COMPILE ERROR；GATE2 **53 套件 / 0 失败 / 0✗**。临时探针文件已全部删除。
- **铁律级教训（已写入 MEMORY.md·GDScript 硬规）**：Godot `mouse_filter` 写原始数字极易写反，**STOP=0 不是"忽略"**。"点击穿透"必须写 `2` 或 `Control.MOUSE_FILTER_IGNORE`；凡见 `.tscn` 里 `mouse_filter = 0` 出现在装饰性/子节点上，先怀疑它是不是本意 IGNORE 却写成了 STOP。此类"无报错但点不了"只用鼠标拾取模拟器查，逻辑测试无效。
- **对历史记录纠偏**：本文件「悬停选择音效」段第 2 条原写"改为 0(IGNORE)"系枚举写反，**已在该条加 ⚠️ 更正注**。真因是 f6a52ef 把子节点写成 0(=STOP) 引入拦截；当前正确值是 2(=IGNORE)。

## 跨模块 BUG 修复机制（用户批评封装查不出关联BUG）
- 用户痛点：一个 UI 小 BUG 修了好久；质疑"模块关上门"机制，理由是它暴露不出跨模块关联BUG。已认账：封装降内部耦合，但查不出"UI↔服务接缝BUG"(信号没发/没人订阅/数据没刷新/契约对不上)。handoff open UI BUG 全是接缝类(notify_quest_track_changed/inventory_add_overflow/player_hp_changed)佐证。
- 交付 `docs/跨模块BUG修复机制.md`：封装与查关联BUG不矛盾(门照关+缝透明互补)；5步协议(铁律0:没复现+失败测试不算修好 / 沿接缝定位 / 判内部vs跨模块走handoff / 接缝集成测试防回归 / 双闸门关单)；建议建 EventBus 信号流日志+信号地图。gen_contract 只查签名缺行为集成测试=真缺口。
- 待办：用户指认具体拖着的UI BUG后，按协议②定位+①钉死失败测试再修。另 WuxiaMenuButton.tscn(mouse_filter 0→2)仍为别窗WIP，未动。

## 静默拦截 BUG 自动守卫 + 顺带修 BattleScene 回归（用户：开始界面卡死、零报错）
- 用户案例：能进游戏、开始界面卡死、全程零报错——根因是 WuxiaMenuButton 装饰子节点 mouse_filter 误写 0(STOP=拦截，反直觉)，盖住按钮中心吞掉 mouse_entered/button_up。属"静默接缝 BUG"，GATE1 永远抓不到。
- 已建自动守卫（用户要求"规避再复发"）：
  1. `tools/lint_mouse_filter.py`（GATE0 静态扫描，纯标准库）：扫 .tscn 找"按钮下可见装饰子节点 mouse_filter=STOP(0)"，visible=false 自动排除防误报；default 层仅报显式=0(零误报)，--tier strict 审计用。验证：合成夹具 BadLabel/显式0 均命中、Hidden(visible=false) 正确排除。
  2. `tests/unit/test_ui_mouse_filter.gd`（接入 GATE2）：加载 screens.json 全部界面 instantiate() 后遍历节点树，断言"BaseButton 可见 Control 子节点不得为 STOP(0)"。TestBase 是 RefCounted 不能 await，故用 instantiate()+树遍历（不需布局/场景树）。
  3. `docs/跨模块BUG修复机制.md` §6 静默拦截专项（mouse_filter 铁律/两道守卫/先查拾取别查逻辑/门禁非绿即阻断）；SOP §3 自查表加 GATE0；MEMORY 补自动守卫指针 + 铁律4门禁非绿即阻断。
- 顺带抓到并修一个多AI回归：`BattleScene.gd _on_auto_pressed` 缩进错（eid 在 for 外）早已 Parse Error，致 GATE2 战斗图标测试长期红却无人管 → 修缩进（fc743e7）。印证"红门禁=别人改崩"须立即修。
- **关键纪律暴露**：用户描述的根因修复 WuxiaMenuButton.tscn(0→2) 一直未提交！本次代为提交并明确归属 UI 窗口（待其设署名认领）。→ 印证"所有修改必须留痕"。
- 双闸门终验：GATE1 rc=0；GATE2 54 套件/0 失败/0 ✗（含新守卫测试）。提交 5cb9663(守卫)+fc743e7(战斗回归)+WuxiaMenuButton 根因修复。

## 让 AI 遵守流程（执行保障四层，用户提问"怎么让AI遵守"）
- 现有软约束已齐（MEMORY.md 铁律自动注入 + change-tracking skill + GATE0/GATE2），但缺"机器级硬约束"。用户问"后面怎么让AI遵守流程"。
- 落地四层 Enforcement：① 本记忆自动注入(最强软约束) ② change-tracking skill 任务级触发 ③ **用户开窗口粘启动口令**(`docs/AI协同启动卡.md`，指定窗口名=git署名，这是点火开关) ④ **git pre-commit 钩子机器兜底**(提交即扫静默吞点击BUG，违规则拦下)。
- 新增：`tools/hooks/pre-commit`(钩子) + `tools/install_hooks.py`(安装) + `lint_mouse_filter.py`加`--files`模式(只查本次提交文件) + `docs/AI协同启动卡.md`(给用户的复制粘贴口令) + SOP §11 执行保障 + MEMORY/技能补指针。端到端验证：构造违规tscn→git commit 被钩子拦下(exit1，未产生提交)。✓
- ⚠ 发现 `tools/check_mouse_filter.py` 是另一 AI 窗口写的重复扫描器(与 lint 同目的/同背景/实现略异)，未提交未接线，已定 lint 为唯一真源，待责任窗口确认后删/并，勿双扫描器并存。

## pre-commit 钩子隐患评估 + 两处加固（用户问"机器约束有无隐患/假性BUG"）
- 用户质疑机器级硬约束的隐患：范围窄致假性安全感/换机消失/覆盖旧钩子/--no-verify绕过/假性BUG/py路径失效。逐一评估属实。
- 已加固两处：① `install_hooks.py` 装前备份旧钩子为 `pre-commit.bak`（不再静默覆盖）；② `lint_mouse_filter.py` 加白名单 `tools/lint_mouse_filter.allow`（确有意的 STOP 精准豁免单文件，替代 `--no-verify` 整体绕过）。已实测：compile OK / 基线0命中 / 白名单豁免生效exit0 / 钩子备份逻辑生效。
- SOP §11 新增「第4层钩子已知隐患与缓解」诚实清单表 + 白名单用法。结论：摩擦极小（仅tscn提交毫秒级）；真要防的是换机消失/覆盖旧钩/绕过文化/假性安全感四条。
- 仍存待办：钩子不在git，换机/新克隆须重跑 install_hooks；py缺失时失败开放（守卫静默失效）；范围窄非通用UI安全网。

## pre-commit 守卫"误拦正常提交"漏洞堵死 + 对用户"有无必要/是否影响战斗背包"的答复（2026-09-02）
- **用户新质疑（关键）**：这个机器约束真有必要吗？真实用意？会不会影响战斗/背包等模块开发？最怕它"假性BUG导致其他AI检测不到、绕远路越走越偏"。
- **实测发现的真漏洞**：`lint_mouse_filter.py` 自身崩溃（如 `.tscn` 含非法 UTF-8 字节致 `open` 解码异常）会以 exit 1 冒充"发现 STOP 高危"，被 pre-commit 钩子误拦一个完全正常的提交并给误导性提示——这正是"机器约束反向卡开发"的唯一路径（此前7条隐患评估漏了"崩溃退出码与真违规退出码混用"）。
- **已堵死（commit 875d391）**：① `__main__` 加全局 try/except 失败开放——扫描器自身异常打印 stderr 告警 + exit 0 放行，绝不冒充违规；② 抽 `_safe_rel()` 消除 `os.path.relpath(fp, REPO)` 跨盘抛 `ValueError` 误放行真违规（findings 打印分支也用）。实测：坏文件→exit0放行✓；真违规→exit1拦✓；跨盘真违规→exit1拦且无误放行✓；基线0命中✓。
- **给用户的结论（已写进 SOP 第4层"给其他 AI 窗口的防绕路说明"）**：
  1. 真实用意 = UI「静默吞点击」这一类零报错 BUG 的**自动门**（用户曾血泪踩过 WuxiaMenuButton 0→2 卡死）。价值真实，但**只防这一种 BUG**。
  2. **零副作用**：只扫 `.tscn` 文本里 `mouse_filter` 一行，不碰/不运行/不修改任何 GDScript 与游戏逻辑。战斗/背包/存档/技能功能开发**完全不归它管**，绝不会"让战斗/背包模块出问题"。
  3. 透明可查：拦截中文提示直接打在 `git commit` 输出，任何 AI 都看得到，不会"检测不到"；来龙去脉 `change_log.py query --module tools` + `docs/更改日志.md`。
  4. 遇拦截正确做法 = 改 `.tscn`（mouse_filter=2 或加 allow 白名单），**绝不要为过钩子去动战斗/背包代码**——那才是真绕远路。已写入 SOP 防后续 AI 误判。
- 损益判定：约束必要（防阴间UI卡死BUG的唯一自动线），对功能模块零影响；唯一风险"误拦"已堵死，剩余皆"漏检"类（换机消失/--no-verify/只扫暂存/空格文件名），不会卡正常开发。

## 其他模块静默 BUG 预防思路（用户新问"会不会也表面不报错实际出BUG"）
- **定调**：会，且本项目已揪出并修过一打此类"阴间 BUG"（零报错但功能错/副作用没发生）——证明这是本项目最高发最阴的一类，不能只靠 mouse_filter 一个守卫。已修典型：BUG-04 商店锁定物经济漏洞(remove 返回值忽略白赚银两)、BUG-03 关系双写不同步脏档、BUG-26 背包空槽 continue 静默丢、UIBackground 音效文件缺失 play 静默跳过、preset_12x12.jpg 错命名导入静默失败、65b6ac6 延迟递归自转切场景永不执行、Lambda 捕获释放回调静默失效、纹理压缩 get_pixel 刷屏拖死主线程。
- **静默 BUG 高发区特征**：①跨模块接缝(信号/EventBus/服务返回值/数据key) ②有副作用但返回值可忽略的经济库存操作 ③异步延迟(await后真正要干的事) ④资源/数据文件加载(exists失败就跳过) ⑤枚举/魔法数字裸写(0/1/2易反)。
- **预防分层建议(给用户，按性价比)**：①[零成本]静默跳过→push_error/assert 现形；经济库存关系操作返回值必查+单测 ②[中/高收益]启动期"数据契约校验器"+资源存在性扫描接入 GATE0(类 lint 提交前/启动扫)；核心信号"发→收"集成测试进 GATE2 ③[机制]EventBus 信号流日志+关键不变量(背包总重/银两/关系单一真源)运行时断言；异步延迟体审查规则(lint/审查清单)。
- **给用户拍板点**：是否让我把最高性价比的两招落地——(a)资源/数据契约扫描器接 GATE0 (b)核心信号发→收集成测试。待用户定。

## 静默 BUG 两道防线落地（用户"继续" → 已收口，2026-09-02）
- **(a) GATE0 资源/数据契约扫描器**：新增 `tools/check_assets_contract.py`（纯标准库）。扫描 .tscn 的 `path="res://..."` 与 .gd 的 `load/preload("res://...")` 是否真实存在 + `data/configs` 下 JSON 合法性；补 lint_mouse_filter/signal_audit/audit_onready_paths 三者盲区（静默加载失败高发区）。
  - 修复链（铁律：失败开放，绝不冒充违规误拦）：① `_to_win()` 归一化 Git Bash 传的 `/d/xxx` POSIX 路径（否则 os.path.isfile/glob 全 False→守卫伪失效，比误拦更危险）；② `os.path.abspath` 必须在 `_to_win` 之后调用（`/d` 被当子目录算成 `D:\d\xxx`）；③ `_to_rel()` 只对含 `res://` 前缀的 group 去前缀（修 load/preload 引用被多截 8 字符误报）；④ `_strip_comments()` 跳过 `#`(gd)/`;`(tscn) 注释内字面量（消 2 处注释假阳性）；⑤ 全局 try/except 失败开放。
  - 实测基线：0 缺失 / 0 坏 JSON（真干净，非压成 0）。
- **(b) GATE2 信号发→收集成测试**：新增 `tests/unit/test_eventbus.gd`，自动被 run_all.tscn 收录。逐信号校验 ① `EventBus.has_signal`（删除/改名即 ✗）② 参数个数与契约一致（签名漂移即 ✗）③ 对通知/状态类接缝额外验证 `emit` 同步抵达订阅方（EventBus 单例未死）。覆盖背包/战斗/玩家/任务/UI/结缘等 90+ 接缝。
  - 关键修正（零副作用）：run_all.tscn 真实加载了 UIManager/GameManager/SaveManager 等 autoload 订阅方。首发版对"指令/流程类"信号（cmd_* / game_saved / bootstrap_* / scene_changed / ui_action_requested / game_error / inventory_add_overflow 的真实消费者 lookup）真实 emit 会触发真实业务（写存档 user://）+ 防御性报错日志（误导其他 AI）。已改为：指令/流程类只做契约校验**不**真实 emit；通知/状态类才 emit（仅驱动 UI 无害刷新）。复验：test_eventbus.gd 0 处 backtrace、0 ✗。
  - 不重复：背包满溢出真实生产者驱动已由 `test_inventory_service` 覆盖，本测试只补"契约+发→收"层。
- **双闸门终验（committed 状态复跑）**：GATE1 零 SCRIPT/PARSE/COMPILE ERROR；GATE2 **套件 通过 55 · 失败 0 · 0✗**（测试数 54→55）。
- **提交**：`f4e8906`[工具] 扫描器+lint路径修复+SOP防绕路说明；`efb5f30`[测试] 信号接缝契约测试+本日志。两笔均 change_log 登记（module=tools / tests），窗口署名 `AI-测试`。
- **连带已堵死的真漏洞（本会话最大收获）**：lint_mouse_filter.py 同样有 `/d/` POSIX 路径 bug → 意味着 pre-commit 守卫此前在 Git Bash 下**一直假性失效（静默跳过扫描）**，比"误拦"更危险；随 lint 修复一并堵死。
- **未动（非本任务）**：`data/configs/ui/ui_sfx.json` / `menu_hover_box.ogg` / `_verify_hover_audio.*` / `更改日志.md` 属音频音效窗口遗留改动，未捆绑提交，留给责任窗口。

## 登录界面悬停音效落地真实木质按钮音（本会话收口，2026-09-02 晚）
- **需求（用户 F5 实测后提）**：把下载的「框大按钮鼠标悬停」音效（爱给网 aigei_com）用作主菜单 5 个按钮的悬停音效。此前 hover 走 `ui_hover.wav` 占位（f6a52ef 合成）。用户担心"格式要不要改、别又出 BUG"。
- **格式核查（关键，防静默无声 BUG）**：`OggS` 魔数只表 Ogg 容器，不表编码。解析首 4KB 找到 `vorbis` + `Xiph.Org libVorbis I 20101101`，确认是 **Ogg Vorbis**（Godot 4 原生 `AudioStreamOggVorbis`）。DURATION=0.11s，适合悬停短音。本机无 ffmpeg，但 Vorbis 原生支持，无需转码。
- **复用现成链路（零代码改动）**：`play_ui_sfx("hover")` 仅被 `WuxiaMenuButton.gd` 使用（=主菜单按钮专属），故只改配置表即可精确命中登录界面、不波及其他 UI。把 `data/configs/ui/ui_sfx.json` 的 `hover.path` 由 `ui_hover.wav` 改为新 `menu_hover_box.ogg`（音量保留 -10.0dB）。
- **强制导入 + 运行时实证（防"文件存在但 load 静默跳过"类 BUG）**：
  1. 复制 ogg 进 `resources/audio/sfx/` 用 ASCII 名 `menu_hover_box.ogg`（避中文/空格路径坑）。
  2. `--quit` 模式**不**导入新资源（侧车 `.import` 不生成）→ 必须用 `godot --headless --editor --quit` 全量重导，生成 `menu_hover_box.ogg.import`（`importer="oggvorbisstr"` `type="AudioStreamOggVorbis"` `loop=false`，证明格式受支持）。
  3. `--script` SceneTree 探针运行时 `load()` + `ResourceLoader.exists` → 输出 `HOVER_LOAD exists=true class=AudioStreamOggVorbis is_audio=true`，全链路打通。
  - ⚠️ 验证坑：位置参数场景在 `_ready` 里 `get_tree().quit()` 会 SIGTERM 无输出；改用 `--script` + `_initialize()` 里 `quit()` 才正常。
- **双闸门终验**：GATE1 零 SCRIPT/PARSE/COMPILE ERROR；GATE2 **套件 通过 55 · 失败 0 · 0✗**（测试数 53→55，含他窗新增守卫，零回归）。
- **提交**：`63263e2` `[UI] 主菜单悬停音效替换为木质按钮音 menu_hover_box.ogg`（3 files：ogg + .import + ui_sfx.json）。pre-commit 钩子跑过（无 .tscn 改动→跳过静默拦截扫描）。精确 `git add`，未带 `docs/变更通告.md` 他窗改动。
- **铁律级教训（已闭环 line 210 的"遗留"状态）**：音频新资源必须 `editor --quit` 重导生成 `.import`，否则 `ResourceLoader.exists` 返回 false、`play_ui_sfx` 静默跳过（本项目的典型静默 BUG 高发区⑤"资源加载 exists 失败就跳过"）。验证链路优先用 `--script` 探针而非位置场景。

## 给"两道防线本身会不会也误导/牵制开发"的自审（用户追问，2026-09-02 晚收口）
- **用户追问**：这几个新设计（GATE0 扫描器 + GATE2 信号测试）会不会也产生"误导性 BUG"、牵制后续开发、让其他 AI 绕远路越走越偏。要求诚实、基于真实代码。
- **诚实结论**：
  - **(a) `check_assets_contract.py` 几乎不可能牵制开发**：grep 确认它**未接进任何门禁 / pre-commit**（只在自身文件头 + allow 白名单 + changelog 出现），是「只报告、exit 0、失败开放」的纯手动工具，当前连自动跑都没有，更不拦提交。唯一风险面：将来有人把 `--strict` 接进 pre-commit 且 allow 没配好才可能误拦——已向用户明确该"红线"（不要裸接 strict 进 pre-commit）。
  - **(b) `test_eventbus.gd` 才是真隐患源，已两轮收敛**：
    - 风险A（写错 arity→假✗误导其他AI去改游戏逻辑）：用脚本机械比对 EventBus.gd 全部真实信号——**97 条接缝全部存在、参数个数完全一致，风险A=0**（首跑脚本因信号行尾带 `#注释` 致正则 `$` 锚定漏抓 9 条，已先剥注释重跑，权威确认 0 不一致）。
    - 风险B（emit 触发真实订阅方→噪音日志+污染单例状态→后续测试偶发失败指向错误测试=越走越偏）：**已根除**。把 test_eventbus 改为"纯契约校验、绝不 emit"（commit `36fa9a6`）：移除 EMIT_SEAMS 与全部 emit，只查 `has_signal`(删除/改名✗)+参数个数(签名漂移✗)，抓全静默接缝三类形态；零副作用/零噪音/零状态污染。✗ 文案自带「若确有意为之请改本测试 SEAMS 表、勿改游戏逻辑」指引，未来任何 AI 看到 ✗ 都不会误读成游戏 BUG、不会去动战斗/背包/结缘代码。
- **双闸门复验（去 emit 化后）**：GATE1 零错误；GATE2 **套件 通过 55 · 失败 0 · 0✗**，且 `test_eventbus.gd` **0 处回溯引用（无噪音）**。日志里其余 ERROR/WARNING 全是别处测试故意测错误分支（no_such_*_999），与本次无关。
- **铁律级收获（写回方法论）**：任何"机器约束/测试守卫"上线前必须自问"它自己会不会产出误导信号"。对单例型通道（EventBus）做集成测试，**绝不真实 emit**，只做契约/声明校验，否则副作用与噪音会反噬后续测试、带偏他人。本记忆"GDScript 硬规"段可补：单例信号测试 = 契约校验优先、emit 验证价值极低且风险高。

## 待办清单 + 打进「工作室专业调教」exe（2026-09-02 收口）
- **需求**：建模块化待办清单（UI/战斗/背包/结缘/音频/世界/任务/测试…），覆盖「隐性BUG / 未实现逻辑 / 是否占位 / 未来优化建议」四维度；并共享进工作室 exe 当分页签。
- **三份产物**：`docs/backlog.json`（数据源，唯一待改入口）+ `tools/gen_backlog.py`（JSON→md+自包含HTML看板）+ `docs/backlog_dashboard.html`（双击即开，studio 同款皮肤）+ `docs/待办清单.md`（人读）。改待办只动 json 重跑生成器。
- **打进 exe（按更新 SOP）**：`studio_core.py` 加 `backlog_get()`（读 backlog.json）→ `studio_server.py` 加 `GET /api/backlog`（复用 `_origin_allowed`/`_send_json`/`discover_project_root`/`load_json` 约定）→ `index.html` 加「待办清单」分页签(section#tab-backlog + data-tab 按钮 + `backlogLoad()` 注入渲染)，init 里调用。
- **重打包 + 同步 + 自检**：`PyInstaller 工作室专业调教.spec --clean` 出 8,321,872B 新 exe（md5 `fd7ebb98`）；同步 6 处副本(工程内 dist×2+tools/dist + Desktop + D:\工作室专业调教 + D:\studio_push_tmp)，全部 md5 一致；`security_selftest.py` 15 断言全过。
- **⚠️ 关键坑（端口 8765 旧进程堆积）**：重打包前必须 `Get-NetTCPConnection -LocalPort 8765` 取 PID → `Stop-Process -Force` 清掉**全部**旧监听进程（实测有 2 个老 script-mode 服务器 33972/25124 占端口，新 exe 起不来/404）。清干净后新 exe(PID 25996) 才绑上；`/api/backlog` 实测 200（9 模块）、`data-tab="backlog"` 已注入 served HTML。
- **用户实测反馈**：刷新工作室页面后「网络连接失败」消失——该提示是更新重启杀旧进程时的瞬间断连，非故障。

## 工作室 exe 重打包：safe-delete shim 拦截 os.remove 的坑（2026-09-02 收口）
- **现象**：`pyinstaller 工作室专业调教.spec --clean` 在 EXE 步 `os.remove(self.name)` 报错——本机 Python `sitecustomize.py` 的 safe-delete 兜底 shim 把 `os.remove` 改成「先丢回收站」，而回收站操作失败 → `OSError [safe-delete] 操作失败` / 或旧进程锁文件 → `PermissionError WinError 5 拒绝访问`。
- **根因定位**：shim 仅拦截**Python 的** `os.remove`（sitecustomize 行 1149 `_SAFE_DELETE_ENABLED` 门控整个 monkeypatch）；Git Bash 的 `rm`/`mv`/`cp` 走系统调用，不受 shim 影响（所以 rename 探测显示未锁、但 pyinstaller 内部 os.remove 被拦）。
- **修复（两步）**：① 关 shim再 build：`CODEBUDDY_SAFE_DELETE_ENABLED=0 pyinstaller 工作室专业调教.spec --clean --noconfirm`（行 35 该 env=0 即不 monkeypatch，os.remove 回原生）；② build 前先 `taskkill /F /PID <占用8765的PID>` 杀掉旧 studio 进程释放 exe 文件锁（netstat -ano | grep :8765 取 PID）。两步齐了 build 才 EXIT 0。
- **pyinstaller 装哪**：managed venv `C:/Users/Administrator/.workbuddy/binaries/python/envs/default`（`python -m venv` 建 + `Scripts/pip install pyinstaller`，本机 6.22.2）。
- **datas 必须含数据文件**：onefile 打包后 `__file__` 指向临时解压目录，读 `MODULE_DIR/xxx.json` 必须 `datas=[('index.html','.'),('startup_card.json','.')]`，否则端点 404。
- **验证闭环**：build 后 `dist/工作室专业调教.exe` 起得来 + `curl /api/startup_card` 200 + `security_selftest.py` 15/15；同步 6 副本用 `cp -f` 后 `md5sum` 全同即齐。

## 工作室平台化架构方案（2026-09-02，用户要求大厂级演进规划）
- **需求**：把当前单机版「武侠内容工作室」演进为「横向能力模块 × 纵向工程版本」的二维解耦平台。支持多角色登录、远程调度、版本一键切换、跨项目总控台、git 元数据中心。未来可抽成独立后台工具，在任何服务器上调用。
- **交付**：`docs/工作室平台化架构方案.md`，含：
  - 五视角现状剖析（程序员/架构师/前端/后端运维/项目经理）
  - 5 大 Domain Module 横向归类（UI视觉/剧情创作/战斗关卡/资产资源/系统治理）
  - 六层解耦目标架构总图（客户端→网关认证→模块注册中心→Domain Modules→工程适配器→数据层）
  - RBAC 角色权限矩阵（超级管理员/剧情创作者/UI美术/战斗策划/资产管理员/治理专员）
  - 工程版本切换设计（manifest.yaml 驱动 Project Adapter）
  - **工程详情面板**（8 维度详细记录全景：开发落地架构·项目预设·后台源代码·开发工具·整体约束·框架思维+原有3面板），git 文件→结构化渲染引擎
  - 跨项目总控台（Super-Admin 视角：总流程/进度状态/人员当前进度/推进进度）
  - 远程调度与部署预留（绑定配置化/HTTPS/WSS/鉴权前置/审计日志/容器化）
  - 6 个 Phase 分阶段执行层（Phase 0~6，每阶段可独立发布、双闸门验收）
  - 开发清单（通用红线+各 Phase 具体项）+ 日志清单（平台自身审计+工程元数据 git 清单）
- **关键设计决策**：
  - Domain Module = 换游戏也存在的「能力」（UI/剧情/战斗/资产/治理），每个模块自包含(路由+逻辑+前端)
  - Project Adapter = 每个游戏一份 manifest.yaml 描述数据路径/格式/可用模块/校验规则
  - 总控台不存数据，从各工程 git 仓库读取元数据中心聚合展示
  - 鉴权先本地实现(users.json)，远程前必须叠加

## Phase 1a 落地：模块注册中心骨架（2026-09-02，用户"continue"后执行）
- **做了什么**：创建 `tools/desktop_studio/modules/__init__.py`（模块注册中心），定义 5 个 Domain Module 的自描述清单（id/名称/角色可见性/功能列表/描述），含 112 个函数→Domain 精确映射、RBAC 角色过滤（6 角色）、函数归属查询。纯元数据层，**零业务函数移动**，零回归风险。
- **新增接口**：`studio_server.py` 加 `GET /api/modules`（返回当前角色可见的 Domain 清单 + 角色列表）。Phase 3 接入鉴权后 role 从 token 解析；当前默认 super_admin 全量。
- **验证**：① `security_selftest.py` 15/15 全通过（零回归）；② 模块包导入测试：5 Domain 注册正确、6 种角色过滤精确（剧情创作者只看 narrative、UI美术只看 ui_visual…）、7 个跨域函数归属查询全部命中。
- **文件变更**：新建 `modules/__init__.py`；编辑 `studio_server.py`（+8 行 /api/modules 处理）。
- **下一步（Phase 1b）**：把 studio_core.py 的 200+ 函数按 Domain 物理迁移到 `modules/<domain>.py`，studio_core 退化为 re-export shim + registry。需要用户拍板 4 个方向（远程紧迫度/数据规模/技术栈/并发模式）后精确执行。
  - 不推翻现有代码，分 Phase 渐进式重构

## 项目经验白皮书 + 项目 skill 扩展 + manifest 登记（2026-09-02，本会话收口）
- **需求（用户大任务③+补充）**：罗列当前开发项目详细经验、大厂级开发文档（架构/各功能与子功能的实现/用了什么代码思路/解决什么/隐患）、梳理项目结构、生成项目 skill、卡住很久的 BUG 根因-解决-避免、提炼经验/流程/可复用性。并补充：项目评价方案（亮点/与大厂差距/显性隐患/维护建议）+ 工作室平台价值定位（复盘调优+跨项目复用+换皮零成本+新AI秒懂架构+多工程进度纵览+任务缺失定位可查）。
- **交付**：
  1. `docs/项目经验白皮书.md`（八章+附录）：第一章概览/第二章架构总览(分层·装配中枢·EventBus NOTIFY/CMD·数据流)/第三章模块与功能清单(功能→子功能→思路→技术→解决→隐患，含 3.3.1 战斗系统深度剖析：CombatCore RefCounted逻辑内核返回 Array[CombatEvent]、CombatService JSON构建+DifficultyManager、TacticalBattleScene/BattleGridNode 逻辑-视图接缝)/第四章结构梳理/第五章卡住BUG专题(BUG-01/03/04/06/14+主菜单freeze+纹理压缩+静默接缝)/第六章项目评价方案(亮点/与大厂差距/显性隐患/维护建议)/第七章经验提炼/第八章平台价值定位/附录。
  2. 扩展 `.workbuddy/skills/wuxia-game-dev/SKILL.md`：新增「十、卡住BUG根因与避免速查」「十一、可复用模式与平台连接器」，上手检查单指向白皮书+change-tracking skill+连接器范式。
  3. `tools/desktop_studio/projects/wuxia_game.yaml` 的 `knowledge.refs` 加 `docs/项目经验白皮书.md`（首推），`reusable_patterns` 补装配中枢/EventBus NOTIFY-CMD/事务化资产API/延迟一帧切换/双闸门，`predecessor_mistakes` 补主菜单freeze；`docs/工作室平台化架构方案.md` §12.4 经验库数据源加白皮书首推。
- **事实来源（零臆造）**：读 MEMORY.md + `docs/代码审计报告_2026-09-02.md` + `docs/更改日志.md` + `docs/契约总表.md` + 核心源码(GameManager.gd/EventBus.gd/combat_core.gd/combat_service.gd/tactical_battle_scene.gd/battle_grid_node.gd)。
- **提交策略（用户拍板「现在就提交」）**：三处改动(白皮书+skill+manifest+架构方案)精确 `git add`，跑双闸门(GATE1+GATE2)验证零回归后，按窗口署名 `AI-架构`/`AI-文档` 提交 + `change_log.py add` 留痕；**严格不碰战斗窗 `preset_10x10.json`/`preset_12x12.json`**（主权边界）。

## 经验库面板落地 + Gitee 私人仓库备份 + 白皮书续篇（2026-09-02 收口）
- **上轮"两个都做"收口**：经济/结缘深度剖析(§3.3.2/§3.3.3)基础上，拆 `docs/经验库/` 11篇可检索子文档；工作室加「📚 经验库」标签页 + `/api/experience` + `/api/experience/doc`(路径穿越防护+仅md/txt)。端点进程内直测全绿(refs=16 / doc 返回 OK / 穿越与非md-txt 均 403)。双闸门用 **Godot 4.7.2** console 复验全绿(55套件0失败0✗；注意工程实际是4.7.2，非4.3，4.3跑GATE无意义)。提交 `21857da`[架构] + change_log + 推 Gitee 私人仓库(asdf1328886661/wuxia-game)成功(8fecdd2..21857da)。
- **Gitee 私人仓库备份（用户要求"后续整个工作室平台开发结构+文本文档 git 到 Gitee 私人仓库保证安全性"）**：origin 已指向 `https://gitee.com/asdf1328886661/wuxia-game.git`；.gitignore 已排 .godot/导出/exe/__pycache__ 等。推送用**临时令牌 URL**(不写 git config、不落盘)。⚠️ 令牌由用户提供，建议后续改 SSH/凭据助手，别在聊天贴令牌。本次推送范围=工作室结构(tools/desktop_studio)+文本文档(docs)，不含游戏二进制/缓存(主权边界 + gitignore 双保险)。
- **白皮书续篇（用户大需求：全角色拆解+复盘+对照工作室平台给优化蓝图）**：新增 `docs/全角色工程手册与平台优化蓝图.md`（第九~十一章）：① 第九章七工位手册(PM/架构/程序/前端/美工/运维/迭代，每工位=职责边界→功能拆解→子模块拆解[真实文件]→实现路径+mermaid思路图→踩坑→0成本目标)；② 第十章复盘(优缺点/可复用/可扩展/当前未来优化/隐藏风险/跨项目可学/小白成长 八维)；③ 第十一章平台优化蓝图(对照10个已落地模块/插件/功能，5个优化方向：双闸门平台化按钮/change_log+handoff合进平台/静默守卫常驻/经验库增强检索；可扩展/经验调用/交互优化；各工位无限趋近0成本愿景；Phase0~6路线)。白皮书附录后加「续篇索引」章节；knowledge.refs 注册续篇(经验库可检索)。双闸门全绿；提交 `3ca0bfb`[架构] + change_log + 推 Gitee(21857da..3ca0bfb)。
- **结构决策**：续篇独立成文件(非塞进白皮书单文件)，延续用户刚认可的"可检索子文档"哲学——白皮书作主锚点+索引，续篇与经验库子文档均可独立检索。mermaid 思路图在 Gitee/编辑器渲染，经验库面板轻量md渲染不解析mermaid(显示代码块，可接受)。
- **GATE1 grep 注意**：grep 含 ERROR 时，SaveManager「存档解析失败(回退备份)」是损坏存档回退测试的**预期日志**(失败0)，非回归；判绿以 `套件：失败 0` + `✗==0` 为准。

## 双闸门平台化验证按钮落地（2026-09-02，兑现0成本愿景#1）
- **需求**：把白皮书第十一章优化方向#1 做成真实工作室功能——PM 不装 Godot 也能一键跑双闸门卡质量。
- **后端**：`studio_server.py` 加 `_godot_exe()`(优先 settings.godot_path，否则托管二进制目录候选 4.7.2/4.3) + `_run_gate()`(subprocess 调 `godot --headless --path <root> --quit` 跑 GATE1 抓 SCRIPT/PARSE/COMPILE ERROR；再调 `res://tests/unit/run_all.tscn` 跑 GATE2，正则解析 `套件：通过 N · 失败 M` + 数 `✗`；返回 green 绿/红) + `GET /api/gate/run`(只读验证，localhost 起源守卫)。超时 GATE1=120s/GATE2=300s。
- **前端**：`index.html` 加「🔧 双闸门」导航 + `tab-gate` 标签页 + `gateRun()`/`gateLoad()`(打开自动跑一次，渲染绿/红报告，红门禁提示禁止提交)。
- **验证（决定性）**：进程内真实 `subprocess` 调起 Godot 4.7.2 headless 跑通——GATE1 errors=0 绿；GATE2 55套件0失败0✗ 绿；green=true。提交 `cb33faa`[后端运维] + change_log + 推 Gitee(3ca0bfb..cb33faa)。
- **⚠️ 外部改动坑（本会话新发现）**：编辑 `studio_server.py`/`index.html` 时反复报 "File has been modified since read（modified by user or linter）"——疑似本机有文件监视/格式化进程在改这两个文件（index.html 行号从 2079 漂到 2202）。对策：编辑前先重新 Read 该文件对应区段再 Edit，可成功；内容未被破坏（grep 确认新端点/标签均在）。
- **⚠️ 提交拓扑坑**：本会话中段出现他窗提交 `678c104 [UI] Phase1 放权`（通用 UISkinLoader + UI皮肤定制tab，改 studio_core.py+index.html）插入在 3ca0bfb 与 cb33faa 之间。故 `git diff 3ca0bfb HEAD -- tools/desktop_studio/` 含 678c104 的 +150(index)+60(core) 与 cb33faa 的 +67(server，含 _run_gate)。`git show --stat HEAD` 只列 index.html 是因 cb33faa 与父(678c104)的 diff 仅含 index.html 增量；studio_server.py 的 _run_gate 确在 HEAD（`git show HEAD:studio_server.py | grep _run_gate` =2 行，已验证）。推送前务必 `git show HEAD:<file> | grep` 裁定真实内容，勿被 --stat 单行误导。
- **沙箱 git 写不落盘仍适用**：本机真实提交靠 `/commit` 流程；本次在 sandbox 内 `git commit`+`git push`(令牌临时URL) 实测能落 Gitee（21857da/3ca0bfb/cb33faa 均上库），说明当前 sandbox git 可持久化+可推远端。

## UI 放权 Phase 1/2 收尾（2026-09-02，本会话续写）
- **Phase 1（commit 678c104）**：游戏侧 `core/ui_skin.gd`(UISkin 装载器)+数据 `data/configs/ui/skin/theme.json`+`confirm_dialog.layout.json`；ConfirmDialog 接入皮肤/尺寸(零 .tscn 改动)；工作室 `studio_core.py` 加 `_ui_skin_path` 白名单+`ui_skin_get/save`；`studio_server.py` 加 `GET/POST /api/ui_skin`；`index.html` 加「🎨 UI 皮肤定制」tab（确认框尺寸滑块+主题配色取色器，含复原默认）。双闸门全绿、exe 重建6副本md5一致。changelog 登记 4c70a17。
- **Phase 2（commit 6c7cab5）**：游戏侧 `core/ui_vfx.gd`(UIVFX 装载器)+数据 `main_menu.vfx.json`；MainMenu 程序化背景(飘叶/云/水/船)接入 VFX 预设(开关+速度/数量/重力外置)；工作室 `ui_skin_save` 扩 `main_menu_vfx` 数值裁剪；`index.html` 加「③ 视觉特效」卡片(开关+滑块)。双闸门全绿(55套件0✗)、exe 重建 md5 `e0b893f2…`、安全自检15/15。
- **收口**：changelog 登记 `ddc1818`(Phase2)；待办清单 `backlog_done.py` 标记 Phase1(678c104)/Phase2(6c7cab5) 为 done 并生成追溯块(日期/时间/resolvedBy=UI/commit/一键复原)；提交 `e69a7d2`。
- **多 AI 纪律复核**：全程未碰他窗未提交文件 —— `studio_server.py`(经验库增强检索 144行，属他窗)、`preset_10x10/12x12.json`(战斗窗)、`2026-09-02.md`(本窗但属其他 AI 段落)；仅 stage 本人文件。
- **CRLF 坑（复现）**：exe POST 写 JSON 带 CRLF，与提交版 LF 产生 3± 行 diff；每次登记前 `git checkout -- <json>` 还原为提交版再登记，避免误带 CRLF 进 changelog。

## 经验库增强检索落地（2026-09-02，续篇第十一章#4，本窗[工作室]）
- **需求**：用户从第十一章 4 个优化方向里选「经验库增强检索」——标签云 + 按角色/模块/BUG号过滤，让新AI/新人秒级命中经验(如 mouse_filter 铁律)，趋近0成本复用。
- **后端**（`studio_server.py`，纯平台层）：加 `import re/glob` + `_EXP_ROLE_KW/_EXP_MOD_KW` 短语级词表 + `_exp_derive_facets()` + `_exp_enrich()`(在 `/api/experience` 返回前富化)。每个 ref 带 `tags/roles/modules/bugs/group`；`facets` 供前端标签云/分面。角色模块优先用子文档「检索关键词」行(权威)，BUG号全文显式提取。glob 展开的绝对路径 `os.path.relpath` 归一化正向斜杠。
- **前端**（`index.html`）：经验库 tab 加 `#expFacets` 标签云 + 角色/模块/BUG 分面按钮(点击切换、组内OR组间AND) + 「显示变更通告(N)」开关(默认隐藏79篇变更通告)；列表项显示角色/模块/BUG mini 标签；打开文档顶部显示 meta。新增 CSS `.exp-facets/.fchip/.mini/.imeta/.refmeta`。
- **验证（决定性）**：进程内 FakeHandler 直测 `/api/experience`(200, refs=dict含分面, core16/notice79, notice路径已归一化非绝对) + `/api/experience/doc`(打开08含mouse_filter) + 穿越 `../../` 返回 403；node --check 主脚本语法OK。验证期 `pip install pyyaml` 仅用于加载 manifest(运行时依赖)。
- **提交/推送**：`e718133`[工作室] 代码(2文件 +239/-14) → `f6aebf4`[文档] change_log 登记 → 推 Gitee `cb33faa..f6aebf4`(PUSH_RC=0，含他窗 678c104/4c70a17)。**纯平台层改动未碰游戏代码，Godot 双闸门不在作用域**；仅 stage 本人两文件，主权边界 intact。
- **取舍**：角色/模块派生两方案权衡后选「检索关键词行优先+正文回退」(比裸正文扫描精准、比加manifest元数据零维护)；变更通告默认隐藏(比删条目更可追溯)。

## 全维度分析终极版（2026-09-02，用户粘贴元提示词，本窗执行）
- **交付**：`docs/项目全维度分析与平台优化_终极版.md`（413行）：①项目全局拆解 ②七角色(PM/架构/后端/前端/设计/运维/TechLead)各含正向+逆向审查清单 ③5子功能深度拆解表(EventBus/事务化资产/战斗/纹理压缩/双闸门) ④七维隐患清单 ⑤工作室平台对照(能力映射表15tab+50+API/可下沉6项/需新增4插件/交互优化/经验调用/0成本路径) ⑥总结(可复用资产表/可扩展点表/路线图) + 《学习记录》+元提示词更新日志。
- **真源对齐**：读取 15 个 tab(data-tab)、50+ API 端点、白皮书零~八章+续篇、11篇经验库子文档、security_selftest 15 检查项、open派单3项，零臆造。
- **提示词库进化**：把元提示词落为 user-level skill `project-full-analysis`(v1.1)，更新日志回写 5 条通用经验(防平台持有数据/路径relpath归一化/高频日志隐藏/权威关键词标签源/能力映射对齐真实清单)。
- **提交/推送**：`0efec70`[文档] 分析文档 → `a8af0e1`[文档] changelog 登记 → 推 Gitee `9999fb0..a8af0e1`(PUSH_RC=0)。纯文档改动，主权边界 intact。

## 提示词系统升级：角色库 → AI 工程操作系统（总控层）
- **产出**：`docs/AI工程操作系统_主控编排.md`（新增），与既有 `docs/提示词系统_全角色闭环自迭代.md`（角色能力模块库）形成两层。
- **核心升级**：从"角色驱动"改为"任务驱动"——L0 项目身份 / L1 真实结构图(20 autoload + EventBus 70+ 真实信号 + 场景文件) / L2 模块卡(Combat/Inventory/Bond/Dialogue/UI-Popup，含真实 BUG-战斗→town掉血崩、BUG-03关系双写、BUG-04售卖锁定物) / L3 任务总控 Prompt(唯一入口，自动编排角色) / L4 角色被调用 / L5 灾难模拟审查 / L6 经验提炼(E1~E4 + 五维知识映射经验库00~10/代码审计报告BUG-01~33/MEMORY.md)。
- **接地事实**：全部锚定真实文件（`autoload/EventBus.gd` 信号、`core/combat_*`、`scenes/...`、经验库 00~10），非模板编造。平台反哺接 `/api/experience` + `manifest.yaml`。
- **用户诉求校准**：用户长文质疑"未读真实代码"，已用 Grep 真实 autoload/信号回应并落地；保留其 `【K】` 格式但强制带"归档目标"字段映射到真实文件。

## AI工程操作系统（2026-09-02，用户分享外部愿景，本窗落地）
- **核心转变**：从「角色驱动」升级为「任务驱动」——装 L3 任务总控作唯一入口，角色提示词降级为「总控可调用模块」，解决 Prompt 通胀。
- **交付**：`docs/项目AI工程操作系统.md`（221行）：八层映射(已有 L0/L1/L4/L5机械/L7雏形，缺 L2模块卡+L3总控) + 真实 Combat Module Card(基于 services/combat/*、core/combat_*、scenes/gameplay/battle/*、data/configs/battles/grids/preset_*.json、tests/unit/test_battle_*.gd 真实文件) + L3 总控 Prompt(接经验库分面=Knowledge Router、双闸门=审查Gate、change_log=变更层、灾难模拟=L5) + L6 五维知识分级 E1-E4(解决知识鲸鱼) + 适配目录结构(不长平行仓库)。
- **skill 落地**：user-level `ai-engineering-os`(v1.0) 作单一总入口；`project-full-analysis` 视为其可调用模块。
- **提交/推送**：`283ffb7`[文档] OS文档 → `f04143b`[文档] changelog 登记 → 推 Gitee `a8af0e1..f04143b`(PUSH_RC=0)。纯文档，主权 intact。
- **外部文本误判纠正**：该愿景文称「无项目代码/需上传ZIP」——实际工程全在 D:/武侠游戏 本仓，直接基于真源构建，无需上传。

## 项目专属「全角色闭环自迭代提示词系统」文档生成（本会话）
- **需求**：用户粘贴两份长文本（通用武侠CRPG提示词系统 + 通用项目提示词模板），要求基于**真实项目代码/经验书/平台**生成专属提示词系统，覆盖七视角(PM/架构/程序/UI/美工/运维/迭代)、正逆向思维、分模块通用提示词、自学习扩充闭环、工作室平台优化对照。
- **关键校正（已向用户说明）**：用户粘贴的 `【K】`+飞书文档格式**本项目不存在**；真实沉淀体系 = `MEMORY.md`(自动注入) + `docs/项目经验白皮书.md` + `docs/经验库/00~10` + `docs/代码审计报告_2026-09-02.md`(BUG-01~33) + `docs/更改日志.md`/`变更通告_*.md`(79篇) + `契约总表.md`。桥接策略：保留 `【K】` 作 AI 输出格式，但每条带「归档目标」字段精确映射到真实经验库位置，并经 `projects/wuxia_game.yaml` 的 `knowledge.refs` + 平台 `/api/experience` 自动载回下一轮。
- **交付**：`docs/提示词系统_全角色闭环自迭代.md`，结构：〇校正 / 一基底上下文(五层/枢纽/服务/事件执行层/协同铁律) / 二载入知识库+自学习底座 / 三(3.1~3.7)七角色提示词模板 / 四(4.0~4.7)分模块通用提示词(战斗/对话/背包/结缘/UI弹窗/资源纹理+其余模块索引表,均含真实信号/路径/BUG关联) / 五工作室平台优化(能力映射表/可下沉清单/新增插件优先级/交互优化) / 六自学习闭环落地(归档命令/校验) / 七小白收获 / 八附录(open派单/BUG速查/经验库索引/双闸门命令)。
- **事实来源(零臆造)**：三个 Explore agent 并行精读 autoload/core/services/scenes/data + docs/经验库 + tools/desktop_studio(端口8765顺延/_exp_enrich富化/security_selftest 15断言/6副本缺口/GATE0未统一)。
- **平台优化要点(文档内)**：P0 写 `deploy.py` 把「6副本同步」机器化 + 整合 `lint_mouse_filter/signal_audit/check_assets_contract/audit_onready_paths` 为 `/api/gate0` 静态门禁；P1 派单看板 tab + 语义经验检索(现仅关键词匹配)；并强调「连接器而非容器」已落地，新增能力勿碰 core/services/autoload/EventBus 契约(locked 段)。

## 工作室 UI 模块入口重组 + 使用手册（2026-09-02，UI 窗口）
- **需求**：用户截图要求把工作室所有 UI 模块内容整合归类、详细分类标注、配使用手册。
- **做了什么**：① 导航栏新增蓝色「UI 模块」分组，聚合 登录界面/预加载界面/UI 贴图/UI 皮肤定制 四个 tab，每个加分类标签（界面/贴图/皮肤）+ title 提示；② 产出 `docs/工作室_UI模块使用手册.md`（四大子模块详述、5 分钟上手路径、数据文件对照表、安全回退、角色分工、FAQ）。
- **验证**：安全自检 15/15；双闸门 GATE1 零错误、GATE2 55套件0✗；重打包 exe（md5 `75c9f211`，build 13:33）同步 7 副本一致；新 exe curl /api/tool_version + /api/ui_skin 正常。提交 `21d7081`[UI] + `8802a79`[文档]（changelog 补 commit hash）。
- **多 AI 纪律**：未碰他窗文件（studio_server.py 经验库增强检索、preset_10x10/12x12.json、其他 AI 的 memory 与未跟踪 doc）；仅 stage 本人 index.html+手册+changelog。
- **注意**：`docs/更改日志.md` 被他窗持续改写（外部改动坑），Edit 需 re-read 或改完即补 commit；记忆文件同样被并发写，用追加以规避冲突。

## 工作室 UI 模块页面内详细分类标注（2026-09-02，UI 窗口 continue）
- **需求延续**：用户说「Please continue」，继续做实「详细分类，标注」。
- **做了什么**：在 登录界面/预加载界面/UI 贴图/UI 皮肤定制 四个 tab 顶部新增 `cat-ui` 分类图例，各功能区标题加 `cat` 色标：背景（绿）/文案（黄）/资源（蓝）/布局（紫）/配色（蓝）/特效（红）；使用手册新增「分类颜色与标注含义」章节，并给所有子模块表格加「分类」列。
- **验证**：HTML 结构 tabs match=True；安全自检 15/15；双闸门 GATE1 零错误、GATE2 55套件0✗；重打包 exe（md5 `a770d38d`，build 13:42）同步 7 副本一致；新 exe curl /api/tool_version + /api/ui_skin 正常。提交 `bab94ca`[UI] + `2ca22af`[文档]。
- **多 AI 纪律**：未 stage 他窗文件（memory、战斗 preset、其他 AI 的 doc）。

## 工作室导航改为下拉式 UI 模块入口（2026-09-02，UI 窗口 continue）
- **需求延续**：用户要求「把 UI 模块做成顶部一个入口，点击展开，里面按登录界面/预加载/UI 子功能分组」。
- **做了什么**：① CSS 把原 `nav-group/ui-group` 改回普通顶层按钮，新增 `.nav-dropdown-wrap/.nav-dropdown/.dd-group/.dd-item` 下拉样式；② nav HTML 只留一个 `UI 模块 ⬇️` 根按钮，下拉里分「界面屏」(登录界面/预加载界面) 和「UI 子功能」(UI 贴图/UI 皮肤定制)；③ JS 增加 `toggleUiMenu/closeUiMenu`、根按钮点击切换、子项点击激活 tab 并高亮根按钮、点外部自动收起；④ 手册「UI 模块在工作室里长什么样」改为下拉菜单 ASCII 说明。
- **验证**：tabs match=True；无 `ui-tag/nav-group` 残留；安全自检 15/15；双闸门 GATE1 零错误、GATE2 55套件0✗；重打包 exe（md5 `65e8deb3`，build 13:50）同步 7 副本一致；curl /api/tool_version + /api/ui_skin 正常。提交 `6ab0ac3`[UI] + `8d573c1`[文档]。
- **多 AI 纪律**：未 stage 他窗文件（memory、战斗 preset、其他 AI 的 doc、studio_server.py 经验库增强检索、未跟踪的 _orc_out.txt/_verify_orc.py）。

## 中期#2 L1 自动依赖图扫描器（2026-09-02，AI-架构窗口）
- **背景**：用户拍板「先短期，再中期」。短期#1(L3任务总控)/短期#2(经验库E1-E4标签) 已于本会话前段收口并推 Gitee（提交 99366aa/447ae1b/6079c2f + 推送 a867ee7..6079c2f）。本段执行中期#2。
- **需求**：扫描全部 .gd 的 class_name/extends/preload/load("res://...")，按五层架构生成依赖图，接入 L3 任务总控。
- **做了什么**：① 新建 `tools/desktop_studio/scan_deps.py`，`scan(root)` 返回 {root,summary,violations,unresolved_refs,nodes,edges}；五层 foundation rank（core<data<services<autoload<scenes<resources/tests/tools）；提取边后按「仅允许上层依赖更基础层」标注向上依赖违例（条件 `rt>rf`）；CLI 支持 --root/--out/--mermaid。② `studio_server.py` 新增 `GET /api/deps`（L1 结构认知），调 `scan_deps.scan(core.discover_project_root())`。③ `index.html` 任务总控 tab 加「🕸 依赖图」按钮 + `orcDeps()` 可视化（摘要/各层文件分布/向上依赖违例清单/未解析引用）。
- **验证（据实）**：HTTP 冒烟 `ok=True`；208 .gd / 205 边（166 已解析/39 未解析）/ **仅 1 处真实向上违例** = `autoload/ui_manager.gd` preload → `scenes/ui/icon_registry.gd`（全局管理器直依赖场景文件，应走 EventBus 解耦）。首版 LAYERS 顺序写反导致 65 假阳性（scenes→core 错判），改 foundation rank 后修正为 1，符合工程「健康但 minor 耦合」现状。
- **零回归**：纯平台层改动；未碰 preset_*.json（战斗窗 modified 保留）/UI 放权窗未交文件/任何游戏代码。pre-commit 钩子因无 .tscn 改动跳过静态扫描。
- **提交与上库**：`fc578c4`[工作室]（3 files, +235）+ `eb021a8`[changelog] 登记（`tools/change_log.py add --commit fc578c4 --module 工作室 --scope "tools/desktop_studio（L1 依赖图）" --ref 中期#2`）；Gitee `6079c2f..eb021a8` 推送成功（认证走本机缓存凭据，未落盘令牌）。
- **待办（派单）**：1 条违例 ui_manager→icon_registry 经 handoff 派给 UI/架构窗口走 EventBus 解耦（非阻断，minor 耦合）。中期#1（背包/对话/结缘/UI/存档 五张 Module Card → docs/模块卡/）尚未启动。

## HUD 四面板默认位置外置 + exe 重打包（2026-09-02，AI-UI 窗口）
- **范围**：继续推进 UI 放权路线图 Phase 3 首切：把 HUD 4 面板默认坐标从 GDScript 硬编码迁到 `data/configs/ui/hud_layout.json`，工作室新增拖拽编辑页。
- **游戏侧**：新增 `core/ui_layout.gd`（UILayout 装载器），4 个 HUD 面板 `_init_drag` 改为 `UILayout.hud_default_pos(...)`，按 1920x1080 参考分辨率等比缩放到当前视口；原有 `user://ui/hud_positions.json` 个人偏好仍优先覆盖。
- **工作室**：`studio_core.py` 加 `hud_layout_get/update`（含备份/裁剪/默认值合并），`studio_server.py` 加 `GET/POST /api/hud/layout`，`index.html` 加「HUD 布局」拖拽 tab；手册补 §2.5。
- **验证**：GATE1 0 错误；GATE2 套件 通过 55 · 失败 0 · 0✗；security_selftest 15/15 PASS。
- **打包交付**：定位到 PyInstaller 在 managed venv `envs/default/Scripts/pyinstaller.exe`（修正环境记忆）；`--clean` 重打包新 exe md5 `5fbd50c3`；同步 6 处副本 md5 一致；curl `/api/hud/layout` 在脚本模式与窗口 exe 均返回 4 面板默认值，POST 持久化生效；测试写入后已用 `git show HEAD:data/configs/ui/hud_layout.json` 还原默认，工作树干净。
- **提交**：`8147d4f`[UI] 代码已在前段 change_log 登记。

## 工作室前端页签合并（2026-09-02，AI-架构窗口）
- **需求**：用户要求把「协同启动卡 + 待办清单」合并到一个页签（不再拆分，方便同屏查看/复制）；随后追加：把「操作日志 / 设置 / 回收站 / 双闸门」也合并，自定义功能名由我定。
- **做了什么**：`tools/desktop_studio/index.html` 纯前端改动——
  - `coord` 页签（🤝 协同启动卡 / 待办清单）：原 tab-backlog + tab-startup 合并，点击分发 `backlogLoad()+loadStartupCard()`。
  - `sys` 页签（自定义名「⚙️ 系统运维」）：原 tab-log + tab-settings + tab-trash + tab-gate 合并为单 section（顺序按用户提法：操作日志→设置→回收站→双闸门），点击分发 `logLoad()+setLoad()+trashLoad()+gateLoad()`。
  - 导航由 7 个按钮精简为 4 个：`sys / coord / exp / orchestrate`。后端接口零改动；各子块元素 ID 全部保留，原加载函数照常工作。
- **验证（据实）**：node --check 嵌入 JS 语法 OK（104037 字符）；grep 确认无 `tab-trash/tab-log/tab-gate/tab-settings` 旧 id 残留（`tab-settings_screen` 是无关的游戏内「设置屏」编辑页，未碰）；端口冲突问题：本机已有运行的 studio exe 占用 8765 且内置旧 index.html，冒烟 curl 打到旧实例，故改用静态校验（磁盘文件确为新版）。
- **零回归**：未碰任何游戏代码与他窗主权文件（preset_*.json / SettingsScreen.gd / studio_core.py / studio_server.py 保持他窗 modified 未动，本次只 stage index.html）。
- **提交与上库**：`ec0f965`[工作室]（+125/-58，纯前端）+ `14ea209`[changelog] 登记；推 Gitee `d4efeef..14ea209` 成功（免交互令牌，已配置）。
- **用户须知**：本机若用已打包 studio exe 看界面，需重打包 exe 才能反映 index.html 改动（源码 `python studio_server.py` 直接跑即是最新版）。

## 双闸门改为手动触发 + 工作室 exe 重打包（2026-09-02，AI-架构窗口）
- **用户反馈**：点开「系统运维」页签就自动执行双闸门验证（耗时几十秒），不知双闸门干嘛、怎么用，要求告知并评判如何改。
- **根因（据实）**：合并 sys 页签时我在点击分发写了 `if(b.dataset.tab==='sys'){logLoad();setLoad();trashLoad();gateLoad();}`，而 `gateLoad()` 逻辑是「首次自动调 `gateRun()`（真正跑 GATE1+GATE2）」；原本双闸门是独立页签、点它才跑合理，合并成系统运维子块后误触发。
- **双闸门是什么（已用大白话告知用户）**：平台化质量门禁 = GATE1（Godot headless --path --quit 零 SCRIPT/PARSE/COMPILE ERROR）+ GATE2（tests/unit/run_all.tscn 零 ✗/失败 0）；任何 AI 改完代码一键确认「没改崩」，多 AI 合并前卡质量（非绿即阻断）。
- **改法（用户选手动触发）**：`index.html` 删 sys 分发里的 `gateLoad()`，原 `gate` tab 死代码分发改注释；双闸门仅「▶ 运行双闸门验证」按钮 `gateRun()` 手动触发。JS node --check OK，grep 确认无 `gateLoad()` 调用点残留（仅剩函数定义本身）。
- **重打包（让桌面 exe 生效）**：spec 已含 scan_deps hiddenimport（本轮早前补 `174d2e4`，避免 /api/deps 在 exe 里缺模块）；因 index 改动需重打包，`--clean` 重打包成功（15s），sync 5 处副本（tools/desktop_studio/dist、tools/dist、dist、Desktop、真桌面）md5 一致 `482b998114d8ca3adc7136287881c782`。
- **零回归**：纯前端+打包，未碰游戏代码与他窗主权文件。
- **提交**：`918a1a7`[工作室 双闸门手动触发] + `eac2934`[changelog]，推 Gitee `81d41ae..eac2934`。

## 架构违例治理 ui_manager→icon_registry + 协同启动卡/待办清单 UI 再重构（2026-09-02，AI-架构窗口）
- **需求①（架构违例治理）**：用户要求治理全工程唯一真实架构违例 `autoload/ui_manager.gd` preload → `scenes/ui/icon_registry.gd`（基础层静态反向依赖上层），走 EventBus 解耦。
- **做法（依赖反转，零静态耦合）**：
  - `EventBus.gd` 新增 `signal icon_provider_registered(get_fn: Callable, has_fn: Callable)`（UI 层经此把图标解析器注入 UIManager 的通道）。
  - `ui_manager.gd` 删除 preload；改持有 `_icon_provider/_icon_has_provider: Callable` 槽位 + `_icon_placeholder_cache`（兜底占位，`get_icon` 永不返回 null）；`_ready` 连 `EventBus.icon_provider_registered`；`get_icon/has_icon` 委托给已注入的 Callable，未注入回退占位。
  - `scenes/bootstrap/Bootstrap.gd`（组合根，scenes→scenes 依赖合法，非任何窗口主权）在 UIManager 就绪后 `EventBus.icon_provider_registered.emit(Callable(IconRegistry,"get_icon"), Callable(IconRegistry,"has_icon"))` 注入。
  - **未改 `icon_registry.gd`**（UI 窗口主权：可'使用'不可'修改'），仅由其持有方在运行时注入。
- **验证（决定性）**：`scan_deps.py --root` 全量 208 .gd / 205 边 / **向上依赖违例 0**（违例正式清零）；GATE1 零 SCRIPT/PARSE/COMPILE ERROR；GATE2 **55 套件/0 失败/0✗**（修正 `test_icon_registry.gd::test_uimanager_delegates_get_icon`：模拟组合根注入后再断言委托，不 emit 信号符合测试窗铁律）。
- **提交**：`fbcfa8b`[架构] + `c75b1a7`[changelog]。
- **需求②（工作室 UI 再重构）**：用户要求把协同启动卡提示词做成可点选复制的列表（下拉/或左侧隐藏栏自动滑出），并把待办清单置为优先展示。
- **做法（tab-coord 重构）**：待办清单作为主内容优先展示；协同启动卡口令收进「左侧隐藏栏」`#coordRail`（鼠标靠左边缘 hover 自动滑出，移开自动收起），点窗口名即复制对应 AI 窗口口令（含通用模板）；`loadStartupCard` 改写为渲染点选列表 `startupCardList`，复用 `copyText` 反馈。游戏侧零依赖。
- **重打包（桌面 exe 生效）**：`--clean` 重打包 `工作室专业调教.exe`（build 16:06，8.58MB，md5 `b03c0324f5aa34d8a56534309773c0d2`），同步 5 处副本（tools/desktop_studio/dist、tools/dist、dist、D:/武侠游戏/Desktop、C:/Users/Administrator/Desktop）md5 一致。
- **提交**：`752d6b3`[工作室] + `c75b1a7`[changelog]；推 Gitee。
- **主权边界**：全程未碰 preset_*.json（战斗窗）、SettingsScreen.gd、UI 放权窗未交文件；架构改动仅在 `EventBus/ui_manager/Bootstrap`（基础层+组合根，AI-架构主权）。

## UI 放权 Phase 3：读档弹窗几何外置（2026-09-02，AI-UI 窗口）
- **范围**：UI 放权路线图 Phase 3 下一站——把读档界面「存档卡片居中列」几何从 GDScript 硬编码外置为 JSON，工作室新增「UI 模块 → 读档弹窗」数值编辑页（对标已落地的设置弹窗）。
- **游戏侧**（`scenes/ui/screens/save_load/SaveLoadScreen.gd`）：新增 `SAVELOAD_LAYOUT_PATH`/`SAVELOAD_LAYOUT_DEFAULT` const + 静态 `_load_layout_config()`（FileAccess+JSON，缺失/写坏静默回退默认）；`_build_content()` 末调用 `_apply_layout()`——将 `_header`/`_scroll` 锚点 0.5 + 偏移 ±col_w/2 实现水平居中列，`_scroll` 垂直范围按 `margin_y_ratio`/列高，`_list_container` 的 VBox separation 应用；`_add_card()` 用 cfg 设 `card.custom_minimum_size`。默认值复现工程出厂 640 居中列，**零回归**。
- **数据文件**：`data/configs/ui/skin/saveload_screen.layout.json`（6 字段：content_max_width/height、margin_x_ratio/margin_y_ratio、card_min_width/height）。
- **工作室**：`studio_core.py` 加 `_SAVELOAD_SCREEN_LAYOUT_DEFAULT()`/`_SAVELOAD_SCREEN_LAYOUT_KEYS`/`_saveload_screen_layout_path()`/`saveload_screen_layout_get/update`（含备份/裁剪/默认值合并）；`studio_server.py` 加 `GET/POST /api/saveload/layout`；`index.html` 加「📁 读档弹窗」下拉项 + `tab-saveload_screen` 6 数值框 + `saveloadScreenLoad/Save/Reset` + tab 分发。手册补 §2.7 + 数据文件对照表行 + 快速上手第 7 条。
- **验证**：GATE1 零 SCRIPT/PARSE/COMPILE ERROR；GATE2 套件 通过 55 · 失败 0 · 0✗；`security_selftest.py` 15/15 PASS；`py_compile` studio_core/server 通过。
- **打包交付**：按 SOP 清 8765 旧进程（PowerShell `Stop-Process -Force`，netstat 兜底）→ `pyinstaller 工作室专业调教.spec --clean --noconfirm` → 同步 7 副本（工程内 dist×2 + tools/dist + 桌面 + D:\工作室专业调教 + D:\studio_push_tmp/dist）md5 一致 `482b998114d8ca3adc7136287881c782`；curl `GET /api/saveload/layout` 返回默认、`POST` 持久化生效、再 POST 默认还原（工作树干净）。
- **关键认知（已核）**：index.html 由 exe 从磁盘就近加载、**未打进 bundle**，故前端改 index.html 不改变 exe md5（482b9981 与前次重打包一致属正常）；真正影响 md5 的是 studio_core.py/studio_server.py 等被打包的 .py。
- **提交**：`20cbfe5`[UI]（6 files：游戏侧 gd + json + 三处 studio + 手册）+ `6930ac4`[changelog]（change_log 登记 读档弹窗几何外置 20cbfe5）。
- **多 AI 纪律**：精确 `git add` 本人文件，未碰战斗窗 `preset_10x10.json`/`preset_12x12.json`（仍保持他窗 modified 未动）。

## 中期#1 扩展 Module Card（战斗/商店/锻造/炼药/门派，2026-09-02，AI-架构窗口）
- **背景**：中期#1 前段已收口背包/对话/结缘/UI/存档 5 张卡（提交 12c7e2e/41930db）。本段把剩余 5 个核心服务模块补成卡，完成中期#1 全量 10 张。
- **做了什么**：`docs/模块卡/` 新增 战斗.md / 商店.md / 锻造.md / 炼药.md / 门派.md（5 张），均据真实代码沉淀：
  - 战斗：`CombatService`(适配层)+`CombatCore`(纯逻辑内核，可单测) 双服务；`preset_10x10/12x12.json` **明确标注不归战斗主权**（属架构窗/工作室后台战棋预览治理域）；BUG-11/12 已修、BUG-18/19/22/32 设计层登记。
  - 商店/锻造/炼药：三服务皆 **无状态、不进档**，产出/扣减一律走 `InventoryService` 事务防资产损失 P0；BUG-04(商店经济漏洞)/BUG-08(锻造枚举) 已修；BUG-15 补测。
  - 门派：`SectService` **唯一实现 ISaveable 的经济/社交类服务**（有持久状态 current_sect_id/reputation/rank）；BUG-07(缺道具枚举) 已修。
  - 跨模块共性：EventBus CMD 信号空壳（仅 cmd_start_combat 真接线）、BUG-16 读取型 DI 登记、`ui_manager→icon_registry` 唯一真实架构违例（在 UI.md 已记，本批卡相关处补引）。
- **索引更新**：`docs/模块卡/README.md` 索引表扩至 10 行（含层定位/主权窗口）。
- **零回归**：纯文档沉淀（docs/），未碰任何 .gd/.tscn/他窗主权文件；pre-commit 因无 .tscn 改动跳过静态扫描。
- **提交与上库**：`docs/模块卡` 精确 add → 提交 `[工作室] 中期#1 扩展 Module Card：战斗/商店/锻造/炼药/门派` → `change_log.py add` 登记 → commit changelog → 推 Gitee（免交互令牌）。

## UI 放权 Phase 1（设置/读档/HUD 布局外置，AI-UI 窗口）
- **路线**：把可参数化的 UI 几何从 .gd 硬编码外置成 JSON + 工作室可视化编辑器（拖拽）。已落地三站：
  1. **设置弹窗** `settings_screen.layout.json`（`SettingsScreen.gd` `_load_layout_config`/`_fit_panel`）→ 提交 `cb0f3ea`。
  2. **读档弹窗** `saveload_screen.layout.json`（`SaveLoadScreen.gd` `_load_layout_config`/`_apply_layout`，640 居中列几何）→ `20cbfe5`。
  3. **HUD 四面板** `hud_layout.json`（`core/ui_layout.gd` `hud_default_pos`+`hud_default_scale`；四面板 `_init_drag`/quest_track 自带实现 apply 位置+缩放；游戏内右下角拖角缩放手柄）→ `8cb2182`。
- **HUD 缩放设计铁律**：用 **uniform scale**（不改内部布局比例、字/条不拉变形），数据模型加 `scale` 字段（夹取 0.6~2.5）；绝不用非均匀 w/h 缩放（会把数值条/字拉变形）。游戏只读取入+按屏等比缩放坐标+按 scale 缩放面板；文件缺失/写坏自动回退 scale=1，零破坏。
- **工作室三站共用技能**：studio_core.py 读写器 + studio_server.py GET/POST + index.html 拖拽画布（位置拖 + HUD 加右下角缩放手柄）。HUD 缩放手柄 JS：拖右下角 → 改 `scale` 字段 → `hudRender` 按 `base*scale*pxPerRef` 预览。
- **验证（全绿）**：GATE1=0 错误；GATE2=55 套件/0 失败/0✗；security_selftest 15/15；PyInstaller `--clean` 重打包 exe，7 副本 md5 一致；curl 实测 GET/POST（含 scale 往返）后 POST 还原默认保树干净。
- **提交纪律（多 AI 协同）**：`git config user.name AI-UI` + 精确 `git add` 自有文件，战斗 preset_10x10/12x12.json 不碰；`change_log.py add` 登记；双闸门 + 安全自检 + 重打包 SOP 全过才交差。

## 隐性BUG核验：notify_quest_track_changed 刷新（派单 08ec940f5674，AI-UI 窗口）
- **现象**：EventBus 已新增 `notify_quest_track_changed`（quest_service.accept/turn_in/reset 三处 emit）+ HUD 任务追踪已订阅，但派单仍 open（协议层未闭环），疑「刷新不全」隐性问题。
- **结论**：**刷新完整，无隐性 BUG**。核验方法 = 新增 `test_quest_track_full_lifecycle_refresh`（走真实 `QuestService.accept/turn_in/reset` + `quest_objective_updated` 链路，断言 接取→出现、进度数字 1/1 实时刷新、交付→消失、reset→清空）。GATE2=55 套件/0 失败/0✗；GATE1=0 错误。
- **闭环**：`docs/backlog.json` 该条 status open→resolved；`docs/接管日志_UI模块_2026-08-29_未完成项.md` 派单 08ec940f5674 open→closed；提交 `9ef5ebd` + changelog。
- **可复用套路（隐性BUG 派单处理）**：这类「协议未闭环/疑似刷新不全」派单，优先**写全生命周期集成测试走真实 API 链路**而非只看代码 → 跑 GATE2 实测；若绿则直接闭环派单（更新 backlog/handoff 状态），无需改业务代码。比盲目改代码更省事、更稳。

## 血泪坑：Gitee 推送弹「同意/授权」浏览器页（本仓库已根治，2026-09-02）
- **现象**：`git push origin master` 卡住无输出（数十分钟），用户端弹 Gitee 浏览器「令牌授权 / 同意」页，必须手动点「同意」推送才过。用户明确要求「所有操作自己同意，别再让我点」。
- **根因**：本机 PortableGit 配了凭据助手 `git-credential-manager.exe`（GCM，`~/.gitconfig` 的 `credential.helper=!"...git-credential-manager.exe"` + `credential.https://gitee.com.provider=generic`）。GCM 拦截 HTTPS 鉴权、不走 URL 内嵌令牌，转而拉起浏览器 OAuth 同意页 → 挂起等点击。
- **根治（已落地）**：给本仓库设 `git config --local credential.helper ""`（中性化 GCM，仅本仓库生效、不动全局）。此后推送用 `GIT_TERMINAL_PROMPT=0 git -c credential.helper= push origin master` 直接走 **URL 内嵌令牌基本认证**，零弹窗、零交互。`git ls-remote` 实测秒回（LSR_RC=0）。
- **结论**：远端 HEAD == 本地 HEAD（`3dcd73a8`）；用户那次手动点「同意」时卡住的推送其实已成功传完，后续再推即 `Everything up-to-date`。本轮 5 笔提交（fbcfa8b/752d6b3/c75b1a7/b985554/cab7b5a）已全部上库。
- **复用规则（铁律）**：本仓库任何窗口/任何 AI 推送 Gitee，**一律加 `GIT_TERMINAL_PROMPT=0 -c credential.helper=`**（或直接依赖已置空的本地配置），绝不再触发 GCM 浏览器同意页。改全局 gitconfig 去掉 GCM 风险大、用户是小白，不采用；本地置空最小侵入且对本仓库所有窗口生效。

## UI 模块按『改什么』重组（前端，AI-UI 窗口）
- **任务**：工作室 UI 模块下拉按「改什么」重组（屏选择器 + 3 类：换皮替换·零风险 / 布局尺寸·低风险 / 视觉特效·需护栏）+ 统一三步流 + 每模块小白引导三行（提案_UI放权方案.md §五）。
- **关键发现**：动手前该重组已由另一 AI 提交 `752d6b3 工作室 UI 重构`（index.html 已是四分组导航 + JS 引导/三步流 + 即将上线占位），HEAD 与我本欲实现的版本逐字节一致 → 我的 Edit 成 no-op，index.html 无新 diff。
- **本提交净贡献**：① 使用手册补「新导航说明 + 统一三步流 + 小白引导三行」段落（`58ebd0d`）；② 重打包 exe（`pyinstaller --clean`，md5 `b03c0324`，7 副本一致）使重组真正可下发；③ curl 验证新导航标记（屏选择器/换皮替换/视觉特效/统一三步流/即将上线×7）已生效；④ `node --check` 确认 index.html 内嵌 JS 语法通过（避免整页 JS 崩）。
- **经验（多 AI 并行坑）**：动手前先 `git show HEAD:<file> | grep <独有标记>` 确认是否已被其他窗口落地，避免重复造轮子。本环境 Edit 写真实盘、Bash git 读沙箱快照，二者可能短暂不一致——以 `git hash-object` 比对工作树与 HEAD 为准。

## HUD 战斗中是否常驻（设计拍版，AI-UI 窗口）
- **用户拍板（直接定）**：HUD **绝不**在战斗中常驻——战斗使用独立的战斗 UI（BattleScene / TacticalBattleScene 自带），与 HUD（状态卡/任务追踪/右上菜单/技能栏四面板）互斥。
- **修复**：把"开战即卸载 HUD"作为**显式策略**收口在唯一战斗入口 `GameManager.start_battle()`（首行 `UIManager.unmount_hud()` + 明确注释）。此前仅依赖 `TownScene._exit_tree` 在"离开城镇"时才卸载，任何非经城镇退出的开战路径都可能残留 HUD（视觉重叠风险）。
- **回归测试**：新增 `tests/unit/test_hud_not_in_battle.gd`（2 项：开战卸载 HUD / 二次开战仍无残留）。`start_battle` 内 `_deferred_change_scene` 是 await 延迟切场景，单元测试同步跑完即 quit，不会真正切场景，故测试安全无副作用。
- **不变量**：TownScene._ready 挂载 HUD、return_to_town→TownScene 重载重新挂载；战斗全程无 HUD；回城后自动恢复。待回城后 HUD 冷却读秒等仍按原逻辑生效。
- **验证**：GATE1 零错误；GATE2 套件 通过 55·失败 0·0✗（预计 +1→56 套件，待跑）。
