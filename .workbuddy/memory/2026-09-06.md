# 2026-09-06 工作日志

## 【里程碑】施工启动：FROZEN 批准 + Phase1 落地（0cbc1ca）
- 用户 2026-09-05 批准 02~18 FROZEN CANDIDATE 升 FROZEN（当时暂停）；2026-09-06 指令「按宪法与 01~18 逐一落地」→ 补办 17 份状态行 FROZEN + ACR-0001 转 APPROVED + 新增 APPROVAL_2026-09-06 批准记录（含 Phase1 范围与遗留债登记）。
- Phase1 五项落地（全部按蓝图 §4 分档，python 断言写入零失败）：
  1. 18图RH-1：project.godot 补 application/config/version="0.5.0"（patch_manager 从此拿真值）。
  2. 18图RH-3：GameConstants.SAVE_VERSION 死常量退役（零消费复核后删除）。
  3. 13图SV-3/P-S1：SaveManager 公开 register_migration({from,to,step:Callable})；patch_manager 弃 has_method 死探测改直连，manifest.save_migration 旧字符串格式 ERROR 响报（禁静默）。
  4. 16图CP-1：data/configs/_retired_ids.json 建立（首登记 npc_town_npcs 数据源标识；git 历史核实 town_npcs 一直为空表、实体早已迁区域真源）。
  5. 16图CP-2b：tools/id_baseline.json 冻结 ID 违例 280 处/44 文件（03§3.3 冻结正则宽口径，比蓝图 202 更保守；基线外零容忍/基线内只减不增）。
- 附带：patch_manager「规范§4.7」/error_handler「§4.2.3」旧编号注释清理（18图Phase1「修注释」）。
- 门禁：verify_all 六门禁全绿（GATE1 零解析 + GATE2 64 用例失败 0 + GATE5 双写防线 0）；change_log 5 条留痕 + 1 份变更通告；commit 0cbc1ca 已推 GitHub（d4b4c05..0cbc1ca）。
- 遗留债（记入 APPROVAL 文档）：MainMenu/Bootstrap 读版本真源=Phase2；id_validator 挂 GATE06=CP-5 Phase2；studio 工程标记仍探测 town_npcs.json 存在性（studio_core.py L118/L129）随工作室域整改。
- 教训复用：全部修改走 python 断言脚本（今日 0 失败），继续规避 Edit 长中文落盘问题。

## 【Phase2 收官】RH 契约落地 + id_validator 挂 GATE06（全绿）
- 18图RH-1：MainMenu/Bootstrap 版本真源化（读 ProjectSettings version + provenance 派生 Build 日期，弃硬编码 VERSION_TEXT/BOOTSTRAP_VERSION，双渲染点收敛）。
- 18图RH-2：GameLogger Boot 行注入 provenance（exe 同级 provenance.json 优先→res:// 回退，空则 dev 态标注）；轮转 10MBx5→5MBx3。
- 18图RH-6：error_handler FATAL 弹窗追加「当前版本+日志路径（反馈附上）」两行。
- 18图RH-3/RH-7：export_presets.cfg 双预设（Win x86_64+Android arm64 占位，keystore 归用户）+ build_release.py 出包编排（provenance 八字段/data 指纹/门禁编排/产物命名；GATE40+/golden 以 [PENDING] 显式标注）+ 安卓导出说明.md。
- 16图CP-5：id_validator.py 三检（CP-R01 基线外零容忍/CP-R02 退役名单/CP-R10 同域唯一+分片行 id 唯一）；挂入 verify_all GATE06（ref_index+id_validator 双校验合并判定）。
- 关键定性（首跑 13 项违规定性为误伤，零真重复）：①键上下文定定义/引用——键 id=定义候选，*_id 与 *_ids=引用（演员表 battle_id/dialog_id、分片 speaker_id、enemy_ids 出怪表全是引用面）；②分片按根 {id,lines} 形状识别——根 id=dlg 域定义（按 NPC 命名是惯例，与演员表 npc 域跨域允许），行 id=分片内唯一性；③bond/relations.json=按 NPC id 键控的社交档案附挂表（BondService 消费），登记 ATTACHED 按引用处理；④_map_index 与区域 index 用 region_id 键，天然不进定义面。
- 基线校正：280→263，剔 17 条空串伪迹（基线生成未过滤空值），_doc 留痕；校正后零新增/零收敛残留。
- 踩坑：同文件多处 Edit 并行发送=快照竞态互相覆盖（ATTACHED 定义被冲掉只剩使用点）——同文件编辑必须串行或走断言脚本。
- 门禁：verify_all 九门禁全绿（GATE1 零错误/GATE2 64 用例 0 失败/GATE6 双校验器通过）；change_log 5 条留痕；Phase2 批次已提交推送。
- 遗留：GATE40+ Benchmark 与 golden 对未落地（build_release [PENDING]）；SV-3 Phase2（golden 生产器+P-S3 拒读+P-S5 SaveHeader）；ACR-0001 Phase2 主战场=09/10/11 事务根治。

## 【续轮 9】Phase2 主战场：09/10/11 三域资产事务根治（60d8ea6 全绿已推）
- 用户指令「先修P0」。实扫定性：基础设施已齐（try_consume/query_add/can_add/add_instance/iid 发号器），缺陷在调用方——7 项真凶：forge 忽略 try_consume 返回值+预检含锁定；quest turn_in 先置 TURNED_IN 后发奖（满包蒸发且无补交入口）；add_items 逐项 can_add 竞争盲区；facts.add_silver 直改不发事件；shop buy 忽略 spend_money 返回；romance 循环扣+手工回滚；equipment 回滚丢实例身份。
- 修复 11 处：inventory 新增 can_add_batch（按栏聚合槽位=先吃堆叠余量再 ceil 折算新槽+全局重量预算；空批次=true）+ add_items 改走它；forge try_consume 单一裁决+can_forge/describe_inputs 解锁口径；turn_in 前置预检（exp/silver/items 同批原子暂缓）+retry_completed_turn_ins（_retrying 防重入）+GameManager 连线 inventory_item_removed；facts.add_silver→add_money+can_add_items；shop spend_money 双保险；alchemy can_refine 解锁口径；romance 聘礼 try_consume 原子扣（空聘礼短路）；equipment 回滚恢复 _equipped_inst[slot]。
- **两处回归实录（高复用）**：①try_consume 空数组=false——romance 空聘礼必须 `not mats.is_empty() and not try_consume(...)` 短路，否则无聘礼 NPC 求婚全挂（36 项测试失败）；②can_add_batch 空批次语义=true（rewards.items=[] 的 nv_quest_guard 曾因此永远无法交付）。二者语义相反，勿混。
- **测试套件间污染实录**：phase2_pilot 塞满 30 把铁剑(105 重)+after_each 只复位力量不清背包→后续 quest 链路套件处于超重状态；修法=污染源清尾（after_each reset）+受害者清头（用例开头 reset）双保险。
- **数据发现**：forge_iron_sword 配方的 material_iron_001 在 materials.json 不存在（死配方，ID 基线 263 存量悬空之一）——恰好构成 forge「材料不可用」天然测试链路；修复数据=内容决策留给用户。
- 战斗用药快照方案（skill 的 player_use_item）因产品决策「战斗禁用药」（2026-09-02 NOT_ALLOWED_IN_BATTLE）不适用，未实施。
- 测试新增：forge 缺料不产出不误扣/can_add_batch 竞争最后空槽+跨栏重量预算/满包交付暂缓+腾格补交+银两原子（q_sect_join_001 主栏武器奖励构造；ORE max_stack=99 堆叠塞不满栏的教训）。verify_all 九门禁全绿+套件 64 过 0 失败；change_log 6 条+变更通告 1 份（GameManager 共享地基连线）；60d8ea6 已推 GitHub。

## 全项目严苛审查（七人评审团，用户委托）
- 宪法 6791 行逐条精读（零遗漏）+ 01 图全文 + 18 图 DoD 逐张对照 + verify_all 实跑 ×2 + 六维扫描，报告：`docs/全项目严苛审查报告_2026-09-06.md`。
- **重大发现①GATE2 flaky**：同代码两次 verify_all 一次 ✗=19(SaveManager 族)一次 65·0 全绿（总数 65 守恒），单独跑存档套件全绿→根因指向 GATE1 缓存自愈与 GATE2 时序；门禁可信度问题非代码回归。
- **重大发现②P-S2 漏修（P0）**：SaveManager L68 register_saveable 仍不查 key 撞车（13 图 P0 族·映射 Phase1 但 Phase1 已收官）→ 注册期 key FATAL 一行级可补。
- 新违例实锤：romance L435 裸 randf（17 DoD-1 被破坏）+ L437 直读系统时间（宪法§79）+ L585 手写 FileAccess 静默解析 + relationship L23/L104 直读 romance.spouses 内部字典（RULE007/008）；combat try_escape rng==null 回退残留（17 DoD-2）。
- 进度底账：18 图 = 3🟢(01/09/18) + 8🟡(02/03/04/07/10/11/12/16/17) + 6🔴(05/06/08/14/15 未开工含 15 部分)；GATE21~32 零物理化确认（verify_all 无 2x/3x/40 槽）。

## P1 批次实施（架构文档窗口，2026-09-06 晚）
- 用户指令「P1先搞，再搞P2」。实扫 13/17 图契约：SV-1~4 开放问题零阻塞，直接实施。
- **#40 SV-3 Phase2 全落地**：P-S3 未知版本拒读（注册表无步骤=不是我们的档，禁盖章）；P-S5 SaveHeader 五字段+checksum（正文 SHA-256 写算读验，**JSON 归一化器统一读写口径**——直拍内存 dict 因 Vector2 序列化往返不等曾致 _verify_file 拒掉刚写的 tmp，回归实录）；P-S6 迁移注册表链走（禁手写 known，内置 1.0.0→1.1.0 正式步骤）；golden 生产器 tools/golden/（场景模式产出 Input/Expected 夹具，人审冻结）+golden 对测试；新建 test_save_header（SV-R01）。
- **P-S2 P0 补课**（背包窗口发现）：register_saveable 注册期 key 撞车拒绝+FATAL 日志。
- **#41 GATE40+ 落地**：bench_main.tscn 场景宿主（--script 无 autoload 的实锤绕法）跑 save_roundtrip/combat_turn，run_benchmarks.py 测 boot 墙钟+五字段基线校验+双 PASS；实测基线 save=1.8ms combat=0.09ms boot=815ms ±50% 已登记；verify_all 新增 --tier performance（物理槽 GATE40）；build_release [PENDING] 两项升级强制。
- **工程实锤**：①基准/生成器宿主必须场景模式；②benchmark 场景内 240s 看门狗防脚本错误挂死门禁；③「flaky GATE2」疑似=多窗口并行改 SaveManager 的中间态被另一窗口跑门禁撞上（本窗口收尾时两次全量 verify_all 均 65·0 全绿）。
- 验证：verify_all 九门禁全绿 ×1 + GATE2 单独 ×3（65 过 0 失败）+ GATE40 tier RELEASE PASS。未提交项：背包窗口的审查报告 docs/全项目严苛审查报告_2026-09-06.md 与 .workbuddy/backups/ 留给归属窗口。

## 审查整改批次落地（6a45c99，AI-审查窗口，03:10）
- 按审查报告 §五 顺序执行：D-01 由架构文档窗口 ac5121e 已修（register_saveable key 撞车拒注册，本次核实后销项）。
- D-02/03：romance_service **五处**（比报告多 2 处：debug_make_spouse.wed_day、_birth.born_day）Time.get_unix_time 全改 _game_day 游戏日计数器（advance_days 喂入+save/load/reset 三处持久化）；_day_key() 返回 _game_day。
- D-02 续：受孕 randf→SeededRNG 按日+序数决定论 seed=hash("romance_conceive:v1:日:NPC:序数")；配额 randi_range→hash("romance_quota:v1:日:NPC")。同输入恒同结果，无需存 RNG 状态。
- D-04：_special_portrait_cfg 手写 FileAccess 改 JSONUtil.load_json（file_exists 前置保留——预留空表合法不告警）。
- D-05：romance 新增 get_spouse_record(npc)（duplicate 副本）/get_children_brief() 查询 API；relationship_service 直读 spouses/children 内部字典 4 处清零。
- D-06：combat try_escape 删裸 randf 回退分支，RNG 缺失 push_error+fail-safe 拒绝逃逸。
- D-07：verify_all.py GATE2 防抖——首跑红自动复跑一次，复跑绿打 FLAKY-RECOVERED 判绿放行；_parse_gate2 辅助函数抽出。
- 测试：test_romance_service 新增 3 用例（游戏日时间源/存档往返/受孕决定论重放），套件 27 项全过。
- 验证：verify_all 九门禁全绿（套件 65·失败 0）；GATE1 零解析错误；pre-commit 三钩子过。
- D-08/09/10 已 handoff 派单：D-08→AI-UI窗口（EventBus 两条 Dictionary 信号存量 79cdf457f41e）；D-09/D-10→AI-架构文档窗口（4cfe89586f88）。
- 踩坑记录：changelog 文件名是 docs/更改日志.md（非"变更日志"）；单跑测试套件禁用 --script 模式（无 autoload，项目已知坑），须走 res://tests/unit/run_all.tscn 场景模式再 grep 用例名。

## 追认批次落档（架构文档窗口，2026-09-06 凌晨）
- 用户指令「按各图 §7 推荐整批追认」+「需要修复的都修复，严格按宪法，不要偷懒」。
- **核实**：D-02~D-07 已由审查窗口 6a45c99 全部整改（romance 五处系统时间→_game_day、受孕/配额 SeededRNG 决定论、JSONUtil 统一 IO、get_spouse_record 查询 API、try_escape fail-safe、GATE2 复跑防抖+3 回归测试）——代码侧无欠账，未重复修复。
- **落档**：①建 docs/architecture/ADR_INDEX.md 统一登记表（ADR-0002=C 基线+06AC-1 扩展 B+16CP-2 联动；0003=延 Phase5；0004=保留 .gd；宪法§23A 预占题不占号自 0005 顺延；patch manifest Callable 域边界备忘；forge_iron_sword 处置备忘）；②17 图开放问题逐图批注已追认（原文保留）；③04-T1 批注；④更改日志登记。
- **实锤①**：id_validator CP-R02 退役名单检查无基线豁免（L122 独立于基线判断）——存量引用未清零前登记退役名单 = GATE6 即时红；forge_iron_sword（materials.json 无 material_iron_001，配方恒 can_forge=false）按 10 图 EC-R07 Phase4 统一迁移，不提前动内容。
- **实锤②**：D-08/09/10 属 P2 排期债（GATE21+ 物理化随 Enforcement 矩阵、buy/sell 0-C 收编依赖 02 图 Transaction Runtime），审查窗口已 handoff 派单，不抢做以免越过施工图排期。

## 缺陷登记全清单收官（fc2debd，AI-审查窗口，03:25）
- 用户下发 D-01~D-10 全清单令"严格按宪法全修"。逐项状态：D-01~D-07 上轮已修（6a45c99/ac5121e）grep 复核在库；D-08/D-09 本轮落地；D-10 按"AI 不自决架构"不改代码、handoff 派单 9d62ece1bdc9 随 02 图 Transaction Runtime 收编。
- D-08 落地：tools/arch_linter.py 物理化 GATE21/22/32 三槽（01图§91 E3/§93/§127），verify_all 默认十二槽全跑。GATE21=0-B.12 Dictionary/Array 裸信号载荷（存量5条退役禁新增）；GATE22=Forbidden API（core业务层白名单豁免 utils/json_util+game_logger、patch_manager、resource_manager、ui_layout/ui_skin/ui_vfx，其余禁 IO/JSON/随机/时间；services/autoload 禁系统时间+全局随机，存量8条分类：存档/日志时间戳=合法元数据豁免、bond t/sworn_day/acquired_time=业务字段待整改、镜头抖动=表现层豁免）；GATE32=EventBus 84信号签名指纹冻结。
- 关键实现细节：基线指纹必须"去注释签名"（整行含注释会在改注释时误报 GATE21/GATE32——实测踩过已修）；自测方法=临时写违例文件/临时加信号→exit=1→删除恢复→exit=0。
- D-09 落地：EventBus item_used/grid_highlight_update 代码内【RETIRED·0-B.12·禁新增同族】标记 + GATE21 基线冻结。
- 意外发现：架构文档窗口并行工作中——用户已整批复核追认 02~18 图开放问题（各图+2行追认标注+新建 ADR_INDEX.md），未提交。我窗提交精确避开他窗文件。
- 遗留：GATE23~31 需 ContractRegistry/TransactionRuntime 等未建基建维持 E0/E1；bond/sworn/inventory 业务字段读系统时间（8条中的5条）待后续批次。

## 【Phase3 批次1】02图 Phase B 契约骨架落地（5a71907，架构窗口，凌晨）
- 用户指令「开工」→ 启动 ACR-0001 Phase3。读图定案：批次1=02图 O-4 契约骨架+契约测试（零回归不动业务），TransactionRuntime 与装配收敛留后续批。
- 落地 core/kernel/ 13子目录24契约文件（§1~§10 全量）：EntityId/ErrorCode15常量/OperationError/Result家族6类+ValidationViolation/Command/Query/DomainEvent(Phase枚举)/Condition+GameFacts+Effect+Rule(@abstract)/TransactionContext(5态状态机)/MutationRecord/UndoStrategy/MutationContext/Repository/GameClock/RandomProvider。
- 撞名处置：services/quest/facts.gd 适配器 GameFacts→ServiceGameFacts（kernel 冻结名归 02图§6 契约；引用面仅 quest_service×2+dialogue_service×1+condition.gd 注释，纯改名零行为变化）。
- 编译必需补充（ADR_INDEX §4 四条备忘）：①MutationContext 02图§6引用未定义→kernel/transaction/ 补最小数据面；②TransactionContext 补下划线内部推进接口 _begin/_mark_*（合法迁移表在注释）；③Query/DomainEvent/Result 子类按 Command 模式补最小 _init；④kernel 临时落位 core/kernel/（GATE22 扫 core/ 自动覆盖 K-R 禁API，顶层迁移随 Phase5 同 ADR-0003 原则）。
- 探针先验：--check-only 验 @abstract 语法通过（报错只是 check-only 不带 autoload 打到旧 GameFacts 依赖链）。
- 验证：--editor --quit 重建类缓存→GATE1 零错→GATE2 66套件0✗（新 test_kernel_contracts 15项全过）→verify_all 九门禁+GATE21/22/32 全绿。
- 协同实锤：提交时存档窗口并行收口 b62f87a(SV-4/SV-6)——提交前重查 git status 确认混合 WIP 已被其收走，日志 diff 只剩我一行，精确 add 31 文件零卷入；tools/id_baseline.json±2476 行是他窗在途 WIP 未碰。
- 下批指针：TransactionRuntime+Journal（11 失败路径 Transaction Test→GATE26/27/28 物理化）→ 装配收敛 18 Autoload（ACR-0001 定性最高风险，需单独 ADR+Write Lease+逐 Service 迁移）。

## AI-审查窗口：18图清单未落地项整批落地（b62f87a→本轮5批）
- **13图 SV-4/SV-6**（b62f87a）：register_saveable 强类型收 ISaveable+get_load_after 依赖声明+Kahn 拓扑加载序(环拒读)+缺 key WARNING；GameState 经 _SaveBridge 组合桥注册(Node 单继承限制,key 不变零迁移)；json_util.save_json 原子写；新增拓扑/环/缺 key/调用序 4 用例。
- **GATE22 去注释修正**（随 b62f87a）：kernel 施工文件铁律注释误伤→匹配与指纹去注释(与 GATE21/32 同口径)；基线同位置换指纹 4 条零新增。
- **03图**（fc2…后续提交）：ADR-0002 二级形态(id_validator extra_patterns 区域缩写 nv|mt+域段封闭正则,基线 263→234 出清 29 条,nv_qg_demo 类缩写+非标域段保留)；C-R04 schema_guard 三段 SemVer+三处 1.0→1.0.0；C-R05 结构指纹基线(结构变更不升版 FATAL,演练验证)；ADR-0004 Schema 类摘要(schema_digest_baseline.schema_classes)；C-R14/15 pack_manifest_validator 基座；GATE6 四检。
- **16图**：CP-R06 三同铁律 region_validator(注册↔分片双向/传送悬空/自引用漂移;空壳区域基线豁免 8 条前瞻登记)；CP-R09 测试资产计数通报(demo/tactical 基线冻结禁新增)；GATE6 五检。
- **12图 QD-R07**（332446a）：任务事件分相队列——两回调入队+call_deferred 帧末冲刷,冲刷循环内新事件继续消费(P-Q2 链不递归不丢)；闸门双标志分离(共用一标志时挂接期同步冲刷被误弹回的回归实录)；load/reset 清队列；test_quest_phases 五用例(含 P-Q2 链复现)；既有测试两处适配冲刷。
- **04图**：GATE41 arch_validators(dependency 禁引矩阵 core 地基纯净/data 禁引上层/services 禁引 scenes/生产禁引 tests(T-R05)+环检测(去注释/自环跳过/环签名去重——首跑 134 误报教训:层方向模型搞反+注释路径误伤))+module_scope(Double 只准住 tests/doubles/ T-R04)+test_ 命名(T-R02)+state_owner REPORT(T-4 观察期)；01§92 七校验器 6/7(changed_file_scope 多 AI 阶段不越界)；Q-1 registry 初版 84 信号导出；Q-3 tests/doubles/ 基座；verify_all 十三槽。
- **验证**：每批 commit 前全量 verify_all 全绿；TestBase expect_eq 只收 int(String 断言用 expect(a==b))的坑再确认。
- **余量指针**：13图 SV-2 二段式 DTO(红线单独批)；12图 Effect 注册表五类收编(QD-2 单独批)；02 kernel 他窗在途(TransactionRuntime→GATE26/27/28)；装配收敛 18 Autoload 需单独 ADR。

## 【核对】AI-审查窗口：D-01~D-10 整改收口指令重复下发→逐条核对销项（零重做）
- 用户再次下发 D-01~D-10 收口清单，按「调前先查」判定为重复指令，本轮=核对验证既有整改完整性+留痕，非重做。
- 逐条证据（代码+git+change_log 三重）：D-01→ac5121e（SaveManager L71-79 撞车拒绝+test_register_duplicate_key_refused）；D-02/03/04/05→6a45c99+7ca0a3f（romance 全走 WeatherTimeService.get_day 真源/SeededRNG 决定论/JSONUtil IO/relationship 查询API 9个公开）；D-06→6a45c99（combat_service try_escape fail-safe，combat_character L72/78 残留属 17 图「随 11 AB-6 Phase2~3」排期项非 D-06 范围）；D-07→6a45c99（GATE2 防抖 FLAKY-RECOVERED）；D-08/09→fc2debd+b62f87a（GATE21/22/32/41 四槽在位）；D-10→随 02 图他窗 5a71907 在途。
- change_log 留痕：L370（D-02~06）/L371（D-07）/L373（D-08/09）。
- 全量 verify_all 十三槽（1~9+21/22/32/41）复跑全绿「可以提交/合并」，GATE6 五检含 region 三同 8 空壳基线豁免，GATE41 state_owner REPORT 观察期不拦。
- grep 坑：relationship 服务在 services/bond/ 非 services/relationship/，先查目录结构再 grep。

## 【派工】AI-审查窗口：D-08/09/10 收尾派工×2（用户拍板「按宪法执行」）
- 派单1（5b058773b4b2→AI-UI窗口）：EventBus 两条 Dictionary 载荷信号（item_used L23/grid_highlight_update L34，GATE21 已 RETIRED 登记）0-B.12 真整改→ItemUsedPayload/GridHighlightPayload 强类型化；引用分布 9 处已摸排随单（inventory_service 发射4+test 1；tactical_battle_scene 发射5+battle_grid_node 订阅1）；联动必做=GATE21 基线出清 2 条 RETIRED+GATE32 指纹基线同步（合法变更路径）；主权提示（inventory=背包窗主权/battle=战斗窗主权，用户拍板 UI 窗统一收口）。
- 派单2（7b7399977562→AI-架构文档窗口）：下批次 P2 排期登记——静默 IO 存量排查（D-04 同族，产出 docs/ 清单归口 JSONUtil+GATE22 基线同步）可先行；buy/sell 0-C 事务收编随 02 图 TransactionRuntime（就绪前勿先行施工，GATE26/27/28 口径以 02 图为准）。
- 派单板=.workbuddy/handovers/（gitignore 不入库，免 commit）；change_log L381 留痕；待 commit=docs/更改日志.md+memory 两文件（本窗遗留一并收口）。

## 【核对+收口】AI-审查窗口：ADR-0002/0003/0004+RH-4+04-T1 五项裁决落地核对（用户授权「宪法内按想法来」）
- 调前先查：五项在 ADR_INDEX 全部已登记（0002/0003/0004=APPROVED；04 T-1~4 与 18 RH-1~4=§2 整批追认），本轮=落地完整性核对零重裁。
- 逐项证据：ADR-0002→id_baseline.json extra_patterns 二级正则锚明；ADR-0003→决策类延 Phase5 无需代码；ADR-0004→schema_guard.py RE_EXPORT 摘要+漂移检测；04-T1→04 图 §11 批注回写+十三槽 GATES 物理体现；RH-4→18 图 §11 批注（Phase4 最小可玩闭环）+export_presets 双预设+安卓导出说明.md 承接。
- 唯一尾巴：11/12 图头部「待 T-1 追认」措辞过期→python 断言脚本改「T-1 已追认 2026-09-06 见 ADR_INDEX §2」，全目录终验零残留。
- 教训复用：长中文文件改动继续走 python 断言脚本（0 失败）。

## 【开工批】AI-审查窗口：13图 SV-2 二段式 DTO 落地（Phase2 红线批，commit 本批）
- 写端：SAVE_VERSION→1.2.0，_build_save_data 每模块键包 {schema_version,data}；内置迁移 1.1.0→1.2.0（裸键包装，只包合法 dict 防手工改档）。
- 读端：_resolve_module_payload——未来模块版本拒读/低位沿 register_module_migration 模块迁移链（断链拒读）/legacy 裸键兜底/非 dict 拒读；checksum 按档内实际数据重算，迁移前验旧口径自洽零断裂。
- 登记制机器化：docs/contract/save_body_registry.json（12 键 DataContract，gen_save_body_registry.tscn dump 真实注册清单产出，含 fields+Owner）；register_saveable 白名单校验未登记 key 拒注册；_module_versions 覆盖表=未来模块升版落点。
- golden：对名动态跟随 SAVE_VERSION，新增 migrate_1_0_0/1_1_0_to_1.2.0 两对，历史对保留；test_save_migration 守门改双对+包装断言。
- 测试：+6 用例（包装/端到端 1.1.0 全链读回/未来版本拒/缺 data 拒/模块迁移链/未登记 key 拒），_isolate_saveables 扩白名单+版本表隔离。GATE2 67 套件全绿。
- 设计教训：模块迁移链可测性——_module_current_version 必须可覆盖（_module_versions 字典）否则链永不可达；字典字面量内联多行 lambda 缩进易碎，提局部变量。

## QD-2 Effect 注册表五类收编批（12图，用户「继续」授权）

### 事件：core/ 磁盘删除撞车（多AI协同实录）
- 施工中发现 core/ 整目录从磁盘消失（67 文件工作区删除态，无移动暂存副本；git HEAD 树与 origin/master 树完好）。
- kernel 窗（另一会话）已推 5a71907（Phase B 契约骨架，core/kernel/ 24 类），其树内 core/ 完好——删除非其提交内容，判定其在途操作中间态。
- 处置：停手观察 25s+30s 无恢复迹象 → `git checkout HEAD -- core/` 恢复 62 文件（command_dispatcher.gd 除外，本批退役）→ handoff 派单 ac5f9560a2bf 通报 kernel 窗。
- 教训：**他窗操作中间态时禁写文件/跑验证/提交**；共享地基消失先查 git 树三处（工作区/HEAD/远端）再定恢复；恢复无损因为索引=HEAD。
- 撞车实害：本窗 GameManager.gd 两段 Edit 被回滚（setup 调用段+声明区段），_init 里 `effects` 乌龙（注册进临时对象未赋成员）也是叠加混乱产物，靠 verify 兜住。

### 落地内容
- core/effect_registry.gd 新建：五类 kind 冻结（reward/story_flag/relationship/progress/presentation），注册期拒第六种=QD-R03 机器化；统一 handler 签名 func(payload,ctx)；apply_line 首冒号 DSL + apply_dict type/op 键双协议；未注册 op=FATAL=QD-R09。
- quest_service：reward 四类+progress 两类收编；**P-Q1 死命令修复**（quest_complete 直连 turn_in，has_method("complete_quest") 探测退役——QuestService 无此方法，端到端不可达）；_init 自足缺省注册表（**奖励发放禁依赖装配顺序**——隔离测试 new QuestService 不走 GameManager 装配，旧 _reward_handlers 自注册永不缺位，收编后必须保等价自足）+attach_effects 换绑共享域表。
- executor 重写：CommandDispatcher 退役（全仓引用归零，文件删除）；_apply_effect match 删除改 apply_dict；set_flag 键域白名单 QD-R06（story_/plot_ + nv_/mt_ 区域分片存量兼容——映射表 L131 nv_flag_* 重映射排 Phase4，一刀切会断新手村剧情）；sfx 去 ResourceLoader 直查（QD-R10，缺失检测归 AudioManager 内建）；shake 只产指令（P-Q10 收口）。
- EventBus +screen_shake_requested（表现指令通道，GATE32 基线 84→85 收编 --fix-baseline）；GameManager 订阅执行相机 Tween（表现自由在装配层）。
- quest_graph._apply match 微内核收编：图引擎自持 EffectRegistry 三 op（闭包活引用 _store 成员，run 换注入 store 仍成立）；emit_event 非五类留守（QD-5 接真 EventBus 另批）。
- dialogue_service 顺序冻结落地：_present_current 行 effects 先、trigger_events 老协议垫尾（12图 L82 冻结原文；选项 effects 次之由 select_option 时序天然成立）。
- 测试：test_effect_registry.gd 新建（五类可达/第六种拒/重复 op 拒/死命令/DSL/dict/装配表六 op 全景）；executor 测试补 quest_complete 端到端+套件状态残留清理（demo_quest 跨测试污染）；inline 测试适配 setup_effects 注入+FLAG 键改 story_ 前缀。
- 验证：--import 解析零错 → verify_all 十三槽全绿（套件 68 全过）。GATE32 拦新增信号→--fix-baseline 收编留痕。

### 设计要点（复用价值）
- **GATE32 收编参数是 `--fix-baseline`**（不是 --update-baseline，与 schema_guard 不同名）。
- EffectRegistry 与 kernel effect.gd 契约互补：契约定形、注册表定执行。
- 完整记录已写入 docs/更改日志.md 12图QD-2 行。

### 宪法合规自查（用户质询「effect_registry不可退役声明是否合规」）
- 结论：实质+程序合规（依据链：12图Freeze清单L161契约面 + 宪法§171收编不丢弃 + ADR_INDEX§2用户追认QD-1/QD-2 + handoff留痕程序四铁律全中）。
- 自查发现缺口并补齐：EffectRegistry与kernel effect.gd咬合关系未持久登记（handoff会被归档）→ 按GameFacts撞名处置先例补登ADR_INDEX §4（1387edf）：契约定形/域级注册表定执行互补、不可退役依据、Phase5迁移口、Phase D对齐走ACR。
- 关键区分：保护既决事项≠新立架构决策（不越「AI不是架构师」红线）；声明措辞含迁移口（可移动需广播），未锁死目录收敛。
