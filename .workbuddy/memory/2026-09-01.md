# 2026-09-01 工作日志

- 按新水墨视觉图重构主菜单：
  - 新建 `scenes/ui/components/wuxia_menu_button/WuxiaMenuButton.tscn` + `.gd`：hover 切换墨迹底板、图标金色 modulate 占位、缩放动画、音效走 AudioManager。
  - 重写 `MainMenu.tscn`/`MainMenu.gd`：加入 TitleGroup（标题 Logo/副标题）、5 个 WuxiaMenuButton 实例，保留 BaseScreen/UIManager/业务解耦。
  - 导入美术资源到 `assets/ui/main_menu/`：title_logo、title_sub、btn_hover_bg、icon_1~5。
  - 更新 `data/configs/localization/strings.csv` 文案为“开始游戏/继续游戏/设置/额外内容/退出游戏”。
  - 更新 `data/configs/ui/main_menu_layout.json` 增加 title_group 布局块。
  - 双闸门通过：GATE1 零错，GATE2 44/0。

- 主菜单开场动画（选项 2）：在 `MainMenu.gd` 加 `_start_title_breathing()`（标题上下浮动 ±7px + 轻微缩放呼吸，正弦缓动循环）与 `_play_enter_animation()`（菜单整列从左滑入 + 各按钮错峰淡入，仅动 alpha 避免和 WuxiaMenuButton 的 hover scale 1.04 打架；VBox 子节点不能 tween position，故整列滑入而非逐按钮位移）。GATE1 零错、GATE2 44/0。

- 【重要坑】WuxiaMenuButton 继承自 Button，原脚本声明 `@onready var icon: TextureRect` 与 **Button 原生属性 `icon`（Texture2D）重名** → 整脚本 Parse Error，主菜单实例化即崩。GDScript 4.x 对原生成员重定义是硬 Parse Error，且 `--check-only`/GATE1(`--quit`)/GATE2 都**不加载该脚本**时完全漏报（套件只报 44/0 假绿）。修复：改名 `icon_rect`（$icon 节点名不变）。诊断手法：用临时场景实例化 MainMenu 跑几帧打印节点，比盲猜快。已加 `tests/unit/test_main_menu_loads.gd` 强制加载 MainMenu+WuxiaMenuButton，堵住该盲点（GATE2 变 45/0）。
- 【规则提醒】Godot 沙箱门禁只读 git-HEAD 快照，**改完必须 commit 再跑 GATE1/GATE2**，否则看不到未提交改动（本会话因此先 commit 再验证）。

- 【素材透明通道陷阱】用户给的素材全是 PNG 透明底，但**通过聊天窗口粘贴/截图进来的图，系统会存成 JPG（无 alpha）**，透明背景因此丢失；而 **LocalSend 直传的 PNG 是完整 RGBA**。本会话一开始误用了聊天里落盘的 JPG 版（title_logo/btn_hover_bg/icon_1,3,4,5 全是 JPEG），后从 `D:\LocalSend\保存文件\` 取回真 PNG（笔触.png/笔触2.png/标题LOGO.png 均为 RGBA 透明）重新导入、改 `MainMenu.gd` 路径后缀为 .png、用 `godot --headless --editor --quit` 重建 .import。教训：**收到“透明 PNG”素材后，先验文件头（magic bytes）确认真是 PNG/RGBA，再落盘**；让用户用 LocalSend 而非粘贴传图。旧 JPG 已移入 `assets/ui/main_menu/_jpg_backup/` 留底。双闸门 45/0。

- 视觉微调（参考用户截图）：`WuxiaMenuButton.gd` 加 `TEXT_NORMAL/TEXT_HOVER` 常量，在 `_refresh_visual_state()` 里动态切换主/副文字颜色（常态 `TEXT_SECONDARY` 灰白，悬停 `GOLD_DARK` 暗金），**纯代码改色，无需新 PNG**；`WuxiaMenuButton.tscn` 按钮尺寸从 360×64 改为 440×72，`bg_panel` 仍以锚点填满按钮、`stretch_mode=KEEP_ASPECT_CENTERED`，把 3220×784 的大 PNG 框定在按钮范围内显示；`MainMenu.tscn` 的标题 Logo 和副标题加 `custom_minimum_size` + `size_flags_vertical=0`，限制 Logo 最大高度 140px、副标题 36px，避免 2880×1440 大 Logo 撑爆布局。双闸门 45/0。

- 修复 hover 墨迹底板铺屏 bug：根因是 `TextureRect` 默认 `expand_mode = KEEP_SIZE`，会按纹理原始尺寸（3220×784）显示，`anchors_preset=15` 也没能约束它，导致一张大图覆盖几乎整个屏幕。修复：`WuxiaMenuButton.tscn` 把 `bg_panel` 改为绝对定位 `offset_left=60/top=6/right=432/bottom=66`，并设 `expand_mode = EXPAND(2)` + `stretch_mode=KEEP_ASPECT_CENTERED`，将墨迹严格限制在按钮文字区域（icon 右侧到按钮右边缘），随按钮宽度等比缩放。双闸门 45/0。

- 修正标题 Logo 铺满全屏 + 笔触文字未居中：用户截图显示 Logo 仍铺满背景、笔触未把文字和图标包在中间。根因仍是 `TextureRect` 默认 `expand_mode=KEEP_SIZE`——这次漏在 `MainMenu.tscn` 的 `title_logo`/`title_sub`。修复：给标题 Logo/副标题都加 `expand_mode=2`；`WuxiaMenuButton.tscn` 把 `bg_panel` 扩展为覆盖整个按钮（0,0→440,72），并重新调整 icon/label_main/label_sub 的 offset 使文字+图标整体在按钮内垂直居中（icon 14~58；label_main 12~40；label_sub 42~60；整体中心约 36=按钮中心）。双闸门 45/0。

- 真正根因再定位 + 统一配置入口：用户后续截图仍显示 Logo 铺满全屏，排查发现 `MainMenu.gd::_build_title()` 在运行期把 `custom_minimum_size` 设成纹理原尺寸×0.45（2880×1440 → 1296×648），直接盖满屏幕。修复：`_build_title()` 改为固定最小高度 140/36、强制 `expand_mode=2`、水平收缩居中。同时响应用户「统一配置入口」需求：新增 `data/configs/ui/main_menu_assets.json`，`MainMenu.gd` 运行时读取其中的 `title_logo/title_sub/btn_hover_bg/icons`，不再硬编码路径；工作室工具「登录界面」新增「主菜单资源替换」面板（后端 API `/api/main_menu/assets/*`，前端上传替换 Logo/副标题/笔触底板/5 图标），并把 `title_group` 加入主菜单自由拖拽布局（四大块）。双闸门 45/0。
