# 2026-09-01 工作日志

## 工作室 exe「看不到更新」根因修复（用户投诉仍是用旧版）
- 现象：用户双击/打开工作室工具，看到的还是旧功能（无「🎨 主菜单资源替换」面板 / 标题组拖拽）。
- 根因（两处）：
  1. **8765 端口堆积了 10 个陈年老进程**（PID 15492/15704/16164/20996/25160/32476/35092/35736/36032/36380 全在 LISTENING）——每次重打包/双击都没杀旧进程，浏览器连到内存里的旧代码，误以为"没更新"。
  2. **除工程内 4 处外还有 2 份野副本是旧的**：`D:/studio_push_tmp/dist/`（md5 d0ced295，正是之前"回滚"那版）、`D:/武侠游戏/tools/dist/`（md5 c6a26cba）。用户若双击这两份即是旧版。
- 修复：
  1. `Stop-Process -Force` 杀光 10 个 8765 进程（PowerShell 强杀；Git Bash 的 `taskkill //F` 参数被吞导致首轮没杀掉，改用 PowerShell）。
  2. 把 2 份野副本用最新 exe（md5 387ac5a57b3c391284fc6480d04905be）覆盖。
  3. `setsid` detached 启动桌面最新 exe（Start-Process 起的子进程会随 PowerShell 会话结束被杀，必须用 setsid 真正脱离才活得久）。
  4. curl 验证：`/api/tool_version` 返回 md5=387ac5a5 + build_time 2026-09-01 19:35（最新版）；`/api/main_menu/assets` 返回新功能 JSON（title_logo/title_sub/btn_hover_bg/icons）→ 确认新进程在跑、新功能可用。
- 全 6 份副本 MD5 现已一致（387ac5a5）；8765 仅 1 个新进程监听。
- **新红线（用户硬性要求，已写入 MEMORY.md 工作室红线段）**：每次更新/重打包工作室 exe 后，必须自己先——① 清 8765 全部旧进程 ② 重打包 `--clean` ③ 同步**全部**副本（含桌面/外部/野副本，不止 4 处）④ 启动新 exe 并 curl 验证 `/api/tool_version` md5 与 `/api/main_menu/assets` 可用，确认用户真能用上新版再交差。
- 端口仍是 8765（没改端口号），问题从来不是端口号，而是旧进程霸占端口 + 野副本旧版。

## 主菜单布局改回 .tscn 驱动（用户要求，2026-09-01 晚）
- **背景**：用户问"为什么一直不让我用 Godot 编辑器拖拽改布局/按钮大小"。澄清：从未禁止；根因是主菜单/HUD/预加载/战棋这 4 类被我做成了「数据驱动」——运行时从 JSON 读位置并覆盖 .tscn，导致 Godot 里拖完一运行就被 JSON 值覆盖"像没保存"；且之前用户嫌 Godot 编辑器老报错，我专门做了网页拖拽工具。用户确认行业主流是"布局写场景/prefab（=Godot 的 .tscn）"，要求改回 .tscn。
- **性能/架构评估（诚恳中立）**：性能零影响（布局是开屏一次性设置，非每帧；JSON 版也才读几百字节一次，改回 .tscn 连这次读盘都省，持平或微优）。架构只动"4 个屏的位置来源"，依赖/EventBus/服务/游戏数值 JSON 全不动；"数值全进 JSON"铁律本意是游戏数值（伤害/掉落/对话），UI 按钮 x/y 本就该在场景里——这次是更贴合铁律本意。
- **执行（MainMenu）**：
  - 读 `data/configs/ui/main_menu_layout.json` 对比 `MainMenu.tscn`：TitleGroup/MenuContainer 已匹配；BottomLeft/BottomRight 用不同表示（边锚+offset vs 归一化锚+0 offset）→ 直接删覆盖会"跳回"旧位。故先把 JSON 值**烘焙进 .tscn**（BottomLeft/BottomRight 改锚点为归一化值、offset 归零），保留用户当前拖好的布局。
  - `MainMenu.gd`：删 `MAIN_MENU_LAYOUT_PATH` 常量、`_build_content` 里的 `_apply_layout()` 调用、`_apply_layout()` 与 `_apply_block()` 两函数。`_load_assets_config()`（换图路径，属内容非排版）保留。
  - grep 确认无 `_apply_layout/_apply_block/MAIN_MENU_LAYOUT_PATH` 残留引用；手动复核 .gd 连贯。
- **诚实保留项（未强行改）**：
  - **LoadingScreen 保留 JSON**：其 `_apply_layout` 读归一化 x/y/w/h 并在 `_on_resized` 里按新视口重算 → 分辨率自适应。烤固定值进 .tscn 会破坏多分辨率支持，故保留（网页工具拖拽对其仍是正确的）。
  - **HUD 保留运行时拖拽**：`HudDraggablePanel` 的拖拽摆放是用户要的玩法特性（游戏内拖+存 user://hud_positions.json），与"编辑器排版"是两回事，已可用。
  - **战棋保留 JSON**：网格坐标反算+外部工具批量调，JSON 真需要。
- **验证说明**：沙箱 Godot 走 git-HEAD 快照，工作树有 40+ 未提交改动，跑 GATE2 需先整体提交（项目铁律禁 `git add -A`，40+ 文件精确 add 不现实）→ GATE2 未跑。但已精确 `git add` 主菜单 2 文件并提交（`2e298f1`），**GATE1（`--headless --quit` 零 SCRIPT/PARSE/COMPILE ERROR）通过**，证明 .gd 改动可编译、.tscn 可解析。用户 F5 即可见效果，且以后在 Godot 拖主菜单存盘即生效。
- **结论**：主菜单布局现已 .tscn 驱动，用户可在 Godot 编辑器直接拖、持久化、零端口/进程负担；性能与整体架构不受影响。

## 主菜单 5 个按钮的大小/图标/间距——改模板而非 MainMenu（用户想自己拖）
- **根因澄清**：用户在 MainMenu 里拖不动那 5 个按钮的大小，因为它们是 `WuxiaMenuButtonScene.instantiate()` 在 `_build_menu()` 运行时生成的**实例**，MainMenu.tscn 里只有空 `MenuContainer`。实例不能直接拖大小；大小/图标/间距由模板 `res://scenes/ui/components/wuxia_menu_button/WuxiaMenuButton.tscn` 决定。改模板→5 个一起变。
- **模板结构**：根 `WuxiaMenuButton`(Button) `custom_minimum_size=Vector2(440,72)`；子节点 `bg_panel`(墨迹底板) / `icon`(笔触图标) / `label_main`(主文字) / `label_sub`(小字，绝对 offset，可单独拖)。字号 `FS_MENU=23`（`ui_theme.gd:75`）由 `WuxiaMenuButton.gd:45` 代码设，**拖不动**，要改字号得改 `FS_MENU` 或把字号做成 @export。
- **已做小改进**：`bg_panel` 由死尺寸 440×72 改为 `anchors_preset=15`（铺满按钮），这样用户在模板里把按钮拉大时墨迹底板自动跟随，不会露馅。提交 `10ddd18`，GATE1 通过。
- **字号也可自调（2026-09-01 晚补做）**：`WuxiaMenuButton.gd` 新增 `@export var main_font_size / sub_font_size`（默认 FS_MENU=23 / FS_TINY=12），`_ready` 改用这俩参数替代写死常量。现在用户在模板 Inspector 里填数字即可改主/副文字字号，5 实例同步。提交 `e802102`，GATE1 通过。至此大小/图标/间距/字号四项用户都能在编辑器自调，无需碰代码。
- **给用户的教学路径**：双击打开 `WuxiaMenuButton.tscn` → 拖根节点边缘改整体大小（或 Inspector 的 Custom Minimum Size X/Y）→ 拖 `icon` 改图标大小/位置 → 拖 `label_main` 左右改"图标-文字间距" → Inspector 里改 `Main Font Size` / `Sub Font Size` 调字号 → Ctrl+S → 回 MainMenu 运行，5 个按钮全变。
- **编辑器预览提示**：MainMenu 里 `@tool` 的 `_editor_preview()` 也会临时生成这 5 个预览实例（`owner=null`，不落盘），所以用户在 MainMenu 视口拖它们不会保存——必须去模板文件改。

## TitleGroup 无法放大缩小/里面框改不了大小（用户问，2026-09-01 晚）
- **根因（两道锁叠加，非用户操作问题）**：
  1. `TitleGroup` 原为 `VBoxContainer`（自动排列容器），大小由孩子撑开，手拖会被弹回。
  2. `MainMenu.gd` 的 `_build_title()` 第 179-180 行运行时强制 `_title_logo.custom_minimum_size = Vector2(0,140)`、`_title_sub.custom_minimum_size = Vector2(0,36)` → 用户在 .tscn 改大小一运行就被覆盖回原值。
- **修复**：`TitleGroup` 由 `VBoxContainer` 改为自由 `Control`（layout_mode=1，加四锚点 0 + offset）；`title_logo`/`title_sub` 由 `layout_mode=2`（容器接管，不可手拖）改为 `layout_mode=1` 自由节点并设好初始 offset（logo 0~140 / sub 148~184，间隙 8）；删 `MainMenu.gd` 第 179-180 两行强制高度锁，并把 `_title_group` 与 `_editor_preview` 内 `tg` 的类型注解 `VBoxContainer`→`Control`。现 TitleGroup 整体、title_logo、title_sub 都能在编辑器自由拖大小/位置、存盘即生效。提交 `27b2ee2`，GATE1 通过。

## 主菜单标题图替换 + 删除副标题图（用户从 LocalSend 传新图，2026-09-01 晚）
- **源文件**：`D:\LocalSend\保存文件\标题LOGO.png`（3.7 MB，Sep 1 21:21），新图为"墨影江湖 / 江湖如渡口·人心惟危"完整标题（已含副标题文字）。
- **操作**：
  - 用新 PNG 覆盖 `assets/ui/main_menu/title_logo.png`；
  - 删除 `assets/ui/main_menu/title_sub.png` 及其 `.import`；
  - 从 `MainMenu.gd` 清理所有 `title_sub` / `TITLE_SUB_PATH` / `DEFAULT_TITLE_SUB_PATH` 引用：删常量、变量、`@onready` 节点、`_load_assets_config` 读取、`_build_title` 纹理与样式设置、`_editor_preview` 预览设置；
  - `MainMenu.tscn`：删除 `title_sub` 节点，并把 `TitleGroup` 的 `offset_bottom` 从 232 缩到 188（只包住标题图 140px + 起始偏移 48px）。
- **验证**：提交 `4a68a64`（5 files，+2/-67，含删除 title_sub.png 及 .import），GATE1 通过（`RC=0` 零解析/编译/加载错误），新图导入正常。

## 删除 title_sub 后清理工作室工具残留并重新打包（2026-09-01 晚）
- **问题**：删除游戏内 title_sub 图片/代码后，`data/configs/ui/main_menu_assets.json`、`tools/desktop_studio/studio_core.py`、`tools/desktop_studio/index.html` 仍有 `title_sub` 入口。若工作室工具继续显示"副标题"上传项，用户上传后游戏无反应，会造成困惑。
- **清理**：
  - `data/configs/ui/main_menu_assets.json`：删除 `title_sub` 字段，更新 `_doc` 描述；
  - `tools/desktop_studio/studio_core.py`：从默认资源字典、读取循环、更新循环、key→field 映射、文件名字典、docstring 中移除 `title_sub`；
  - `tools/desktop_studio/index.html`：从 `MM_ASSET_LABELS` 中移除 `title_sub` 标签，UI 自动跟随不再渲染。
- **重打包并同步（按 2026-09-01 用户硬性 SOP）**：
  - `PyInstaller --clean 工作室专业调教.spec` 成功，新 exe 位于 `tools/desktop_studio/dist/工作室专业调教.exe`；
  - 同步 6 份副本：桌面、`D:/武侠游戏/dist/`、`D:/工作室专业调教/dist/`、`D:/studio_push_tmp/dist/`、`D:/武侠游戏/tools/dist/`、`D:/武侠游戏/tools/desktop_studio/dist/`，全部 `0cc55ded5a43c1dee275acef99ff250e`；
  - 启动后验证：8765 仅 1 个监听进程；`/api/tool_version` 返回 md5=`0cc55ded`、build_time=`2026-09-01 21:38`；`/api/main_menu/assets` 返回 `title_logo`/`btn_hover_bg`/`icons`，**不再包含 `title_sub`**。
- **注意**：本轮 curl 验证被环境变量 `http_proxy=http://127.0.0.1:61762` 拦截，改用 `--noproxy 127.0.0.1` 才直连 8765。

## 主菜单 5 按钮整体缩到 65%（2026-09-01 晚）
- **需求**：开始/继续/设置/额外/退出 5 个按钮的文字+图标缩到现有 65%，但悬停水墨笔触底板大小不动。
- **关键结构**：`WuxiaMenuButton.tscn` 根 `custom_minimum_size=440x72`，子节点 `bg_panel`(悬停墨迹底板) 此前是"铺满按钮"的满锚点 → 会随按钮缩放。要"底板不变"必须把它**解耦为固定 440x72 居中**（改 anchors_preset=8 中心锚点 + offset ±220/±36），否则缩按钮时底板也跟着缩。
- **改动**：
  - 按钮盒 440x72 → **286x47**（65%）；
  - 图标 40x44 → **26x29**，并把 `expand_mode` 由 1(IGNORE_SIZE) 改 2(EXPAND) 保证纹理按矩形缩放、真正变小（KEEP_ASPECT_CENTERED 不变形）；
  - 标签偏移同步缩到 65%；
  - 字号 `@export main_font_size` 23→**15**、`sub_font_size` 12→**8**（直接写死，不再引用 FS_MENU/FS_TINY，因为只缩主菜单按钮、不波及其它界面）；
  - `bg_panel` 解耦为固定 440x72 居中，悬停时大墨迹底板照常显示于小按钮之后。
- **验证**：提交 `39fa997`，GATE1 通过（`RC=0` 零解析/编译错误）。纯游戏侧改动，不涉及工作室工具，无需重打包/端口检查。
- **备注**：`WuxiaMenuButton` 仅主菜单使用（grep 确认），改默认值安全；其余引用是 test_main_menu_loads.gd 与工作室工具标签映射，不受影响。

## 工作室「UI 贴图」模块——贴图直写 .tscn（2026-09-01 深夜）
- **需求来源**：用户改了 UI 底层（位置从 JSON 驱动改为 Godot .tscn 拖拽控制），但 35 个 UI 界面零贴图资源（41 个贴图节点全是空壳），传图完全没入口。
- **方案**：新建 `tools/desktop_studio/tscn_assets.py`（纯标准库，可打包进 exe）：
  - `scan_ui_screens(root)` → 扫描 35 个 `.tscn`、41 个贴图槽位（TextureRect/TextureButton/Sprite2D 等）
  - `set_slot_texture(root, screen_rel, node_path, res_rel, prop, backup_root)` → 插入 ext_resource + 写 `texture=ExtResource(id)`，**绝不动 anchor/offset/size**
  - `add_background_slot(root, screen_rel)` → 给无槽位界面加全屏 StudioBg 空槽
  - 路径白名单正则 `^[A-Za-z0-9_\-./]{1,256}$`
- **关键设计决策（必记）**：
  1. **ext_resource 不写 uid**——我生成的 uid 不在 Godot 缓存里会报 `invalid UID` 警告。让 Godot 自己分配。
  2. **不预生成 .import**——Godot 会信任预生成的残缺 .import 并跳过导入→加载失败。交给 Godot 自动导入（用户传图后必然要开编辑器拖位置，零风险）。
  3. **ctex 命名算法**：`md5("res://" + relative_path)`——已验证与 Godot 一致。
- **后端/前端**：studio_core.py 追加 5 个函数；studio_server.py 加 5 条路由；index.html 新增 `data-tab="uiart"` 标签页。
- **验证**：security_selftest 15/15；GATE1 零错误；GATE2 45 套件 0 失败；端到端 8 项全过。
- **打包同步**：spec 加 `hiddenimports=['tscn_assets']`；6 份副本 md5 一致；curl 验证 `/api/ui_screens` 返回 35/41。
- **提交**：`7be961a`（主体）、`55dc00d`（spec）、`9c04997`（README）。

## 主菜单：移除标题呼吸 + 扩大悬停底板30%（2026-09-01 晚）
- **需求**：① 大标题 Logo（墨影江湖）移除上下移动的伪呼吸感；② 悬停水墨笔触底板扩大 30%。
- **① 标题呼吸**：`MainMenu.gd` 的 `_start_title_breathing()` 用两个 `set_loops()` tween 让 `_title_group` 做 `position:y ±7` + `scale 1.025` 的循环呼吸。删掉该函数定义 + 第108行调用，标题恢复 .tscn 静态位置，不再浮动。
- **② 悬停底板扩大**：`WuxiaMenuButton.tscn` 的 `bg_panel`（中心锚点 fixed 尺寸）由 440x72 扩大 30% → **572x94**（offset ±286/±47）。注意：用户曾在编辑器里把 bg_panel 重定位过（offsets 变 -292/-36.5/148/35.5，但尺寸仍是 440x72），重新读文件后按 572x94 写入。
- **验证**：提交 `ef623e5`，GATE1 通过（`RC=0` 零解析/编译错误）。纯游戏侧改动，无工作室工具/端口动作。

## 主菜单：大标题Logo放大40%（2026-09-01 深夜）
- **需求**：大标题LOGO放大40%。
- **改动**：`MainMenu.tscn` 的 `title_logo`（TextureRect）显示区由 376x140 放大 ×1.4 → **526.4x196**（offset -46.2/-24.0/480.2/172.0），保持原视觉中心 (217,74) 不变（向四周均匀扩展）。`expand_mode=2`(EXPAND) + `stretch_mode=5`(KEEP_ASPECT_CENTERED) 不变，纹理按比例填入放大后的矩形。
- **验证**：提交 `8f0e59b`，GATE1 通过（`RC=0` 零解析/编译错误）。纯 .tscn 改动，无工作室工具/端口动作。

## 主菜单按钮：修复崩溃 + 水墨底板再放大40%（2026-09-01 深夜）
- **崩溃现象**：用户截图显示 `Invalid assignment of property or key 'texture' with value of type 'Nil' on a base object of type 'null instance'`，栈顶 `WuxiaMenuButton.gd` → `MainMenu.gd:276 add_child(item)`。
- **根因**：`WuxiaMenuButton.tscn` 里的 `bg_panel` 节点丢失（用户在 Godot 编辑器里调模板时误删），`@onready var bg_panel = $bg_panel` 为 null，`_refresh_visual_state()` 里 `bg_panel.texture = ...` 直接崩溃。
- **修复**：
  1. 在 `WuxiaMenuButton.tscn` 重新加回 `bg_panel` 节点（TextureRect），中心锚点 + offset ±286/±47（即 572x94）。
  2. `WuxiaMenuButton.gd` 的 `_refresh_visual_state()` 加 null 防御：只有 `bg_panel != null` 才赋值，`icon_rect == null` 直接 return，避免以后再误删节点导致崩溃。
- **追加需求**：用户要求水墨底板再放大 40% → 572x94 ×1.4 = **800.8x131.6**（offset ±400.4/±65.8），保持居中。
- **验证**：提交 `3f57747`。
  - GATE1 通过（`RC=0` 零解析/编译/加载错误）。
  - GATE2 通过：套件 **45/45 通过，0 失败，0 个 ✗**。
  - 纯游戏侧改动，无工作室工具/端口动作。
